{
  "name": "dear-embeiu-rules-tests",
  "private": true,
  "description": "Unit tests cho Firestore + Storage security rules (chạy trên Firebase emulator).",
  "scripts": {
    "test": "mocha"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.14.0",
    "mocha": "^11.7.6"
  }
}
