---
description: Hiện portfolio tất cả feature + trạng thái hiện tại
argument-hint: [tên feature để xem chi tiết]
---
Đọc `project/ROADMAP.md` và tóm tắt gọn cho user:
- Feature đang ở pha nào (📋/🎨/💻/🧪/✅), cái nào **đang chờ user smoke-test thiết bị**, cái nào chờ **deploy rules/functions**.
- Nợ kỹ thuật P0 nổi bật.
- Backlog NOW kế tiếp.

Nếu user nêu tên feature (`$ARGUMENTS`) → đọc thêm `project/features/<ten>/roadmap.md` + `overview.md` và báo chi tiết phase/việc còn lại của riêng nó. Tên gần đúng thì tra trong ROADMAP.md để khớp.
