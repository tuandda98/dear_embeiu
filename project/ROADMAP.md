# 🗺️ ROADMAP (portfolio) — Index tất cả feature

> **Cấp dự án:** toàn cảnh mọi feature + pha hiện tại. Roadmap CHI TIẾT của từng feature nằm ở `features/<ten>/roadmap.md` (chia phase Now/Next/Later riêng).
> Cập nhật trạng thái ở đây mỗi khi feature đổi pha.
> Trạng thái: `📋 Spec` → `🎨 Design` → `💻 Dev` → `🧪 Test` → `✅ Done` · (`⏸️ Paused`, `❌ Dropped`)
> Ưu tiên: P0 (chặn release/giá trị cao) · P1 · P2.

## Feature đã ship (v1.0.0) — đã tài liệu hoá làm baseline

> Đã ✅ ship; còn **nợ kỹ thuật** ghi trong `overview.md` mục "Nợ kỹ thuật" của từng feature (Tester nên chạy, Dev nên siết trước/sau release Play).

| Feature | Ưu tiên | Trạng thái | Nợ kỹ thuật nổi bật | Spec / Roadmap riêng |
|---------|---------|-----------|---------------------|----------------------|
| Auth (tài khoản) | P0 | ✅ Shipped · 🧪 **Đợt 1 PASS code-level (2026-06-05): Quên mật khẩu + Xác thực email bắt buộc** (link Firebase, hard-gate có grandfather user cũ, anti-enumeration) — chờ 6 smoke-test runtime · ✅ **Flow hardening (2026-06-05): #2 re-auth tại chỗ khi xoá tài khoản · #3 listener auth-state (session thu hồi tự về guest) · #4 gộp Splash/AuthGate** — analyze sạch, chờ smoke-test · ⏳ **Đợt 2: Google/Apple** (đã chốt, chờ user setup Console/Xcode 2× dev+prod; Apple bắt buộc kèm Google iOS — App Store 4.8) | 🔴 local password plaintext; validation yếu · ✅ email verify (Resend callable) **đã deploy cả dev+prod** (2026-06-05, secret dùng chung) | [spec](features/auth/overview.md) · [test](features/auth/test.md) · [roadmap](features/auth/roadmap.md) |
| Coupling (mã mời) | P0 | ✅ Shipped | 🔴 invite-code enumeration; coupleId sửa được · 🛡️ **Hardening 2026-06-04: chống `coupleSavePermissionDenied` cho user thật** (PO-gate PASS) — profile edit chỉ ghi 7 field sửa-được (`toProfileEditPayload`, không re-send memberIds/status/inviteCode cũ → hết fail `isCoupleProfileEdit` khi local stale); create/join/leave dùng `authUid` chuẩn + user-write narrow (`toCoupleMembershipPayload`); auto-recovery 1 lần (refresh token + re-fetch). KHÔNG đổi rules. | [spec](features/coupling/overview.md) · [dev](features/coupling/dev.md) |
| Counter (đếm ngày yêu) | P0 | ✅ Shipped | 🔴 ngày không đổi theo ngôn ngữ (gap A) | [spec](features/counter/overview.md) · [roadmap](features/counter/roadmap.md) |
| Gallery (ảnh chung + push) | P0 | ✅ Shipped | 🔴 push hardcode VI (gap B); 🟡 ai cũng xoá ảnh partner | [spec](features/gallery/overview.md) · [roadmap](features/gallery/roadmap.md) |
| Reminders (local) | P1 | ✅ Shipped | 🟡 permission fail im lặng; DST | [spec](features/reminders/overview.md) · [roadmap](features/reminders/roadmap.md) |

## Đang làm

> **📌 Brainstorm 2026-09-05 (tối, chưa dev):** **care-message → timeline RIÊNG** (màn dòng thời gian quan tâm gom theo ngày, TÁCH khỏi Nhật ký câu hỏi, client-only) → [overview](features/care-message/overview.md). ❌ Ý "tự đặt câu hỏi hôm nay" đã **BỎ** theo user (2026-09-05): cả hai luôn trả lời câu hỏi của hệ thống, không ai đặt/sửa câu hỏi.

> **📌 Phiên 2026-09-05 (đợt 2 — 9 agent song song, user uỷ quyền chốt theo đề xuất) — 💻 Dev XONG `onboarding` + `endless-questions`, 🧪 Tester đang soi, PROD chờ lệnh:** **`onboarding`** (intro lần đầu + "cần cả hai": link tải trong tin mời + trang `dearembeiu.com/get`, checklist chờ partner + nhắc lại 24h/72h, feature tour theo version, intro 3 slide) → [overview](features/onboarding/overview.md) · **`endless-questions`** (engine 4 tầng: marker = nguồn sự thật, chọn theo `askedBankIds` thay `% n`, template thời gian/ngữ cảnh/lịch/mood/mốc, "nhìn lại" từ lịch sử, AI opt-in) → [overview](features/endless-questions/overview.md). ⚠️ Bank 229 câu sẽ lặp từ ~giữa 01/2027; thêm câu vào bank hiện tại = xáo trộn hoán vị mọi cặp.

> **📌 Phiên 2026-09-05 — `care-message` 🆕 (Quan tâm) + catch-up gate (gated):** (1) **Quan tâm**: user soạn title ≤60 + body ≤200 (6 chip gợi ý) → `couples/{id}/careMessages` → CF `notifyCareMessage` push **nguyên văn** + inbox `care_message`; entry 💌 header Home + tile Profile; tap → Home. Rules additive + 13 test (222 pass); ✅ DEV deployed, **PROD chờ lệnh**. ⚠️ User đổi ý: instant nudge từng gỡ (06-30) nay làm lại dạng này. (2) **Catch-up gate** ACCOUNT-GATED `thaohathao14@gmail.com`: quét 14 ngày quên (từ 2026-09-05) → modal chặn ép trả lời lần lượt + nhắc "Anh By <3" band 1140–1159 (9:30→21:30 mỗi 2h). Client-only. 3 agent Dev song song, analyze 0 · test 24/24. 💻 Dev XONG, ⏳ smoke-test 2 máy + build 1.6.0. → [care-message](features/care-message/overview.md) · [daily-question dev](features/daily-question/dev.md).

> **📌 Phiên 2026-06-30 — `partner-nudge` 🆕 (Nhắc người ấy):** A nhắc B qua thông báo (bù việc mọi reminder cũ đều local). **Gói gọn thành 1 TOGGLE "Cũng nhắc người ấy" trong form Lời nhắc Cài đặt** (KHÔNG màn riêng/instant nudge — đã thử rồi gỡ theo user). Bật → mirror lời nhắc sang `partnerReminders/` (liên kết qua `notifyPartner`+`partnerReminderId`; sửa/xoá/bật-tắt đồng bộ sang bản người ấy) → máy B watch+arm LOCAL band 3000–3049 theo tz B; CF `notifyPartnerReminderSet` confirm-push đánh thức B. Rules additive (`partnerReminders` author-owned) + test emulator 197 pass; client (model `partner_reminder` + service + `PartnerReminderProvider` chỉ-nhận + `CustomRemindersProvider` mirror + toggle form + wiring session_resolver); analyze 0. 💻 Dev XONG, ✅ DEV deployed, ⏳ smoke-test. → [partner-nudge](features/partner-nudge/overview.md).

> **📌 Phiên 2026-06-19 (đa-feature, chi tiết ở `dev.md` từng feature):**
> - **lunar-reminder** 🆕 (gated `dodaoanhtuan@gmail.com`): lịch âm offline (Hồ Ngọc Đức, 7 test pass) + nhắc mồng-1/rằm; v2 cho **chỉnh ngày (1..30) + giờ** + **màn lịch grid đầy đủ**. Local. analyze 0. CHƯA smoke-test. → xem dòng riêng bên dưới.
> - **mood** (Tâm trạng): icon TO khi **trùng tâm trạng** + redesign "đồng điệu" (glow + tên 2 người + tim đập); **push hiện luôn mood** ("Anh Tuấn đang thấy Nhớ", CF `notifyPartnerMood` + map nhãn vi/en) — DEV deployed; auto-nhắc tâm trạng đối phương xác nhận đã có & gentle.
> - **daily-question** 🐛 fix: Lottie/confetti reveal **phát lại mỗi lần mở app** → guard per-day Hive (`dq_reveal_seen_<coupleId>`), chỉ ăn mừng 1 lần/ngày.
> - **reactions** 🐛 fix QUAN TRỌNG: rule chỉ nested → `collectionGroup('reactions')` permission-denied ⇒ "A thả tim B không thấy" (chưa từng sync). Thêm rule recursive `match /{path=**}/reactions/{reactionId}`. Verify emulator. (Đã deploy DEV+PROD — theo log trước đó.)
> - **chat** (+chat-status, chat-background): phân biệt giọng-chữ header vs tin nhắn · chip title canh giữa + cao = nút back · read-receipt → **chữ "Đã xem · HH:mm"** (kiểu iMessage, bỏ icon đỏ) · legibility chữ trên ảnh nền (scrim) · deploy rules `receipts` DEV.
> - **love-tree**: bầu trời đêm **full-bleed** (sau header, full-width) · thử MVP "Bầu trời sao" rồi **revert về cây** (giữ code sao sau cờ `_useConstellation`).
> - **settings**: gỡ hộp note "Xoá toàn bộ dữ liệu chung…".
> - ⚠️ **Auto deploy-log:** hook `trace-firebase-deploy.sh` **chưa wire** trên máy này → các deploy DEV hôm nay (rules + `notifyPartnerMood`×2) **chưa có vết** trong `project/.firebase-deploy-log/` (chỉ live trên Firebase). PROD mọi feature 06-18/06-19 **chờ lệnh**.

| Feature | Ưu tiên | Trạng thái | Spec / Roadmap riêng |
|---------|---------|-----------|----------------------|
| **Lịch âm + nhắc mồng-1/rằm (lunar-reminder, gated dodaoanhtuan@gmail.com)** | P2 | 🚧 **Code-level xong (2026-06-19)** — âm lịch offline Hồ Ngọc Đức (`utils/lunar_calendar.dart`, 7 test pass) + thẻ Settings (ngày âm hôm nay + can-chi + mồng-1/rằm tới + toggle) + nhắc local mồng-1/rằm; **v2: chỉnh ngày (chip 1..30) + giờ (≤6) + màn lịch grid đầy đủ** (`lunar_calendar_screen.dart`). Band notif 1060–1099, cap ≤24 (iOS 64-pending), top-up mỗi app open. Gate theo email (pattern `_lunarCalendarEmails`). analyze 0. **Local, không backend. CHƯA smoke-test runtime.** | [overview](features/lunar-reminder/overview.md) · [dev](features/lunar-reminder/dev.md) |
| **Mood "Tâm trạng hôm nay" (daily hook)** | P1 | 🚧 **Code-level + DEV deployed (2026-06-19)** — card Home 2 chiều + picker 8 mood (Lottie) + CF `notifyPartnerMood` (gentle: chỉ ping khi mood đổi/lần đầu trong ngày, push-only). **[06-19 thêm]** icon TO + redesign "đồng điệu" khi trùng mood; **push hiện mood** ("X đang thấy <mood>"). rules-test 187. **PROD chờ lệnh.** | [overview](features/mood/overview.md) · [dev](features/mood/dev.md) |
| **Icon redesign toàn app (Lucide → Iconsax)** | P1 | ✅ **XONG TOÀN APP (2026-06-14)** — user "sweep tiếp tất cả, tự quyết". Đổi 199 usage Lucide/33 file → Iconsax (`iconsax_plus`), Linear mặc định / Bold nav-selected+accent+heart; UI hearts → Iconsax heart (giữ tim loader brand + check_rounded). `lucide_icons` **gỡ hẳn**. `flutter analyze lib` 0 issue, app build+chạy OK. Tự QA Home/nav. **Chờ user nghiệm thu các màn + commit.** Icon launcher/native-splash chưa đụng (cần asset). | [design](features/icon-redesign/design.md) · [dev](features/icon-redesign/dev.md) |
| **Profile redesign (Hành trình & Huy hiệu)** | P1 | 🎨 **Code-level xong (2026-06-14)** — Concept B (user chọn qua preview). Bỏ 4 ô stat trùng → `MilestoneTrail` (dải cột mốc curated, mốc đã-qua/tới/xa + "còn N ngày" + progress); bỏ "Tủ kỷ niệm" 2 menu phẳng → lưới 2×2 huy hiệu số-thật bấm-được (Chuỗi/Kỷ-lục→StreakSheet · Kỷ-niệm `photoCount`→tab Gallery · Nhật-ký→Journal). +`onRequestTab` ProfileScreen + 8 l10n vi/en. analyze 0. **Chờ user nghiệm thu trực quan + commit.** Client-only. | [design](features/profile-redesign/design.md) · [dev](features/profile-redesign/dev.md) |
| **Force update (bắt buộc cập nhật bản cũ)** | P1 | 🧪 **Code-level xong (2026-06-14)** — hard-only · Firestore `config/app` public-read · so build number toàn cục · FAIL-OPEN. Gate ở đầu `SessionResolver._resolve` (chặn cả guest) → `ForceUpdateScreen` (PopScope chặn back, nút mở store). `AppUpdateService` + route + l10n vi/en. analyze 0, rules-test 160 pass (6 ca config). **CHƯA deploy rules + CHƯA tạo doc `config/app` (chờ user) + chưa runtime-verify.** | [spec](features/force-update/overview.md) · [dev](features/force-update/dev.md) |
| **Design unify (đồng bộ toàn app theo ngôn ngữ Home v3)** | P0 | 🧪 **Test PASS code-level (2026-06-11)** — Designer audit 16 màn + token sheet + 11 primitives; Dev apply 15 màn (Stage A nền móng + 4 nhóm song song): 9 widget chung mới (`ContentCard`/`HeaderIconButton`/`subScreenAppBar`/`IconBadge`/`InkTile`/`EntranceReveal`…), bóng-tối mặc định cho page helpers, card nội dung trắng đặc r24 thống nhất, back-squircle 100% màn con, hết GestureDetector trần theo spec, vá 4 vi phạm Reduce Motion + cinema timer, NotifCenter bỏ glass-trong-list, Journal mint→blush, D3 DateFormat locale, nút primary pill h52. Analyze 0 issue + test 18/18. design-system.md đã cập nhật bản chuẩn. Chờ user smoke-test 7 mục runtime (test.md). | [spec](features/design-unify/overview.md) · [design](features/design-unify/design.md) · [test](features/design-unify/test.md) |
| **Chat (tab thứ 4 — trò chuyện riêng 2 người)** | P0 | 🧪 **PASS-có-điều-kiện code-level (2026-06-11→12)** — pipeline đủ Designer→Dev→Tester→fix: collection mới `couples/{id}/messages` (rules additive create-only + 10 case test, 154 pass), CF `notifyChatMessage` (push không lộ nội dung tin), tab 💬 index 1 + remap deep-link (photo→tab2, chat→tab1), bubble navy/trắng + composer + pending optimistic + phân trang 50+50 (cursor chỉ chốt từ server-snapshot), unread dot Hive seen-marker. **[2026-06-14] Love Note RETIRED hẳn** → Chat thay hoàn toàn; auto-migrate lịch sử cũ vào Chat (CF `mirrorNoteHistoryToChat` trigger realtime cho couple lệch phiên bản + `migrateLoveNotesToChat` callable backfill idempotent; client gọi lazy khi mở Chat); backend Love Note giữ cho app live (backward-compat). **DEV deployed rules+CF (gồm 2 hàm migrate); PROD CHỜ LỆNH.** Nợ minor: entrance seed, rules-test unauth, local Hive cap. Chờ smoke 2 thiết bị (nặng nhất: regression deep-link cold/warm). | [spec](features/chat/overview.md) · [design](features/chat/design.md) · [dev](features/chat/dev.md) · [test](features/chat/test.md) |
| **Chat background (ảnh nền đoạn chat)** | P2 | 🚧 **Code-level xong (2026-06-18)** — user "đổi nền chat bằng ảnh, chỉ duyệt ảnh kích thước hợp lệ". 1 mục Settings → picker `ChatBgScreen`: nguồn = ảnh gallery + cover, **lọc decode kích thước thật** (chỉ ảnh DỌC `h≥w` + cạnh ngắn ≥720px; ảnh ngang/nhỏ ẩn) + tile "Mặc định" xoá nền; single-select. Nền **dùng chung 2 máy** qua `prefs/home.chatBgPhotoId` (rules additive +`chatBgPhotoId` ≤200, emulator 172 pass). ChatScreen bọc `Stack[ảnh cover, scrim, hội thoại]`, watch + Hive cache offline. analyze 4 file feature sạch. **rules DEV deployed 2026-06-18; PROD chờ user + chưa smoke 2 thiết bị.** ⚠️ branch `phase3` có sẵn lỗi compile ở `love_tree_screen.dart` (WIP khác, không thuộc feature này). Cùng đợt: full-bleed nền + nút back frosted-disc trên ảnh tối (`HeaderIconButton.backed`) + header chat top 0 + gỡ avatar tin nhắn (chat 1-1). | [spec](features/chat-background/overview.md) · [dev](features/chat-background/dev.md) |
| **Chat status (đang gửi/đã gửi/đã nhận/đã đọc)** | P1 | 🚧 **Code-level xong (2026-06-18)** — user yêu cầu trạng thái tin nhắn. Subcollection mới `couples/{id}/receipts/{uid}` (`deliveredAt`/`readAt` serverTimestamp, id==uid, rules additive + 6 test, emulator 178 pass). `ChatProvider.statusOf` suy 4 trạng thái từ receipt của partner; ghi delivered khi nhận tin mới + read khi mở chat (`markSeen`); UI `_StatusLabel` (clock→tick→tick-đậm→tick-hồng) dưới tin cuối cùng mình gửi. analyze sạch. **rules DEV deployed; PROD chờ lệnh + chưa smoke 2 thiết bị.** | [spec](features/chat-status/overview.md) · [dev](features/chat-status/dev.md) |
| **Pagination (phân trang gallery / love-note history; journal có sẵn)** | P1 | 🧪 **PASS-có-điều-kiện code-level (2026-06-11)** — Gallery: watch realtime 30 ảnh mới nhất + loadMore cursor (infinite scroll, footer kết) + `count()` aggregate cho tổng + on-this-day query riêng theo ngày (Home cinema không phụ thuộc full-list); History: window 50 + Xem thêm; Tester đợt 1 FAIL 3 major (gap re-sync / rebuild-loop local / race nhiễm couple cũ) → Dev-fix 5/5 → PO-gate PASS. Nợ: BUG-6 startAfterDocument (theoretical). Chờ smoke 2 thiết bị (8 mục test.md). | [spec](features/pagination/overview.md) · [dev](features/pagination/dev.md) · [test](features/pagination/test.md) |
| **Couple code (mã ghép đôi riêng — fix rejoin flow)** | P0 | ✅ **Done (2026-06-05)** — tách `couple.coupleCode` khỏi `user.inviteCode`; fix Bug leaveCouple không update remaining member status; double-lookup join; rules `couple_codes` deployed dev+prod; 8/8 AC PASS | [spec](features/couple-code/overview.md) · [test](features/couple-code/test.md) |
| Custom reminders (reminder tuỳ chỉnh, local) | P1 | 🧪 Test PASS (smoke on-device OK) — gate D7 đang đổi sang force-open theo Reminders v2 | [spec](features/custom-reminders/overview.md) · [roadmap](features/custom-reminders/roadmap.md) |
| Reminders v2 (bỏ nudge hằng ngày + milestone tự bật/tắt + giờ-theo-mốc Dv8) | P1 | 🧪 Test PASS — chờ smoke-test thiết bị | [spec](features/reminders/overview.md) (5b) · [roadmap](features/reminders/roadmap.md) |
| Settings (màn Cài đặt tổng, gom Profile + giờ theo mốc Dv8) | P1 | 🧪 Test PASS (29/30) — chờ user smoke-test thiết bị (2026-05-31). **+Profile v2 + Settings v2 redesign (2026-06-11): Profile hero tap-to-edit + strip 3 chip + "Tủ kỷ niệm"; Settings grouped-list phẳng (gộp Thông báo, Tài khoản lên đầu + sign-out vào trong, xoá 2 mục trùng Profile v2, footer version) — dev xong, analyze sạch + test 18/18, chờ smoke-test** | [spec](features/settings/overview.md) · [roadmap](features/settings/roadmap.md) |
| Photo report (UGC compliance Apple 1.2) | P1 | ✅ Done (2026-05-31) — rules đã deploy | [spec](features/photo-report/overview.md) · [roadmap](features/photo-report/roadmap.md) |
| Guest mode (fix Apple reject 5.1.1) | P0 | 🧪 Test PASS code-level — chờ user smoke-test thiết bị (5 case runtime) (2026-06-01) | [spec](features/guest-mode/overview.md) · [roadmap](features/guest-mode/roadmap.md) |
| Invite sharing (copy/share mã mời — giảm ma sát ghép đôi) | P1 | 🧪 Test PASS code-level — chờ user smoke-test (Phase 1: copy/share cross-platform; QR=P2, link 1-chạm=P3 để sau) (2026-06-01) | [spec](features/invite-sharing/overview.md) · [roadmap](features/invite-sharing/roadmap.md) |
| Home engagement (đẩy đăng ảnh từ Home — North Star) | P0 | 🧪 Test PASS code-level — chờ user smoke-test (Phase 1: CTA "Thêm kỷ niệm" + empty-state; partner-signal/streak=P2) (2026-06-01) | [spec](features/home-engagement/overview.md) · [roadmap](features/home-engagement/roadmap.md) |
| **Couple journal (Nhật ký Q&A + Love Note 2 chiều — a2 giữ chân)** | P1 | 🧪 **Test PASS code-level (2026-06-04)** — màn Nhật ký câu hỏi (marker doc lưu text câu hỏi vi/en, chỉ hiện ngày cả 2 đã reveal, phân trang 30 + Xem thêm), Love Note card hiện thêm "Lời nhắn của bạn" (AnimatedSize), token `accentLavenderDeep`. Analyze sạch; **rules marker đã DEPLOY**. Chờ user smoke-test runtime 2 thiết bị. | [design](features/couple-journal/design.md) · [dev](features/couple-journal/dev.md) |
| Daily question (#5 — câu hỏi mỗi ngày kiểu SumOne, reveal sau khi trả lời) | P1 | 🚧 Dev xong — chờ test + deploy rules/functions (v1: ngày=local, reveal client-side, confetti, no-streak) (2026-06-02). **Vá nền 2026-06-04 (a1): bank 58→229 câu vi/en + no-repeat theo couple (FNV-1a+permutation, daysSinceEpoch) — PO-gate PASS (analyze sạch, 14/14 test, no-dup, no-ICU)** | [dev](features/daily-question/dev.md) |

| Lottie moments (animation "khoảnh khắc": ghép đôi / empty gallery / daily reveal — Đợt 2 polish) | P2 | 💻 Dev — `LoveLottie` + wire 3 moment, analyze sạch, **3/4 slot có file** (pháo hoa/tim/camera, placeholder); **chờ user smoke-test + preview-đổi tông**; milestone cần trigger (no-cờ, release theo branch) (2026-06-03) | [spec](features/lottie-moments/overview.md) · [dev](features/lottie-moments/dev.md) |
| **Analytics (event funnel — North Star)** | P0 | 🧪 **Test PASS code-level** (2026-06-03) — `firebase_analytics` + AnalyticsService no-context (no-op khi Firebase chưa sẵn/opt-out), 14 event + user props + screen_view, toggle opt-out (default ON), privacy posture (no-tracking, xcprivacy/policy), **đóng Gap G language**. Chờ runtime GA4 DebugView + user điền form Play/Apple console. Phase 2, no-cờ | [spec](features/analytics/overview.md) · [dev](features/analytics/dev.md) · [test](features/analytics/test.md) |

| **Daily question — thả react câu trả lời (feature: daily-question reactions)** | P2 | 🚧 **Dev xong 2026-08-22, chờ deploy backend + smoke-test 2 máy.** React ❤️😍😂🥹🔥👍 lên câu trả lời của NGƯỜI ẤY (không react câu của mình — chặn ở rules), hiện ở **Home sau reveal + Nhật ký**, có **push** `notifyDailyAnswerReaction`. Collection RIÊNG `answerReactions` (KHÔNG dùng lại tên `reactions` — sẽ lọt vào map reaction ảnh trên mọi bản đã phát hành). analyze 0 · test 24/24 · **rules-test 209** (+12). ✅ **PROD deployed 2026-08-22** (rules + indexes + CREATE CF `notifyDailyAnswerReaction`, verify functions:list; trace `20260821T185633Z`). Còn: smoke-test 2 máy thật sau khi store live. | [dev](features/daily-question/dev.md) |
| **Daily question reminder (b2 — local nudge trả lời câu hỏi)** | P1 | 🧪 **Audit thông báo → sửa HẾT (2026-08-09, 3 vòng)** — 39 case, **10 bug + 2 vấn đề copy, đóng 12/12**: BUG-1 người trả lời TRƯỚC nhận 4 noti SAI/ngày (vá 2 lớp: CF gửi `data.bothAnswered`+`contentAvailable` → client huỷ dải trong background isolate; + khi `iAnswered` bỏ 22h/23h) · **BUG-2 dải BACKSTOP 1020–1033 rolling 7 ngày từ MAI** (trước đó không mở app ⇒ KHÔNG có nhắc nào) · BUG-3 tách copy 23h · BUG-5 gate `coupleActive` · BUG-6 bỏ giờ trùng EOD · **BUG-7 CF tự stamp marker `bothAnswered` bằng Admin SDK** (hết mất-ngày khỏi streak/journal + hết race copy) · BUG-8 hint Settings · BUG-9 6 câu giọng · BUG-10 ẩn tile gated · V3 title riêng · V4 xoay vòng copy · + D7/D12/D13/D14. **Cố ý KHÔNG sửa (rủi ro > lợi):** múi giờ LDR · channel Android riêng · exact alarm. analyze 0 · **test 24/24** · rules-test 197. ⚠️ **CHỜ LỆNH deploy `functions:notifyDailyAnswer` prod** + build app mới + smoke-test 2 máy. | [dev](features/daily-question/dev.md) · [test](features/daily-question/test.md) |
| **Couple streak (b3 — chuỗi ngày kết nối, shame-free)** | P1 | 🧪 **Test PASS code-level (2026-06-04)** — PO-gate PASS (analyze sạch, thuật toán anchor+đếm-liên-tiếp+longestRun đúng, milestone one-shot guard Hive, fail-soft). Thuần client: marker `bothAnswered` set client-side trong submitAnswer (additive, KHÔNG deploy), StreakService (watch limit 180, filter client) + StreakProvider (5 state + đệm 1 ngày + milestone one-shot guard Hive). UI: StreakChip (footerExtra CounterCard), StreakSheet (explainer + celebration confetti/count-up 5 mốc), Journal summary. ~30 key l10n vi+en. Analyze sạch, fail-soft. Giới hạn v1: longest≤180 ngày, ngày lịch sử trước b3 không có cờ → streak tính từ giờ. | [design](features/streak/design.md) · [dev](features/streak/dev.md) |
| **Reactions ❤️ trên ảnh (b1 — siết vòng lặp 2 chiều, North Star)** | P1 | 🚧 **Dev xong full-stack (2026-06-04)** — subcollection `photos/{id}/reactions/{uid}`+collectionGroup watch, `ReactionProvider` optimistic+rollback, 6 emoji, 3 surface (feed bar / fullscreen on-dark / Home badge read-only), tap/double-tap/long-press picker + heart-burst. rules ADDITIVE + CF `notifyPhotoReaction` (D3 skip self, chỉ onCreate) + teardown + deep-link + i18n 8 key. analyze sạch, node-check PASS. **rules+functions(`notifyPhotoReaction`)+index ĐÃ DEPLOY (2026-06-04)**. ⚠️ **[2026-06-19] FIX bug nặng "A thả tim B không thấy":** rule chỉ nested → `collectionGroup('reactions')` permission-denied (reaction CHƯA TỪNG sync cross-device từ lúc ship). Thêm rule recursive `match /{path=**}/reactions/{reactionId}` (read, member-scoped); verify emulator; deploy DEV+PROD (theo log trước). | [design](features/reactions/design.md) · [dev](features/reactions/dev.md) |

| **Notification center (trung tâm thông báo + tap điều hướng) + Font unify** | P1 | 🚧 **Dev xong full-stack (2026-06-06)** — Firestore-backed inbox `users/{uid}/notifications` (CF `writeInboxNotifications` ghi trong cả 6 sender + cleanup `deleteAccount`), rules ADDITIVE + composite index + 13 rules test (suite 140 passing), client model/service/`NotificationInboxProvider`/`NotificationCenterScreen` + bell-badge header Home, tap→tab tái dùng `NotificationTapRouter` (vá bug `partner_left` thiếu nhánh), l10n vi+en, render theo locale hiện tại. **+ Font: TOÀN APP 1 phông Be Vietnam Pro bundled (`assets/fonts`, gỡ google_fonts) — fix vỡ dấu tiếng Việt + nháy font iOS.** analyze sạch. **ĐÃ DEPLOY DEV (rules+index+functions); prod chờ lệnh user.** Chờ smoke-test 2 thiết bị. | [overview](features/notifications/overview.md) · [dev](features/notifications/dev.md) · [test](features/notifications/test.md) |
| **Home v2 (redesign Trang chủ — gộp header, reorder theo habit loop, consistency/a11y)** | P1 | 💻 **Dev xong (2026-06-10)** — implement đủ spec: header hợp nhất (greeting theo buổi + AnimatedCoupleName, xoá hero glass + title "Trang chủ"), reorder 3 nhóm Hôm nay/Kỷ niệm/Cột mốc (spacing 12/28, entrance order mới, giữ deep-link daily_question), recent tap→`openPreview` đúng ảnh (Hero `recent-photo-{id}`), on-this-day chữ tối (fix contrast), bell Semantics+InkWell, tile InkWell ripple, RefreshIndicator nền trắng, nav label 11px, skeleton mới, +5 key l10n vi/en. 1 file code (`home_screen.dart`) + 2 ARB; analyze sạch; KHÔNG backend/deploy. **Bổ sung theo user:** milestone progress gộp VÀO CounterCard (`progressFooter` slot mới, white-on-gradient) — bỏ section "Cột mốc" riêng, Home còn 2 nhóm; CounterCard redesign "aurora glass" + tên couple vào card + nền ảnh chung 2 người (adaptive scrim theo độ sáng ảnh); **"Hôm nay" v3 (2026-06-10): gộp 2 card thành `TodayRitualCard`** — tap-to-compose bottom-sheet, blur-teaser câu trả lời partner, done-state thu gọn, phong bì 💌 note chưa đọc (Hive seen-marker), 1 hàng link archive; +8 key l10n. Chờ Tester + smoke-test 2 thiết bị. **🎨 Design re-review hiện trạng (2026-06-10 cuối ngày, PHẦN 3 design.md):** 3 🔴 (C1 contrast calendar leaf ~1.5:1 · A1 reduce-motion · L1 CounterCard ~460px đè ritual card dưới fold) + 7 🟡 (P1 ticker tab ẩn, A2/A3/A4, C2/C3, L2/L3) + 3 thiếu (analytics cinema, trigger đăng ảnh, offline fallback) — **user duyệt "làm hết" → 💻 Dev implement TRỌN cùng ngày** (reduce-motion pass, TickerMode theo tab, leaf bóng tối, đồng hồ 1 dòng tổng-giờ, bỏ subtitle, hold-to-pause + Semantics + dedupe + placeholder cinema, camera thì thầm, event `memory_cinema_opened`; analyze sạch, CHƯA commit) — chờ smoke-test (kèm test Reduce Motion + VoiceOver). | [design](features/home/design.md) · [dev](features/home/dev.md) |
| **Pagination (phân trang gallery + love-note history)** | P1 | 💻 **Dev xong (2026-06-11)** — Gallery: watch window 30 realtime + infinite scroll `fetchOlderPhotos` (cursor Timestamp uploadDate, map tích lũy id — D1), partner-delete detect trong window, `count()` aggregate cho Profile/composer (D2), on-this-day query riêng theo năm + rewire Home cinema (D3, xử lý 29/02), footer shimmer + dòng kết `galleryEndOfFeed` (l10n vi/en). Love-note history: window 50 + nút "Xem thêm" 50/lần (cursor createdAt), screen chuyển provider-driven (`startHistory`/`stopHistory`), model `LoveNote` thêm `id`. Journal không đụng (D5/AC5). KHÔNG đụng rules/functions/index (D6). analyze 0 issue + 18 test pass. Chờ Tester + smoke-test 2 thiết bị (couple >60 ảnh / >100 note). | [overview](features/pagination/overview.md) · [dev](features/pagination/dev.md) |
| **App robustness (review toàn flow — chống freeze UI)** | P0 | 🧪 **Test PASS code-level (2026-06-04)** — PO-gate PASS, chờ runtime verify. **Pass 1 (cold-start + auth):** diệt splash-treo-~1phút — timeout 5s + fallback local + race 8s ở `session_resolver`, `getIdToken()` bỏ force, đẩy push-sync/lastSeenAt/device-prune chạy nền (fire-and-forget), defer init non-critical qua post-first-frame ở `main.dart`; logout không kẹt (unregister token timeout/fire-and-forget, `signOut()` trả bool), delete account `BlockingLoadingOverlay` chống double-submit + callable timeout 60s. **Pass 2 (photo/gallery):** nén ảnh feed 1920/q85, `decodeWidth`→cacheWidth/memCacheWidth (hết jank/OOM), bỏ existsSync khi có remoteUrl, gallery error-state (hết nuốt lỗi), pull-to-refresh, batch upload tiến độ. analyze sạch, KHÔNG deploy. | [review+dev](features/app-robustness/dev.md) |

| **Chat (tab Trò chuyện — bottom nav thứ 4, thay tile Nhật ký lời nhắn)** | P0 | 💻 **Dev xong full-stack (2026-06-11)** — model/service/`ChatProvider` (window 50 realtime + Xem thêm 50, map+dedup, guard race sau await), `ChatScreen` (header 2 tầng chip ghim + history icon → LoveNoteHistoryScreen, bubble navy/trắng burst, divider ngày/gap≥60', pending .65+clock, composer card nổi r24 đĩa gửi navy, waiting-partner CTA), nav 4 tab + unread dot 10px (Hive `chat_seen_`), deep-link đổi index (photo→2, chat_message→1) cả push + notif-center, rules ADDITIVE `messages` (≤1000, immutable) + 10 rules test (suite 154 passing), CF `notifyChatMessage` (privacy: push không kèm nội dung tin), 13 key l10n vi/en, gỡ tile "Nhật ký lời nhắn" ở Profile. analyze 0 + 18 test pass. **ĐÃ DEPLOY DEV (rules + CF); prod chờ lệnh.** Chờ Tester + smoke 2 thiết bị (regression deep-link photo→tab 2 quan trọng nhất). | [spec](features/chat/overview.md) · [design](features/chat/design.md) · [dev](features/chat/dev.md) |

## Đã Done

| Feature | Ưu tiên | Trạng thái | Spec / Roadmap riêng |
|---------|---------|-----------|----------------------|
| 🌳 Cây tình yêu (Love Tree) | P1 (giữ chân) | 🚧 **v2.2 pivot "Cành hoa anh đào" (sakura) code-level xong (2026-06-18, analyze 0)** — user chê cây cũ "xấu quá" → đổi metaphor: bỏ tán-blob-xanh → 1 cành sakura nghiêng cong, blossom = mốc đính DỌC cành (5 cánh khía + nhuỵ vàng-kem + glow). Pivot THUẦN RENDER qua `SakuraBranchRenderer` (đổi 1 hằng `kTreeRenderer`, giữ `PaintTreeRenderer` rollback); GIỮ NGUYÊN milestone math/stage/tap→MomentSheet/sky-giờ-mùa/coach/share/bloom-anim/lastSeen/deep-link/Reduce-Motion. 1 nguồn geometry chung `_SakuraBranchGeometry` (painter + bloomPositions), anchor deterministic per-index `_bloomHash` (không Random). 0 key l10n mới. **CHƯA smoke-test runtime** (blossom đậu mép cành mọi stage/xoay máy + no-regression #1/#2/#3 + Reduce Motion). (v2 #1+#2+#3 code-level 2026-06-18; v1 ✅ Done 2026-06-14, Tester 12/12 PASS). **[2026-06-19]** bầu trời đêm **full-bleed** (sau header + full-width, `SkyBackdropPainter` tách lớp root); thử MVP "Bầu trời sao" (vô cực) rồi **revert về cây** (giữ code sao sau cờ `_useConstellation=false`). | [overview](features/love-tree/overview.md) · [design](features/love-tree/design.md) · [test](features/love-tree/test.md) |
| Language (đa ngôn ngữ) | P0 | ✅ Done (2026-05-31) | [spec](features/language/overview.md) · [roadmap](features/language/roadmap.md) |

> ✅ Gap G (analytics `language_changed`) đã đóng bởi feature **analytics** (2026-06-03).

## Hạ tầng & chất lượng (tooling, không phải feature sản phẩm)

| Hạng mục | Trạng thái | Ghi chú |
|----------|-----------|---------|
| **Firebase rules unit tests** | ✅ **Done (2026-06-06)** | 140 test cho Firestore+Storage rules (gồm 13 ca subcollection `notifications`) (`firebase_rules_test/`, `@firebase/rules-unit-testing`+Mocha trên emulator), phủ mọi match block + allow/deny theo catalog rủi ro Tester (IDOR, immutability, field-whitelist, transition couple, enumeration). Chạy: `scripts/test-firebase-rules.sh`. **Tự enforce:** Stop hook băm `firestore.rules`+`storage.rules`+`functions/index.js` → đổi thì chạy lại hết, FAIL thì chặn. Phát hiện: device doc bắt buộc có key `languageCode` (rules engine deny khi thiếu key). Chi tiết: [`firebase_rules_test/README.md`](../firebase_rules_test/README.md). |

## Backlog (chưa tạo folder — tạo khi bắt đầu làm)

Theo `../CLAUDE.md` mục 11 (Product roadmap):

**NOW**
- ✅ **Mood "Tâm trạng hôm nay" (2026-06-19)** — daily hook: card Home 2 chiều + picker 8 mood + push khi người ấy đổi mood. Built + DEV deployed, prod chờ lệnh. Xem `features/mood/`.
- ~~Analytics + event funnel~~ → đã có folder, **🧪 Test PASS code-level** (xem bảng "Đang làm").
- Onboarding/tutorial (giảm rớt bước ghép đôi)
- Reactions ❤️ trên ảnh (tận dụng push) — *mở rộng feature gallery*
- Day streak (đã stubbed l10n)

**NEXT**
- Home-screen widget
- Daily question (có thể dùng AI/Claude API)
- Shared calendar + đếm ngược sự kiện
- Dark mode

**LATER**
- Premium/subscription (sau khi có analytics + retention)
- AI features (caption/lời yêu, "ngày này năm xưa")
- LDR pack / chat / wishlist chung

---

*Khi tạo feature mới: copy `_templates/` (gồm `roadmap.md`) → `features/<ten-feature>/`, thêm dòng vào bảng phù hợp, xoá khỏi Backlog. Mỗi feature tự quản phase chi tiết trong `roadmap.md` của nó.*
