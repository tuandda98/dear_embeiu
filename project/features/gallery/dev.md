# 💻 Dev — Gallery

> Dev sở hữu. Đọc [overview.md](overview.md) + [design.md](design.md).

- **Trạng thái dev:** ✅ Đã implement (baseline)

## Đã implement
- `photo_service.dart` (CRUD + watch Firestore stream + upload Storage + captions), `photo_provider.dart` (watch/sync/add/delete/updateCaption), `storage_service.dart` (cache local). Feed UI + fullscreen viewer ở `gallery_screen.dart`. CF push partner-photo.

## Việc cần làm tiếp (từ nợ kỹ thuật)
- [ ] Push đa ngôn ngữ (gap B — chung feature [language](../language/dev.md)).
- [ ] Ngày feed locale-aware (gap A).
- [ ] Rules: chỉ author được xoá ảnh của mình (hoặc cả 2 đồng ý).
- [ ] Re-validate ảnh thực (magic bytes) thay vì tin contentType client.
- [ ] Re-upload ảnh offline khi online lại; check `existsSync` cache path; rollback optimistic khi server fail.
- [ ] Giới hạn size/số ảnh batch + giới hạn ký tự caption.
- [ ] Xoá dead code `MasonryGallery` nếu chắc không dùng.

## Nhật ký implement
- [2026-05-30] [PO] Khởi tạo doc; liệt kê việc cần Dev.
- [2026-06-01] [Dev] Notification tap deep-link: chạm push mở đúng tab. `push_notification_service.dart` thêm `NotificationTapRouter` (`ValueNotifier<int> pendingHomeTab`, -1=không có) + `_handleNotificationTap(RemoteMessage)` map `type` → tab (photo_posted→Gallery 1, partner_joined→Home 0, type khác/thiếu→bỏ qua). `initialize()`: `onMessageOpenedApp.listen(_handleNotificationTap)` (warm) + `getInitialMessage()` (cold). `home_screen.dart`: initState đọc pending (cold-start, set trước khi mount) + listen notifier (warm → addPostFrameCallback + guard mounted), consume sau khi áp dụng. Không thêm package, không navigatorKey. `flutter analyze` sạch.
- [2026-06-05] [Dev/Lead] **Fix "không đăng được hình".** `photo_service.dart` `addPhoto`: `putFile` giờ truyền `SettableMetadata(contentType: _contentTypeForExtension(ext))` thay vì để SDK tự suy. Storage rule bắt buộc `contentType.matches('image/.*')`; ảnh có đuôi lạ/thiếu (HEIC, file temp cache-dir) có thể bị SDK gán `application/octet-stream` → upload bị từ chối. Thêm helper map ext→image/* (png/gif/webp/heic/bmp/jpeg, default jpeg). analyze sạch, không đổi rules/native. (Phụ: dev project thiếu function — đã deploy đủ parity, xem coupling/dev.md.)
- [2026-06-14] [dev/lead] Consistent padding/margin (user chỉ màn Gallery): (1) thu `_floatingTopShowcaseMaxHeight` 296→256 — bỏ ~40px khoảng trống chết dưới card composer (header SliverPersistentHeader cao hơn nội dung thật ~226px; giữ buffer cho tên couple 2 dòng khỏi cắt); (2) thống nhất inset 3 card đầu gallery về 16 (composer 14→16, add-today-prompt 18→16, today-memories giữ 16). Gutter ngoài đã đồng nhất 16 sẵn (showcase/CTA/feed). analyze 0. CHƯA đụng `_MarqueeRow` (chip ticker tự cuộn → chip bị cắt mép là hiệu ứng cố ý, chờ user xác nhận có muốn đổi thành hàng tĩnh).
- [2026-06-14] [dev/lead] Theo user: chip "khoảnh khắc/Feed riêng" đổi từ `_MarqueeRow` (ticker tự cuộn — bị cắt mép, trông như lỗi padding) → `Wrap` tĩnh (2 chip đứng yên trong lề card, tự xuống dòng nếu locale dài). Xoá hẳn class `_MarqueeRow`/`_MarqueeRowState` (124 dòng) + gỡ import `app_motion` (hết dùng trong file). analyze 0.

- [2026-06-14] [dev] **"Đăng ảnh mới"→"Chụp hình" (camera trực tiếp) + "Thêm nhiều"→"Thêm hình" (user):** `_pickAndAddPhoto` đổi `ImageSource.gallery`→`ImageSource.camera` (rear) + BỎ dialog caption → chụp xong đăng thẳng feed (caption sửa sau qua edit ảnh). Áp cho cả 4 CTA gọi `_pickAndAddPhoto` (composer/FAB-compact/empty-state). Multi-add `_pickMultiplePhotos` GIỮ NGUYÊN (gallery + caption dialog) — chỉ đổi label. Labels: `postNewPhotoBtn` "Đăng ảnh mới"→"Chụp hình"/"Take photo", `addMultipleBtn` "Thêm nhiều"→"Thêm hình"/"Add photos", gen-l10n. **⚠️ NATIVE: thêm `NSCameraUsageDescription` vào `ios/Runner/Info.plist`** (bắt buộc, thiếu = crash khi mở camera). Android: image_picker dùng intent, không cần CAMERA perm. analyze 0 issue. (Composer luôn có cả Chụp hình + Thêm hình kể cả khi feed trống → vẫn vào gallery được.)

- [2026-06-14] [dev] **FIX crash "Chụp hình" iOS (user báo):** `pickImage(ImageSource.camera)` KHÔNG bọc try/catch → trên **iOS Simulator (không có camera)** ném `PlatformException` không bắt → crash. Vá: bọc try/catch quanh `pickImage`, lỗi → snackbar `cameraUnavailable` (key mới vi/en) thay vì crash. ⚠️ 2 điều người dùng cần biết: (1) **Simulator KHÔNG có camera** → "Chụp hình" luôn fail ở simulator (giờ hiện snackbar, không crash) — phải test trên **máy thật**; (2) cần **FULL rebuild** (không hot-reload) để iOS áp `NSCameraUsageDescription` — thiếu = hard-crash quyền (try/catch không cứu được, là native pre-check). analyze 0 issue.

- [2026-06-14] [dev] **"Thêm hình" → sheet DRAFT xem trước + caption chung (user):** trước đó multi-pick upload thẳng (không caption, không xem trước). Mới: `_pickMultiplePhotos` sau khi `pickMultiImage` → mở `showModalBottomSheet` `_GalleryDraftSheet` (modal trắng r28, lift trên bàn phím): hàng thumbnail ngang (Image.file r16, mỗi cái có nút x bỏ chọn) + 1 TextField caption chung (rose tint r18, maxLen 280) + nút "Đăng N ảnh" (accentLove pill h52, disable khi rỗng). Trả `_GalleryDraftResult(files, caption)`; null/empty = huỷ. Provider `addPhotosBatch` thêm param `caption` (áp cho MỌI ảnh trong batch, pass xuống `_photoService.addPhoto`). +3 key l10n (`galleryDraftTitle`/`galleryDraftCaptionHint`/`galleryDraftPostBtn{count}`) vi+en, gen-l10n. Camera 1-ảnh vẫn đăng trực tiếp không caption. analyze 0 issue.
- [2026-06-14] [dev] **CreatePostScreen — màn "Tạo kỷ niệm" full-screen (thay `_GalleryDraftSheet`, theo design.md §🆕):** MỚI `lib/screens/create_post_screen.dart`: `CreatePostScreen` (StatefulWidget, route `fullscreenDialog: true`) + `CreatePostResult{files, caption}`. Nền `dawnBlush` + `ContentCard` trắng r24; app bar custom [X `HeaderIconButton` trái · nút "Đăng" pill rose h40 icon `send` phải, disable khi 0 ảnh]; author row (`_buildCoupleAvatar` 44 + `AnimatedCoupleName` heart14 + meta `createPostAuthorMeta(displayName, today)`); caption `TextField` borderless minLines1/maxLines6/maxLength280 counter ẩn, hint 16 w400 tertiary, không autofocus; khu ảnh tự suy từ `_files.length`: 1 → ảnh lớn `AspectRatio(4/5)` r16 + hàng "Thêm ảnh từ thư viện" (icon `imagePlus`), ≥2 → `GridView` 3 cột r14 ô vuông (ô cuối "+ Thêm ảnh" icon `plus`) + dòng đếm `createPostPhotoCount`. Nút ╳ gỡ ảnh `InkResponse` đĩa black .55 viền trắng (24/grid, 30/single); gỡ ảnh cuối → `pop()` (huỷ). `PopScope canPop:false` → `_onClose`: chỉ hỏi discard (AlertDialog) khi caption không rỗng, rỗng → pop thẳng. Haptic selectionClick (gỡ)/mediumImpact (đăng). **gallery_screen.dart:** `_pickAndAddPhoto` (camera) BỎ đăng-thẳng → `_composeAndPost([file], user)`; `_pickMultiplePhotos` thay `showModalBottomSheet(_GalleryDraftSheet)` → `_composeAndPost(files, user)`. MỚI helper `_composeAndPost`: push `CreatePostScreen` → nhận `CreatePostResult` → `addPhotosBatch(paths, caption, progress)` + snackbar (giữ logic cũ). GỠ `_GalleryDraftSheet`+`_GalleryDraftResult` (~215 dòng cuối file); thêm import `app_user.dart`+`create_post_screen.dart`. **Camera giờ KHÔNG đăng thẳng — cả 2 luồng vào compose, đường ra THỐNG NHẤT `addPhotosBatch` (1 ảnh cũng qua batch).** +13 key l10n vi+en (`createPostTitle/AuthorMeta/CaptionHint/AddMore/AddFromLibrary/PhotoCount/Btn/RemovePhoto/DiscardTitle/Message/Confirm/Keep` + `today`), gen-l10n; key cũ `galleryDraft*` GIỮ làm orphan (chưa xoá). Không đụng provider/service/rules/native/Firestore — thuần UI + routing. analyze lib/ 0 issue. **Cần Tester soi:** (1) layout grid khi đúng 2 ảnh (3 cột cố định, 1 ô ảnh + ô thêm ở hàng đầu — đúng spec nhưng trông thưa); (2) `today` hardcode "Hôm nay" (spec chốt, không dùng DateFormat thật); (3) camera→compose→"Thêm ảnh"→≥2 ảnh tự chuyển grid; (4) discard dialog chỉ bật khi caption không rỗng (gỡ ảnh không tính); (5) caption-per-ảnh ngoài scope (1 caption/batch, giới hạn model).

- [2026-06-14] [dev] **Dựng CreatePostScreen theo spec Designer (Mode 1 orchestrate):** tạo `lib/screens/create_post_screen.dart` (`CreatePostScreen` full-screen `fullscreenDialog:true` + `CreatePostResult{files,caption}`) — nền dawnBlush + ContentCard trắng, app bar [X · nút "Đăng" pill rose disable khi 0 ảnh], author row (avatar couple + AnimatedCoupleName + "Đăng bởi … · Hôm nay"), caption borderless multiline (minLines1/maxLines6/maxLen280), khu ảnh tự suy: 1 ảnh→lớn 4:5 r16 + "Thêm ảnh từ thư viện"; ≥2→grid 3 cột r14 + ô "+Thêm ảnh" + dòng đếm; nút ╳ gỡ từng ảnh; gỡ hết→tự pop; `PopScope` hỏi discard khi caption≠rỗng. Gallery: `_pickAndAddPhoto`(camera) + `_pickMultiplePhotos` cùng gọi `_composeAndPost(files,user)` → push CreatePostScreen → `addPhotosBatch(paths, caption, progress)` + snackbar. **Camera giờ KHÔNG đăng thẳng — vào compose.** GỠ `_GalleryDraftSheet`/`_GalleryDraftResult` (~215 dòng). +13 key l10n vi/en (`createPost*` + `today`), gen-l10n; `galleryDraft*` thành orphan (giữ tạm). analyze lib/ 0 issue. Chưa rebuild — chờ user verify mắt.

- [2026-06-14] [dev] **Polish CreatePostScreen cho friendly + đồng nhất (user):** (1) **Header** thêm tiêu đề giữa `createPostTitle` "Tạo kỷ niệm" kiểu FB/IG (Stack: ✕ trái · title 18 w800 navy giữa · nút Đăng pill phải). (2) **Card** bỏ Divider cứng giữa caption↔ảnh, gọn spacing (author→caption 12, caption→ảnh 16). (3) **Caption** bọc fill mềm off-white (`textPrimary` .04, r16) + **DIỆT viền hồng**: theme `InputDecorationTheme` có `enabledBorder`/`focusedBorder` OutlineInputBorder accentLove → chỉ set `border:none` bị leak (đọc như box validation); override CẢ enabled/focused/error/disabledBorder = none + `filled:false`, minLines 2 cho thoải mái. analyze lib/ 0 issue.

- [2026-06-14] [dev] **CreatePostScreen v2 — header đồng bộ app + nút Đăng xuống đáy (user "làm sao back về? Đăng ở bottom, header đồng bộ toàn app"):** (1) Header bỏ app-bar custom (X+title+post) → dùng `SubScreenHeader` chip-only (back ← + chip "✦ KỶ NIỆM MỚI" `createPostBadge`, `onBack:_onClose`) ⇒ có nút BACK rõ ràng, giống mọi màn con. (2) Route đổi `fullscreenDialog:true` → push thường (slide phải, back ← hợp lý). (3) Nút "Đăng" full-width pill rose `accentLove` h52 thành **thanh dính đáy** (`_buildBottomPostBar` trong Column[header, Expanded(scroll), bottomBar]) — thumb-reach, keyboard đẩy lên. Back: caption rỗng→pop ngay; có caption→dialog discard (PopScope chặn back-gesture + onBack đều qua `_onClose`). Gỡ import `header_icon_button` (hết dùng). +`createPostBadge` vi/en, gen-l10n; `createPostTitle`/`cancel` thành orphan ở màn này. analyze lib/ 0 issue.

- [2026-06-21] [dev/lead] **"Đổi ảnh khác" — cập nhật lại ẢNH của một kỷ niệm đã đăng (user "ở phần thêm hình ảnh, cho phép cập nhật lại hình đã đăng"):** trước chỉ sửa được caption/xoá/report; nay thay được CẢ file ảnh (caption/ngày/người đăng GIỮ NGUYÊN). **Không đụng backend** — rules photo-update sẵn cho phép đổi `remoteUrl`/`storagePath` (chỉ chặn đổi `coupleId`/`authorUserId`/`uploadDate`); caption-edit đã chứng minh merge-set chạy. **`photo_service.dart`** `replacePhotoImage({currentUser, photo, localImagePath})`: upload file mới vào path mới `couple_photos/<coupleId>/<id>_<ms><ext>` (uniquifier → URL đổi, tránh CDN cache ảnh cũ) → merge-set `remoteUrl`/`storagePath`/`updatedAt` qua `toFirestoreUpdate()` → best-effort xoá storage cũ + file cache local cũ; fallback local-only swap cache. **`photo_provider.dart`** `replacePhotoImage(photoId, localImagePath, {currentUser})`: `_setLoading(replacingPhoto)` → service → `_photosById[id]=updated` (+ sync onThisDay) → notify; lỗi `PhotoSyncException`→`_errorMessage`+rethrow. **`gallery_screen.dart`**: `_replacePhotoImage(photo)` = `pickImage(gallery)` (chọn ảnh = xác nhận, không dialog thừa) → provider → snackbar `photoUpdatedSuccess`/`photoUpdateError`. Vào từ 2 chỗ: (a) popup menu feed thêm item `replacePhotoAction` (enum `_PhotoFeedAction.replaceImage`); (b) fullscreen preview thêm IconButton `gallery_edit` (`onReplaceImage`, cạnh nút edit caption). +5 key l10n vi/en (`replacePhotoAction`/`galleryReplacePhotoTooltip`/`replacingPhoto`/`photoUpdatedSuccess`/`photoUpdateError`), gen-l10n. analyze 0. (Như caption/xoá: cả 2 thành viên đều đổi được ảnh của nhau — nhất quán hành vi hiện có.)

- [2026-06-21] [dev/lead] **Redesign menu "⋯" ảnh đã đăng → action sheet on-brand (user "design lại tab chỉnh sửa chú thích/đổi ảnh khác/xoá ảnh"):** bỏ `PopupMenuButton` chữ trơn (icon không đồng bộ, cramped mép phải) → `IconButton(more)` mở `_showPhotoActionsSheet(photo)` (modal bottom sheet `cardSurface` r28, grab-handle, theo pattern `_showReportReasonSheet`). Sheet: **header** thumbnail 52 r14 (`SharedPhotoView`) + tiêu đề `photoOptionsTitle` "Tùy chọn ảnh" + dòng "Đăng bởi … · ngày" (reuse `_feedPostedByLabel`/`_formatFeedDate`); **nhóm 1** (rounded surfaceLight) = Chỉnh sửa chú thích (`edit_2`) · Đổi ảnh khác (`gallery_edit`) · Xoá ảnh (`trash`, **đỏ `AppColors.error`**, chỉ hiện khi signed-in — giữ gate cũ); **nhóm 2** tách riêng = Báo cáo ảnh (`flag`, rose); mỗi hàng = badge tròn 40 tint .12 + label 15.5 w600 + chevron `arrow_right_3`, divider hairline indent 64; nút **Huỷ** (`l10n.cancel`) đáy. Helpers mới `_buildPhotoActionGroup`/`_buildPhotoActionTile` + model `_PhotoActionSpec{icon,label,action,tint}`. Sheet trả `_PhotoFeedAction` → `switch` gọi đúng `_editCaption`/`_replacePhotoImage`/`_deletePhoto`/`_reportPhoto`. +1 key l10n `photoOptionsTitle` vi/en, gen-l10n. (Fullscreen preview vẫn dùng IconButton riêng — không đụng.) analyze 0.
