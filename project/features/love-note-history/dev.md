# 💻 Dev — Love note history

> Dev sở hữu. Spec gốc: [overview.md](overview.md).

## Nhật ký implement
- [2026-06-11] [Dev] **Fix composer xấu (user kèm screenshot "xấu quá designer"):** 3 lỗi — (1) **viền rose của `InputDecorationTheme` toàn app** (focus accentLove 1.4, hợp form nhưng trong chat bar đọc như lỗi validation) đè lên ô nhập vì chỉ override `border` → override đủ `border/enabledBorder/focusedBorder` = none, pill r22; (2) **thanh trắng không chạm đáy màn** (SafeArea bottom cắt → hở dải gradient dưới composer) → body SafeArea `bottom: false`, composer tự pad `MediaQuery.padding.bottom` (tự về 0 khi bàn phím mở — không double-pad) và nền trắng kéo qua home indicator như Messenger; (3) **nút gửi disabled = navy .35 nhìn như cục xám chết** → ghost state: rỗng = đĩa `surfaceLight` + icon `textTertiary`, có draft = navy + trắng, chuyển `AnimatedContainer` 180ms. analyze sạch + test 18/18.
- [2026-06-11] [Dev] **Nâng thành CHAT THẬT kiểu Messenger (user "cải tiến thành chat luôn"):** thêm **composer bar** dưới màn — strip trắng `cardSurface` shadow hắt lên (black .06 blur 12 offset(0,-4)) + TextField pill `surfaceLight` r20 (token input app; minLines 1/maxLines 4, **maxLength 140 counter ẩn** — khớp rules, hint `loveNoteSheetHint`, sentences-cap) + **nút gửi đĩa navy 44** (`LucideIcons.send` trắng 19; `ValueListenableBuilder` theo draft — dim .35 khi rỗng, spinner khi đang gửi; Semantics `loveNoteComposeCta`). Gửi = `LoveNoteProvider.setMyNote` (đúng flow ritual card Home: update note + append archive) → stream realtime tự đẩy bubble mới vào đáy; thành công → haptic + clear, lỗi (catch cả exception offline/rules) → GIỮ draft + snackbar key MỚI `loveNoteSendFailed` (en+vi, gen-l10n). Body → Column [Expanded(list), composer] — bàn phím đẩy composer, reverse-list tự neo đáy. Empty-state đổi copy sang `loveNoteStartChat` (mời gửi tin đầu — có sẵn ARB). analyze toàn repo sạch + test 18/18.
- [2026-06-11] [Dev] **Cho phép gửi TRÙNG (user chốt sau note known-semantic):** `LoveNoteProvider.setMyNote` bỏ điều kiện `trimmed != previousText` — mọi lần gửi non-empty đều append history ("yêu em" ×2 = 2 bubble, đúng ngữ nghĩa chat). An toàn cho flow Home: `_LoveNoteSheet` ở ritual card tự gate trùng/rỗng (pop false) TRƯỚC khi gọi provider nên không phát sinh duplicate từ đó. analyze sạch + test 18/18.
- [2026-06-11] [Dev] **Polish vòng 2 theo review senior-designer (user "làm tất" 4 mục):** (1) **bubble mình: gradient love→loveDeep → NAVY `textPrimary` đặc** — đỏ-trên-hồng vibrate (nền là tint của chính nó), trùng palette destructive trong app, trắng-trên-love chỉ ~3.2:1; navy = màu primary-action của app, contrast ~15:1; (2) **divider theo NGÀY** (1 dòng/ngày thay gap>30' — lời nhắn thưa nên gap-rule thành log) + UPPERCASE micro-caps + `textSecondary` w700 (textTertiary chìm trên gradient); hôm nay → giờ, năm nay → bỏ năm; (3) **góc trong cụm 6→10** (hết răng cưa); (4) **avatar chữ cái đầu partner** 24px tròn `primaryGradient` cạnh bubble CUỐI mỗi cụm trái (bubble trước chừa slot 24 giữ thẳng mép; lấy tên qua CoupleProvider — mình là creator thì partner = person2, fallback "♥"); maxWidth 0.74→0.72; skeleton chừa slot avatar. Áp lên bản đã có pagination (load-more giữ nguyên). analyze sạch + test 18/18.
- [2026-06-11] [Dev] **Redesign MESSENGER-STYLE (user kèm screenshot: "liền mạch, giống design của messenger"):** viết lại `love_note_history_screen.dart` — bỏ card đồng phục (tên + ngày trên TỪNG card) → **hội thoại liền mạch**: bubble mình = phải, gradient `accentLove→accentLoveDeep` chữ trắng (gradient để giữ contrast trên đầu sáng); bubble partner = trái, trắng đặc chữ navy + shadow black .05; KHÔNG tên per-message (chat 2 người — bên trái/phải đã mã hoá người gửi, xoá `_authorName`/`_HistoryCard` + import couple/content_card); **timestamp = dòng ngăn giữa cụm** (`_TimeDivider` 11 textTertiary center, gap >30' mới hiện; hôm nay → giờ, năm nay → bỏ năm, cũ → đủ ngày — D3 locale); bubble cùng tác giả liền cụm: cách 3px + góc phía người gửi dẹt 6 (đầu/cuối cụm tròn 18), khác cụm 10px; maxWidth 74%; **`ListView reverse: true`** — mở màn đứng ở tin MỚI NHẤT dưới đáy (provider newest-first → reversed chronological → feed back-to-front, header landing thành item trên cùng); skeleton thành bubble so le trái/phải; empty state giữ. Không key l10n mới. analyze sạch + test 18/18.
