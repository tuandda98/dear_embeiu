# Cây tình yêu (Love Tree) — dev log (Dev)

## Trạng thái
- [2026-06-18] [dev] **REVERT về CÂY** (user: "revert lại thành cái cây"). Tắt cờ `_useConstellation = false` → hero lại là `_TreeHero` (cây sakura, trên nền trời full-bleed như cũ). Code sao (`_ConstellationSky`/`_ConstellationPainter`) GIỮ NGUYÊN dạng dormant sau cờ (cả 2 path còn live, không dead-code, không unused) để revisit sau; bật lại = đổi cờ về `true`. analyze 0.
- [2026-06-18] [dev] **v3 MVP "Bầu trời sao kỷ niệm" (user chọn hướng vô cực = sao).** Thay PHẦN VẼ hero cây→trường sao, **blast radius tối thiểu**: thêm 2 class mới `_ConstellationSky` (StatelessWidget) + `_ConstellationPainter` (CustomPainter) sau `_TreeHero`; đổi call site trong `_buildBody` qua cờ `final bool _useConstellation = true` (non-const → `_TreeHero` vẫn live, rollback 1 dòng, không dead-code). **Tái dùng:** `reached` mốc + `nucleusColor` (màu theo loại) + `ambient` clock (twinkle) + `_openMomentSheet` (tap→sheet) + nền đêm full-bleed của screen. Mỗi mốc=1 sao; vị trí deterministic qua `_bloomHash` (meander đáy-trái→đỉnh-phải + wobble sin + jitter), nối nhau đường mờ (cũ→mới sáng dần); sao mới nhất (i≥newFromIndex) to+sáng+sparkle chữ-thập; twinkle = sin(ambient) lệch pha theo index, Reduce-Motion → tĩnh (clock=null). Tap = `GestureDetector` opaque 44px trong suốt đè lên sao. **GIỮ NGUYÊN** header/cards/copy/stage-text/share-card/coach + toàn bộ code cây (chỉ không gọi khi cờ bật). analyze 0, đã format. **CHƯA: pan/zoom (P2), chòm theo năm (P3), copy sao (P4 — hiện title vẫn 'Cây non'/'flowers' = placeholder prototype), share card sao (P5).** User verify hướng nhìn trước khi làm tiếp.
- [2026-06-18] [dev] **Full-bleed night sky (user: "full background cả header cả width, nhìn như bầu trời đêm 1/2 màn hình").** Bầu trời hết là thẻ vuông bo góc giữa màn → giờ **tràn full-width + lên sau header**, phủ ~55% trên, tan dần vào dawnBlush. **Cách FINAL (an toàn):**
  - **`build()` (root):** sky là **1 lớp RIÊNG ở đáy Stack** = `Positioned(top:0, left:0, right:0, height: screenH*0.55, child: IgnorePointer(CustomPaint(SkyBackdropPainter)))`, sau đó `SafeArea > Column[ header, Expanded(_buildBody) ]` (GIỮ layout gốc), rồi shareCard. → sky phủ sau status bar + header + full width; cây nằm trên.
  - **`_TreeHero`:** GIỮ NGUYÊN cấu trúc gốc (`SizedBox(treeHeight) > LayoutBuilder > Stack[tree, flowers, particles, confetti, coach]`), chỉ **BỎ lớp sky `Positioned.fill` của nó** → hero trong suốt, sky của root lộ qua. **Hệ toạ độ cành/hoa KHÔNG đổi.**
  - **`SkyBackdropPainter`:** gradient trải full chiều cao box + đuôi alpha 0 (`colors+[last.alpha0]`, stops `[0,.34,.60,1]`) → không vạch cắt, melt vào nền.
  - **Header legibility:** `SubScreenHeader` +param `onBackground` (nút back đeo đĩa frosted `backed`); love-tree truyền `onBackground:true` + share `backed:true`. Chip trắng vốn đọc được. → header đọc tốt cả 4 phase trời.
  - ⚠️ **LẦN 1 THẤT BẠI (đã bỏ):** thử nhồi sky vào trong `_TreeHero` (box cao `topInset+treeHeight`, sky `Positioned.fill` full-box + cây bọc `Positioned(bottom:0,height:treeHeight)`) + body full-bleed `Positioned.fill` + header overlay → **analyze 0 nhưng runtime MẤT cả cây lẫn nội dung, chỉ còn dải trời** (ảnh user). Rút kinh nghiệm: giữ `_TreeHero` nguyên, tách sky ra root.
  - KHÔNG đụng `_LoveTreeShareCard`. Thuần client, không backend. **analyze 0 (đã `dart format`). CHƯA smoke-test runtime sau bản FINAL — user verify lại.**
- [2026-06-18] [dev] **v2.2 "Cành hoa anh đào" (sakura) implement xong** theo `overview.md` "# v2.2" + `design.md` "# Design Spec v2.2". `fvm flutter analyze` (cả project) → **No issues found** (0 issue). Pivot THUẦN RENDER: GIỮ NGUYÊN `LoveTreeService` (milestone math + thresholds + `stageForFlowers` + `nucleusIcon/nucleusColor/petalEdge`), thứ tự `reached`, tap→`MomentSheet`, `lastSeen`/bloom-once, coach mark + caption, sky-theo-giờ + mùa, thẻ Khoe cây, deep-link, Reduce-Motion. CHƯA smoke-test runtime.
  - **Ranh giới renderer + ROLLBACK 1 hằng:** `kTreeRenderer` (cuối `love_tree_screen.dart`, ngay sau `PaintTreeRenderer`) đổi từ `PaintTreeRenderer()` → `SakuraBranchRenderer()`. Đổi lại 1 hằng này = rollback toàn bộ visual về v2.1 (cây tán blob). `PaintTreeRenderer`/`LoveTreePainter`/`_LoveFlower`/`_FlowerPetalsPainter`/`_Nucleus` GIỮ NGUYÊN trong file (chỉ dùng khi rollback). `_canopyBloomPositions` (= phyllotaxis v2.1) cũng giữ, dùng bởi `PaintTreeRenderer`.
  - **`LoveTreeRenderer` interface mở rộng:** thêm 2 method `bloomPositions(stage,size,count)` + `canopyGeometry(stage,size)` để layout hoa cũng renderer-agnostic. Top-level `bloomPositions(...)` giờ delegate `kTreeRenderer.bloomPositions(...)`. `_TreeHero`/`_ShareTree`/`_LoadingTree`/`_StateMessage` vẽ qua `kTreeRenderer.treePainter/flowerWidget` (trước hardcode `LoveTreePainter`/`_LoveFlower`).
  - **1 nguồn hình học cành chung** = `_SakuraBranchGeometry(stage,size)` (private). Cành chính = 1 cubic bézier P0`(.16w,.96h)` C1`(.30,.66)` C2`(.42,.30)` P3`(.74,.20)`, clip tới `tEnd` mỗi stage bằng de Casteljau (`_subBezier`) → cành ngắn là sub-curve THẬT của cùng đường. Nhánh phụ rẽ tại `tBranch` (young 1 / green 2 / bloom 3). Trả `segments` (mỗi `_BranchSegment` có `pointAt/tangentAt/normalAt/wHalf/length/outlinePath`) + `tipPoint`. **Cả painter (vẽ) lẫn `bloomPositions` (neo) gọi CHUNG geometry này** → blossom đậu đúng mép cành (không "đậu hụt").
  - **`_sakuraBloomPositions`:** phân bổ count blossom vào segments theo TRỌNG SỐ độ dài (remainder ưu tiên segment dài), dàn đều `base_t=(j+.5)/n` trong [tLo,tHi], jitter t ±0.04 + offsetMag (`wHalf + r*.55 + 0..5px`) + lệch vuông góc so le 2 bên — **TẤT CẢ randomness từ `_bloomHash(index)`, KHÔNG `Random()`/thời gian** ⇒ deterministic per-index (AC #3). Clamp trong canvas (margin=r+4). Anti-overlap pass cuối: blossom < 36px thì đẩy ra deterministic (không chồng hit-box). `_coachTargetIndex(positions)` GIỮ (chọn dy nhỏ nhất = gần ngọn cành).
  - **`_BranchSpec` (thay vai `_StageSpec` cho sakura):** seed `tEnd .34`/2 nụ kín · sprout `.52`/1 nụ · young `.74`+1 nhánh · green `.90`+2 nhánh+2 lá · bloom `1.0`+3 nhánh+3 lá+glow đỉnh. bark `#A8755C→#724631` (nâu ấm, không xám), gradient vuông góc trục (`_shadeFlowerColor`).
  - **`SakuraBranchPainter`** (public, qua `treePainter`): vẽ nhánh phụ→cành chính (FILL-outline thuôn + gradient khối + vân thân young+ + ngọn bo tròn), nụ kín (`_paintBud`: bầu hồng `#FFD6E0→#FF8FA3` + đài `#9FBF7A` + cuống), lá sakura RẤT ít (`#B8CBA0→#9FBF7A→#C9A8B0`). `skeleton=true` → cành xám phẳng `surfaceLight` (không nụ/lá/màu). `shouldRepaint` chỉ khi stage/season/skeleton đổi.
  - **Blossom mới** = `_SakuraFlower` (cùng chữ ký `_LoveFlower`: diameter box, nucleus center, bloom-in 540ms easeOutBack + nhuỵ-pop delay .22 + stagger 140ms, sway ±2°, hit-pad ≥44 `_flowerHitPad`+`GestureDetector` opaque). Painter cánh `_SakuraPetalsPainter`: **5 cánh khía chữ-V** (`_sakuraPetalPath`), gradient 3-stop `#FFF0F4→#FFD6E0→petalEdge(kind)`, viền trắng .40→.10, bóng lift dưới hoa. Nhuỵ `_SakuraNucleusPainter`: **glow mềm thay viền chip** + 7 chấm tua nhuỵ vàng-kem `#FFE6A8`/`#FFF4D6` + đĩa tâm gradient + (icon kind nhỏ trắng `nucleusIcon` đặt bởi widget ở tâm).
  - **Ambient:** đổi `_ParticlesPainter._paintFalling` từ giọt-lá → **cánh sakura rơi** (`_sakuraPetalPath` ~10px, gradient `#FFD6E0→#FF8FA3` xuân/bloom, `#F0B95A` thu) + lắc lư/xoay; GIỮ khung `_AmbientParticles` (đom đóm đêm, Reduce-Motion static rải quanh gốc, cap hạt).
- [2026-06-18] [dev] **v2 "Cây sống & kể chuyện" implement xong** (3 phần #1+#2+#3) theo `overview.md` "# v2" + `design.md` "# Design Spec v2". `fvm flutter analyze` (cả project) → **No issues found** (0 issue). Thuần client — KHÔNG backend/Firestore/Function/native đổi. CHƯA test runtime trên thiết bị, CHƯA rebuild app. v1 milestone math + thứ tự `reached` + `_bloomPositions`(phyllotaxis) + `lastSeen`/bloom-once + `canopyGeometry` GIỮ NGUYÊN — v2 chỉ THÊM tương tác + lớp vẽ + thẻ share.
- [2026-06-14] [Dev] Implement xong v1 LoveTreeScreen theo `design.md` + `overview.md`. `flutter analyze` (cả project) **0 issue**. CHƯA test runtime trên thiết bị, CHƯA rebuild app. Thuần client — KHÔNG backend/Firestore/Function/native đổi.

## v2 — file tạo/sửa (2026-06-18)
- **`lib/widgets/moment_sheet.dart` (TẠO MỚI)** — `MomentSheet` (StatelessWidget) = bottom sheet "Khoảnh khắc này" (#1). Khung sheet chuẩn app (ClipRRect top r28 + BackdropFilter blur(20) + `cardSurface` + grab handle 40×4 + `SingleChildScrollView` phòng tràn + pad `viewInsets.bottom`), khớp `StreakSheet`/`MemoriesSheet`. 2 entry tĩnh: `MomentSheet.showBloomed(...)` / `MomentSheet.showLocked(...)` (cả 2 tự `HapticFeedback.selectionClick`).
  - Hero nhuỵ 72×72: bloomed = `nucleusColor(kind) .12` nền + glow `.30 blur20` + `nucleusIcon` size 34 ĐẶC màu loại; locked = đĩa `textTertiary .10` + `IconsaxPlusLinear.lock_1` 30 textTertiary, không glow.
  - Title/middle/copy/CTA theo `kind`+`locked`: **days** title "{v} ngày bên nhau" + dòng "Nở ngày {date}" (`DateFormat.yMMMMd(locale).format(anniversary + value ngày)`), không CTA. **photos** title "Kỷ niệm thứ {v}" + (nếu resolve được ảnh) "Đăng ngày {date}" + thumbnail `AspectRatio 4:5` ClipRRect r20 (remoteUrl→CachedNetworkImage; localPath+existsSync→Image.file; else fallback surfaceLight + gallery icon) + CTA "Xem trong Thư viện". **streak** title "Kỷ lục chuỗi {v} ngày" — KHÔNG dòng-ngày (chỉ câu khích lệ), không CTA. **locked** title như từng kind + hint "Còn {n} … nữa để nở 🌱" (days/photos/streak riêng key) + CTA photos="Thêm kỷ niệm"/streak="Trả lời hôm nay" (days không CTA).
  - **GUARD photo `len<value`**: helper `resolvePhotoForMilestone(photo, value)` (ở `love_tree_screen.dart`) trả `null` khi `index = sortedPhotos.length - value < 0` → sheet ẩn thumbnail + CTA + dòng-ngày, chỉ còn title + câu copy. KHÔNG crash.
  - **CTA deep-link (cầu nối `NotificationTapRouter`, KHÔNG push '/home' thẳng)**: "Xem trong Thư viện" → `pendingHomeTab=2` + `pendingPhotoId=photo.id` → `_closeToHome` (pop sheet rồi `maybePop` pop LoveTree về Home; Gallery consume `pendingPhotoId` qua `_onDeepLinkPhotoRequest` mở đúng ảnh). locked photos → `pendingHomeTab=2`+`pendingCompose=true`; locked streak → `pendingHomeTab=0`+`pendingHomeFocus='daily_question'`.
- **`lib/screens/love_tree_screen.dart` (SỬA — thêm v2 lên v1)**:
  - **#1 bấm hoa**: hoa trên cây (`_LoveFlower`) bọc `GestureDetector(behavior: opaque)` + padding hit-pad → vùng chạm ≥44px (hoa KHÔNG phóng to, chỉ nới touch box qua `_flowerHitPad` + bù offset `Positioned`); dòng mốc đã-nở trong `_MilestonesCard` bọc `InkWell` ripple rose .08. Cả 2 → `_openMomentSheet(...)` resolve data theo kind rồi mở `MomentSheet.showBloomed`; dòng mốc kế (locked) → `MomentSheet.showLocked`.
  - **#2 cây sống động**: `_phase`/`_season` tính 1 lần ở entry (`late final`, KHÔNG tick mỗi giây). Thứ tự vẽ trong `_TreeHero` Stack: `SkyBackdropPainter`(trời+sao tĩnh đêm) → `LoveTreePainter`(thêm param `season` để tint tán, GIỮ stage geometry) → hoa overlay → `_AmbientParticles`(đom đóm/cánh rơi) → confetti. `SkyBackdropPainter`: 4 gradient phase (dawn/day/dusk/night theo hex spec, phủ ~72% trên, top-fade) + 14 sao tĩnh fraction cố định (night) + "+" sparkle tĩnh cho sao to. `_AmbientParticles`/`_ParticlesPainter`: night=5 đom đóm (Lissajous chu kỳ qua `_ambient` 8s + breathe alpha lệch pha/index, glow maskFilter blur4); spring/autumn=4 cánh/lá rơi (loop progress, sway sin, xoay; spring=cánh hồng, autumn=lá vàng xoay nhiều hơn); hè/đông không hạt rơi (chỉ tint tán). Sway hoa: `_LoveFlower._buildSwaying` `Transform.rotate ±0.035rad` quanh bottomCenter, đọc `_ambient.value` + offset pha `index*0.6` (1 controller chung). **Reduce Motion (B.5)**: `_ambient` = null khi reduceMotion → sky+sao GIỮ (painter tĩnh), đom đóm vẽ tĩnh (`t=-1`, pha=0), cánh rơi vẽ tĩnh rải ở gò, tint mùa GIỮ, sway TẮT, bloom/confetti TẮT (v1 đã xử lý); bướm KHÔNG implement (optional spec). `SkyBackdropPainter.shouldRepaint`/`LoveTreePainter.shouldRepaint` chỉ true khi phase/season/stage/skeleton đổi (thêm hoa/hạt KHÔNG repaint cây/trời).
  - **#3 thẻ "Khoe cây"**: `_LoveTreeShareCard` (4:5, logic width 360 → export pixelRatio 1080/360 = 3× ≈ 1080×1350) bọc `RepaintBoundary(key:_shareCardKey)`, render off-screen `Positioned(left:-3000)`. Nền `sunsetRomance` gradient + radial glow trắng + `_ShareTree`(tái dùng `LoveTreePainter`+hoa tĩnh, reduceMotion:true) + `AnimatedCoupleName(pulseHeart:false)` + statsLine + tagline + watermark "Dear Embeiu". Flow `_shareTree`: build `_ShareCardData` → setState render → `endOfFrame`+40ms → `_captureShareCard` (`toImage`→PNG) → ghi temp `getTemporaryDirectory()/love_tree_<ts>.png` → `SharePlus.instance.share(ShareParams(files,text,sharePositionOrigin))`. `sharePositionOrigin` = rect toàn màn (iPad popover). Lỗi/`_sharing` guard im lặng (no toast). 2 entry: header trailing `HeaderIconButton(share, accentLoveDeep)` (hiện khi `canShare` = data≠null, ẩn no-couple/waiting/loading) + `_SharePill` "Khoe cây" trong `_BloomBanner` khi có hoa mới.
- **`lib/l10n/app_en.arb` + `app_vi.arb` (SỬA)**: +20 key v2 `loveTreeMoment*`/`loveTreeLocked*`/`loveTreeShare*` (đủ trong CẢ 2 ARB, placeholder `{v}`/`{count}`/`{date}`/`{n}`/`{days}`/`{flowers}` khai báo `@`-block). Voice "hai bạn/cả hai/hai đứa". Đã chạy `fvm flutter gen-l10n` → `app_localizations*.dart` regenerate (KHÔNG hand-edit). `loveTreeShareFailed` để sẵn (snackbar optional, hiện share-flow im lặng — chưa wire, không phải lỗi).
- **Backend/model/native**: KHÔNG đổi. Hive: KHÔNG key mới (tái dùng `love_tree_seen_<coupleId>` của v1). KHÔNG dependency mới (confetti/share_plus/path_provider/cached_network_image/intl đã có). Không build_runner.
- **Câu hỏi mở Designer §J đã đóng**: `NotificationTapRouter.pendingPhotoId` ĐÃ TỒN TẠI + Gallery consume (`_onDeepLinkPhotoRequest`) → CTA "Xem trong Thư viện" deep-link đúng ẢNH (không chỉ mở tab) — vượt mức tối thiểu AC#2.

## v2 — lệnh đã chạy
- `fvm flutter gen-l10n` (sạch).
- `fvm flutter analyze` (cả project) → **No issues found! (ran in 14.1s)**.

## v2 — lệch spec / điểm Tester cần soi
- **GUARD photo `len<value`** (server-aggregate `photoCount` có thể > số ảnh đã load): mốc ảnh cao + chưa load đủ → sheet chỉ title+copy, không thumbnail/CTA/ngày. Soi: mốc 50/100 ảnh khi list mới load 1 trang; offline; ảnh remote chưa cache (fallback icon gallery) vẫn cho CTA mở Gallery.
- **Reduce Motion (OS toggle)**: bật → 0 chuyển động (đom đóm/cánh-rơi/sway tắt → tĩnh; bloom/confetti tắt) nhưng trời+sao+tint-mùa+cánh-tĩnh-ở-gò vẫn đẹp. Soi từng phase/mùa (đặc biệt đêm: sao tĩnh + 5 đom đóm tĩnh; xuân/thu: cánh/lá rải gò).
- **Deep-link pop về Home**: `_closeToHome` = `navigator.pop()` (sheet) + `navigator.maybePop()` (LoveTree route) trên CÙNG navigator (sheet không `useRootNavigator`). Soi: sau tap CTA "Xem trong Thư viện" về đúng tab Gallery + mở đúng ảnh; locked photos → composer; locked streak → Home + scroll card daily-question. Không double-pop quá (về sai chỗ) khi navigator stack bất thường.
- **Share trên iOS/Android**: thẻ off-screen `Positioned(left:-3000)` render đủ 2 frame trước capture (`endOfFrame`+40ms). Soi: PNG ra đủ nét (~1080×1350), không trắng/cắt; `sharePositionOrigin` popover iPad không crash; share lỗi/cancel im lặng; spam tap (guard `_sharing`).
- **Đổi giờ/tháng máy**: `_phase`/`_season` chốt ở entry (không tick) → đổi giờ/tháng phải MỞ LẠI màn mới đổi trời/mùa (không đổi realtime trong 1 phiên — đúng spec B.0). Soi 4 phase (5/12/17/22h) + 4 mùa (3/6/9/12).
- **Thẻ share — vùng cây stage bloom (≥10 hoa)**: card tree height ≈ `(360*1.25)*0.46 ≈ 207px`, stage bloom `canopyR=140` + 10–18 hoa phyllotaxis → có thể hơi sát mép/đè dày. KHÔNG crash (chỉ thẩm mỹ). Soi thẻ ở couple nhiều mốc (nở rộ) xem cây/hoa có cân đối trong khung 4:5 không.
- **Hit-test hoa ≥44px**: hoa nhỏ mép tán (scale 0.82 ≈ 28px) nới touch box bằng padding trong suốt + bù offset Positioned → hoa vẫn đúng vị trí slot. Soi: bấm trúng hoa nhỏ; 2 hoa sát nhau không "ăn" nhầm chạm của nhau; bấm vùng trống cây không mở sheet.
- **Sheet streak không có ngày**: đúng (StreakProvider chỉ lưu `longestStreak`, không lưu ngày đạt) — Tester đừng báo "thiếu ngày" là bug.

## File tạo mới
- **`lib/services/love_tree_service.dart`** — toàn bộ milestone-math + Hive seen-marker + visual token theo loại hoa. Pure, không provider/backend (để màn cây và badge StreakChip tính `flowerCount` GIỐNG HỆT nhau). API chính:
  - `enum FlowerKind { days, streak, photos }`, `enum LoveTreeStage { seed, sprout, young, green, bloom }`.
  - `buildMilestones({days, longestStreak, photoCount})` → `List<LoveTreeMilestone>` (mỗi mốc có `index` ổn định: sort days→streak→photos, mỗi loại tăng dần; `reached` bool).
  - `flowerCount(...)` = đếm mốc `reached`. `stageForFlowers(int)` = ngưỡng PO (0→seed,1-2→sprout,3-5→young,6-9→green,10+→bloom).
  - `daysTogether(DateTime)` = date-only, clamp ≥0.
  - `readLastSeen(coupleId)` (sync, `Hive.box<String>('app_settings')`, key `love_tree_seen_<coupleId>`, fail-soft→0) + `writeLastSeen(coupleId, flowers)` (async, best-effort). Lưu int dưới dạng String (khớp box `Box<String>`).
  - `nucleusIcon/nucleusColor/petalEdge(FlowerKind)` — token màu/icon (ngày=heart `Icons.favorite` rose; streak=`IconsaxPlusLinear.flash` cam-hồng gradient; ảnh=`IconsaxPlusLinear.gallery` lavender).
  - **Icon library:** màn Love Tree dùng **IconsaxPlus** (đồng bộ với phần lớn screen khác: header badge `magic_star`, banner `magic_star`, nurture `tree/flash/gallery/messages`, chevron `arrow_right_3`, flag) — KHÔNG Lucide (spec gốc đề xuất Lucide; đã chuyển sang Iconsax cho nhất quán app). `streak_chip.dart` giữ icon cũ của nó (không thêm icon mới — badge chỉ là chấm Container + emoji 🌸/✨).
- **`lib/screens/love_tree_screen.dart`** — `LoveTreeScreen` (StatefulWidget, push thường qua MaterialPageRoute). Gồm:
  - `LoveTreeScreen`/`_LoveTreeScreenState`: đọc `context.watch` 3 provider (Couple/Streak/Photo); tính flowerCount/stage; quản lý bloom + lastSeen.
    - `_lastSeen`/`_seenAtEntry` đọc Hive 1 lần ở `initState`; `_commitSeen` ghi lastSeen=flowerCount qua `addPostFrameCallback` (animate trước, ghi sau); `_bloomCelebrated` đảm bảo confetti chỉ bắn 1 lần/lượt; `_confetti` (ConfettiController) bắn khi `hasNewBlooms && !reduceMotion`.
  - **Nền `dawnBlush` + transparent Scaffold + SafeArea** (màn con push riêng, KHÔNG trong shell 4-tab). Header = `SubScreenHeader(badge: loveTreeBadge, badgeIcon: LucideIcons.flower2)` (không title — chip là tên màn).
  - `_TreeHero`: `Stack` = `CustomPaint(LoveTreePainter)` nền + hoa là **widget overlay** `_LoveFlower` (Positioned) + `ConfettiWidget` sparkle. Rải hoa deterministic `_bloomPositions` (golden-angle 137.5° + `sqrt((i+.5)/14)` + jitter Knuth-hash theo index, anchor vào canopy thật của stage qua `LoveTreePainter.canopyGeometry`). Hoa keyed theo `kind-value` để State không bị recycle khi list dài thêm giữa lượt.
  - `_LoveFlower` (StatefulWidget): cánh = `CustomPaint(_FlowerPetalsPainter)` (5 cánh giọt radial-gradient pink→edge), nhuỵ = `_Nucleus` (Container tròn + `Icon` Lucide/Material, streak đổ gradient chéo). Animate nở: cánh scale+fade easeOutBack 540ms, nhuỵ pop trễ (Interval 0.22→1), stagger 140ms/bông (cap 6). reduceMotion → render thẳng, không controller.
  - `_BloomBanner`: pill r16 rose .10, viền .30, text `loveTreeNewBloomBanner(count)`/`...One`. EntranceReveal khi !reduceMotion.
  - `_NurtureCard` ("Cùng vun đắp") + `_NurtureTile` ×3 (giữ chuỗi/thêm kỷ niệm/trò chuyện) — **cả 3 tile `Navigator.maybePop()` về Home** (v1 chốt PO).
  - `_MilestonesCard` + `_MilestoneRow` + `_BloomedChip`: list mốc đã nở (✓ đã nở) + 1 mốc kế mỗi loại ("còn N…", opacity .65). 0 hoa → 3 mốc đầu mỗi loại.
  - `_StateMessage`: no-couple (`loveTreeNoCoupleBody`) / waiting (`loveTreeWaitingTitle/Body` + nút pill "Mời người ấy" → pop). `couple == null` / `isWaitingForPartner` → không vẽ vườn.
  - `_LoadingTree`: cây xám skeleton (`LoveTreePainter(skeleton:true)`) + 2 ShimmerSkeleton khi streak loading lần đầu.
  - `LoveTreePainter extends CustomPainter`: vẽ gò đất (quad bezier gradient gold→lavender + crest sáng), bokeh (7 đốm vị trí cố định), thân + nhánh + tán **blob mây** (`_blobPath` cubic/quad bezier theo bảng `bumps` hằng số) + lá giọt (`_paintLeaf`) + bóng tán (S6+ MaskFilter.blur) + glow rose (S10+) + cánh rơi (bloom). 5 stage qua `_StageSpec` (static `_specForStage`). `shouldRepaint` chỉ true khi stage/skeleton đổi (hoa ngoài painter). Static `canopyGeometry(stage,size)` để overlay hoa anchor đúng canopy.

## File sửa
- **`lib/widgets/streak_chip.dart`**:
  - Tap chip → **push `LoveTreeScreen`** (MaterialPageRoute) thay vì `StreakSheet.show` (PO chốt; bỏ import `streak_sheet.dart`, thêm import couple/photo provider + `LoveTreeScreen` + `LoveTreeService`).
  - Badge dụ-vào: `_hasUnseenBlooms` (tính flowerCount qua LoveTreeService, so `readLastSeen`) → khi có hoa chưa xem: chấm glow 8px `accentLoveDeep` top-right (Stack clip off) + đổi emoji ✨→🌸. Fail-soft (no-couple/waiting/error → false).
  - `StreakSheet` vẫn dùng ở `home_screen.dart`/`profile_screen.dart` + auto-celebration streak → KHÔNG dead code.
- **`lib/l10n/app_en.arb` + `app_vi.arb`**: +33 key `loveTree*` (badge/stage0-4/flowerCount/banner/nurture×7/milestones×10/states×4). Placeholder `{count}` int khai báo ở `@`-block (en). Đã chạy `flutter gen-l10n` → `app_localizations*.dart` regenerate (KHÔNG hand-edit). Voice "hai bạn/cả hai" (không "hai đứa").

## Backend / model / native
- KHÔNG đổi. Không Firestore/rules/Function/Storage/native. Hive: 1 key mới `love_tree_seen_<coupleId>` trong box `app_settings` (đã mở sẵn ở `main.dart` trước runApp) — không adapter mới, không migration, không build_runner.

## Deploy
- Không có gì để deploy (thuần client). CHƯA submit/build.

## Lệnh đã chạy
- `flutter gen-l10n` (sạch).
- `flutter analyze` (cả project) → **No issues found**.

## [2026-06-17] [dev] Wire điều hướng 3 tile "Cùng vun đắp"
- User: 3 tile trong "Cùng vun đắp" giờ điều hướng thật thay vì chỉ `pop()`:
  - **Giữ chuỗi mỗi ngày** → Home tab + scroll tới card câu hỏi hôm nay (`pendingHomeTab=0` + `pendingHomeFocus='daily_question'`).
  - **Thêm một kỷ niệm** → Gallery tab + tự mở composer thêm ảnh (`pendingHomeTab=2` + cờ mới `pendingCompose=true`).
  - **Cùng trò chuyện hôm nay** → Chat tab (`pendingHomeTab=1`).
- Cơ chế: Love Tree là route push TRÊN Home; Home sở hữu tab + card daily-question và đã listen `NotificationTapRouter`. Mỗi tile set router rồi `maybePop()` để Home áp dụng — tái dùng đúng cầu nối deep-link của push notification (KHÔNG tự push tab-child).
- Thêm mới: `NotificationTapRouter.pendingCompose` (ValueNotifier<bool>) + `consumeComposeRequest()` ở `push_notification_service.dart`. `GalleryScreen` listen cờ này (warm-only — tree chỉ mở khi app đang chạy) → `_onComposeRequest` defer 1 frame rồi gọi `_pickMultiplePhotos()` (không nhân đôi logic upload, dùng lại luồng Gallery sẵn có).
- Files: `lib/screens/love_tree_screen.dart` (import service + 3 helper `_goToDailyQuestion/_goToAddMemory/_goToChat`), `lib/screens/gallery_screen.dart` (listener compose), `lib/services/push_notification_service.dart` (cờ + consume).
- `flutter analyze` (cả project) → **No issues found**.

## Lệch spec / cần Tester soi
- **Điều hướng "Cùng vun đắp" [ĐÃ WIRE 2026-06-17]**: streak→câu hỏi hôm nay (Home+focus), kỷ niệm→Gallery+composer, chat→Chat tab. Backlog cũ "đổi tab" đã đóng. Tester soi: timing picker mở ngay sau pop+switch tab (native modal, defer postFrame); back từ Chat về Home (previousIndex=0); cancel picker thì chỉ nằm ở Gallery.
- **Canopy anchor cho hoa**: đã anchor hoa vào canopy THẬT theo stage (cải tiến so với spec gốc "dùng box green/bloom") để hoa không lửng ở stage nhỏ (sprout/young). Tester verify hoa nằm trên tán mọi stage, không tràn xuống thân, không chồng che nhuỵ.
- **Sparkle confetti**: bắn 1 lần/lượt khi có hoa mới + !reduceMotion (`ConfettiWidget` emit từ canopy center, 10 particle). Cần soi runtime: confetti có hiển thị trong vùng cây không (Stack clip), reduceMotion có tắt sạch không.
- **lastSeen ghi postFrame**: nếu user thoát màn NGAY trước postFrame, lastSeen có thể chưa kịp ghi → lần sau animate lại (hiếm, chấp nhận được). Tester thử mở→thoát nhanh.
- **Loading→data**: dùng `streak.isLoading && state==hidden` làm gate skeleton; nếu streak fail-soft (hasError) thì coi longestStreak=0 (không chặn cả màn). Photo/Couple không có cờ loading riêng ở đây → photoCount/daysTogether dùng giá trị hiện có. Tester verify không "nhảy số" hoa khi data về muộn.
- **Badge StreakChip**: `_hasUnseenBlooms` đọc Hive sync mỗi build — rẻ + fail-soft. Verify chấm mất sau khi vào màn cây (lastSeen update) + quay lại Home.
- Cây/hoa 100% CustomPaint/shape, KHÔNG asset ảnh. Verify thẩm mỹ từng stage (seed/sprout/young/green/bloom) + 3 loại nhuỵ đúng màu/icon trên thiết bị thật (màu gradient + blur có thể khác theo GPU).

## [2026-06-18] [dev] Điểm Tester soi cho v2.2 (sakura)
- **Blossom đậu mép cành mọi stage**: vì `_SakuraBranchPainter` và `_sakuraBloomPositions` dùng CHUNG `_SakuraBranchGeometry` nên không lệch về NGUYÊN TẮC; nhưng cần soi RUNTIME: blossom có nằm gọn dọc cành (không lơ lửng giữa không trung, không đậu hụt mép) ở cả 5 stage + máy nhỏ + xoay ngang (size đổi). Đặc biệt blossom gần ngọn cành (normal gần ngang) có thể bị clamp vào mép phải/trên → soi xem có dồn cục/chồng không.
- **Anchor deterministic (AC #3)**: mọi randomness từ `_bloomHash(index)` (không `Random`/thời gian). Xoay máy / rebuild / mở lại → blossom cũ KHÔNG nhảy. Soi kỹ vì layout đổi hẳn so với v2.1.
- **Anti-overlap (hit-box)**: thêm pass đẩy blossom < 36px ra xa (deterministic). Tap đúng blossom, không nhầm bông kế — soi ở stage đông blossom (bloom 10+) và blossom mép scale 0.82.
- **No-regression #1/#2/#3**: bấm blossom (trên cành + dòng card) → `MomentSheet` đúng kind; sky 4 giờ + đêm sao/đom-đóm; 4 mùa (cánh sakura rơi xuân/bloom, thu ngả vàng); coach mark 1-lần trỏ blossom đỉnh + caption; thẻ "Khoe cây" (giờ vẽ cành); bloom-anim + confetti + `lastSeen`. Đổi giờ/tháng máy để test sky/mùa.
- **Reduce Motion**: cành/blossom/nụ/lá/cánh-rơi vẽ tĩnh đẹp; sway/đom-đóm/bloom-anim/coach-ripple/cánh-rơi-động TẮT; banner tĩnh. Soi cánh rơi static rải quanh gốc (không blank).
- **Skeleton loading**: cành xám phẳng `surfaceLight` (không nụ/lá màu, không nháy màu) → fade sang cành thật khi data về.
- **Rollback**: nếu visual sakura có vấn đề nặng, đổi 1 hằng `kTreeRenderer = SakuraBranchRenderer()` → `PaintTreeRenderer()` (cuối `love_tree_screen.dart`) là về lại cây v2.1 ngay.
- **Lệch spec nhỏ (Dev quyết, không ngoài scope)**: spec §C.2 ghi "6–7 chấm tua nhuỵ" → chọn 7 (cố định). Glow đỉnh bloom vẽ TRONG `SakuraBranchPainter` (không vẽ ở `_TreeHero`). `canopyGeometry` của renderer khai báo theo contract nhưng hiện chưa có call-site gọi trực tiếp (coach dùng `_coachTargetIndex(positions)`, glow vẽ trong painter) — giữ cho ranh giới v3.

## [2026-06-19] [designer+dev] BG bầu trời theo BUỔI (user: "ban ngày cũng phải có bg đẹp như trời sao ban đêm")
Trước: `SkyBackdropPainter` chỉ vẽ **sao ở `night`**; dawn/day/dusk chỉ gradient nhạt → ban ngày trống trải. Sửa (chỉ trong `SkyBackdropPainter`, painter-only, static/Reduce-Motion-safe):
- **Gradient giàu hơn** mỗi phase (vẫn hài hoà brand hồng): dawn=bình minh ấm vàng-hồng · day=xanh trời nhạt→hồng · dusk=hoàng hôn cam-hồng-tím · night=tím đêm.
- **Thiên thể theo buổi** (mới): `_paintSun` (mặt trời + quầng sáng radial) cho dawn (mọc thấp góc trái) / day (cao góc phải) / dusk (lặn thấp, quầng cam to); `_paintCloud` (mây trắng blur) cho day (3) + dawn (1); `_paintMoon` (trăng tròn + glow) cho night; `_paintStars` thêm param `alphaScale` → night đầy sao, **dusk có sao mờ** (0.45). Màu tham khảo palette sunrise/sunset trên web.
- Vị trí mặt trời/trăng đặt theo `Size` fraction (responsive). analyze 0. Verify trực tiếp emulator **buổi sáng (06:38)**: mặt trời mọc góc trên-trái + gradient bình minh ✓. Day/dusk/night cùng painter (không đổi được giờ emulator do production-build chặn root) — verify code + analyze. Client-only, không deploy.

## [2026-06-19] [dev] Bầu trời ĐỘNG sáng/trưa/chiều (user: "buổi tối có sao, sáng/trưa/chiều chưa có gì" → animate)
User muốn nền sáng/trưa/chiều sống động như trời sao đêm. **Quyết định nguồn:** thử auto-tải Lottie nền từ web nhưng BẾ TẮC (sandbox chặn `lottiefiles.com`/`lottie.host` CDN; repo weather GitHub tải được thì **GPL-3.0 / không license** + màu vàng-xanh lệch tông hồng) → user chốt **animate bằng code** (khớp palette 100%, không lo license).
- Chỉ sửa `SkyBackdropPainter` (painter-only) + call-site: thêm field `t` (clock phase [0,1), `-1`=static Reduce-Motion) + getter `_moving`/`_tau`.
- Chuyển động dịu mắt, dùng LẠI `_ambient` controller 8s có sẵn (KHÔNG thêm controller): **mây trôi** (`_paintCloud` thêm `driftPhase`, sway sin lệch pha) · **nắng/trăng "thở"** (pulse ±5-6% quầng sáng) · **sao nhấp nháy** (mỗi sao 1 pha riêng, dusk+night) · **chim bay** (`_paintBird` mới — bóng hải âu 2 nét lướt trái→phải + vỗ cánh, dawn 2 con + day 2 con).
- Call-site: `reduceMotion ? CustomPaint(static) : AnimatedBuilder(_ambientController())` truyền `t=_ambient.value`. `shouldRepaint` so thêm `t`. Static frame = mây/sao/nắng base + chim đậu mid-flight (không blank).
- analyze **0** (file + toàn dự án). Client-only, KHÔNG deploy. Verify thẩm mỹ runtime: cần soi mây trôi/chim bay mượt + perf máy tầm trung (sky repaint 60fps có blur mây — nếu nặng thì giảm số mây/tắt blur khi moving).

### Vòng 2 (user screenshot: "bg xấu, mặt trời+mây mờ" + "thiết kế bg ngày/trưa/chiều giàu như trời sao đêm")
- **Mặt trời NÉT**: `_paintSun` bỏ đĩa radial mép trong suốt (gây nhoè) → 3 lớp: aura mềm rộng + "seat" glow ôm sát + **đĩa đặc rìa full-alpha** (tâm sáng→core). Hết "vệt bẩn".
- **Mây RÕ**: blur sigma `r*0.45→0.16`, opacity `0.55→0.82`; +param `tint` (mây hoàng hôn ám cam/hồng/tím ở dusk).
- **Giàu chi tiết như trời đêm**: mỗi buổi ngày giờ là CẢNH nhiều lớp: `_paintLightMotes` (đốm sáng lung linh — bản ban-ngày của trường-sao, dùng lại 14 vị trí `_stars`, 2 circle phẳng/đốm, twinkle lệch pha; vàng ấm@dawn, trắng@day) + 4 tầng mây trôi + `_paintFlock` (đàn 5 chim đội hình chevron trôi chậm, vỗ cánh lệch pha) thay 2 chim lẻ. dusk giữ sao mờ (0.5) + mây ám hồng + đàn chim về.
- bg dawn ấm/đậm hơn (bớt washed-out): `#FFD7A3@.80 → #FFB3CC@.54`.
- `_drawBird(center,span,flap,alpha)` tách từ `_paintBird` (giờ bỏ). analyze 0. Vẫn client-only. **Verify simulator dawn (07:36).**

### Vòng 3 (user screenshot dawn: "sky chỉ mặt trời + mây, BỎ đàn chim; mặt trời cũng nên có Lottie")
- **Mặt trời → Lottie**: `assets/lottie/sky_sun.json` = **Meteocons `clear-day`** (Bas Milius, **MIT**, mặt trời trơn tia xoay #F8AF18, 128×128 loop 6s) — tải qua **npm registry → tarball `@meteocons/lottie@0.1.0`** (jsDelivr npm/CDN bị 404, nhưng `registry.npmjs.org` reachable → lấy tarball, giải nén `package/fill/clear-day.json`). KHÔNG mặt cười (Noto chỉ có 🌞 có mặt → loại).
- Render: overlay `Lottie.asset` `Positioned` trong Stack `build()` (sau lớp sky CustomPaint, TRƯỚC SafeArea → sau header/cây; `IgnorePointer`), vị trí+cỡ theo phase (dawn trái w·0.42, day phải-cao w·0.34, dusk phải-thấp to w·0.48); night = null (giữ trăng vẽ tay). `repeat/animate = !reduceMotion`, lỗi→`SizedBox.shrink`. +import `package:lottie`.
- **BỎ đàn chim + đốm sáng**: gỡ `_paintFlock`/`_drawBird`/`_paintLightMotes` + `_paintSun` (giờ sun là Lottie). Painter daytime CHỈ còn `_paintCloud` (giữ drift + tint hoàng hôn). Bỏ luôn sao mờ ở dusk (user "chỉ mặt trời + mây"). Night giữ nguyên trăng+sao.
- analyze 0. Client-only, không deploy. **License: thêm Meteocons (MIT) vào ghi công cùng Noto.** Verify runtime.

## [2026-06-19] [dev] Bỏ tiêu đề giai đoạn cây (user request)
User: "xoá cái cây xanh đi" → gỡ dòng tiêu đề tên-giai-đoạn ("Hạt mầm/Mầm non/Cây non/Cây xanh/Nở rộ") ở hero love tree. Xoá `Text(_stageTitle(...))` + `SizedBox(height:4)` (`love_tree_screen.dart` §3) **và** method `_stageTitle` (giờ orphan). Giữ caption "Chạm vào mỗi bông…" + subtitle "{n} bông hoa đã nở" (`_stageSubtitle`). `stage` vẫn dùng (painter + subtitle bloom). l10n `loveTreeStage0..4` giữ trong ARB (unused, vô hại). analyze 0. Client-only, không deploy.

## [2026-06-19] [dev] Mặt trời "vầng sáng mềm" vẽ tay + bg sáng/trưa XANH DA TRỜI pastel
User (screenshot dawn-ish 09:36): "bg buổi sáng chưa đẹp, mặt trời quá xấu → bg phải là màu xanh da trời; trưa/chiều mỗi buổi 1 gam; tối GIỮ NGUYÊN (rất thích)". Research palette sunrise/sunset web (schemecolor, color-hex). User chốt qua AskUserQuestion: **mặt trời = "vầng sáng mềm" (glow)** + **độ xanh = "pastel dịu"**.
- **GỠ Lottie mặt trời** (đảo vòng 3): xoá overlay `Lottie.asset('assets/lottie/sky_sun.json')` + khối tính `sunCenter/sunSize`/biến `w` trong `build()` + **import `package:lottie`**. Asset `sky_sun.json` còn nằm đĩa nhưng KHÔNG dùng nữa (dep `lottie` giờ unused toàn dự án — để lại, analyze không lỗi).
- **Mặt trời = `_paintSun` vẽ tay** trong `SkyBackdropPainter` (cùng idiom `_paintMoon`): quầng halo radial rộng "thở" theo `_ambient` (±5%) + đĩa orb fill radial (highlight→core) blur nhẹ mép → tan vào halo, KHÔNG tia cứng. Gọi đầu mỗi case daytime (mây vẽ sau → trôi trước mặt trời): dawn mọc thấp-trái `#FFE6B8/#FFD49A` (warm trên trời pastel), day cao-phải nhỏ-sáng `#FFF4D8/#FFE6AE`, dusk lặn thấp-phải to `#FFC785/#FF9E6E` (tan vào trời vàng). Vị trí theo `Size` fraction (giữ tọa độ Lottie cũ).
- **bg pastel xanh** (giữ cấu trúc "dải trời tan vào dawnBlush hồng" như buổi tối user thích — KHÔNG đổi cả nền, cây/hoa vẫn trên hồng): dawn `#C8E6F7@.56→#E4F2FB@.34→#EFF7FC@.08` (nhạt+mát hơn, sáng sớm airy); day `#B8DEF5@.62→#DCEFFB@.40→#EAF5FC@.10` (xanh nhất, đúng hex user chọn ở preview). dusk GIỮ NGUYÊN golden hour, night GIỮ NGUYÊN.
- analyze **0** (file + toàn dự án). Client-only, KHÔNG deploy. **Verify runtime:** screenshot lúc 09:36 = phase `day` → sẽ thấy trời xanh pastel + vầng nắng mềm góc trên-phải. Cần soi thêm dawn/dusk (production build chặn đổi giờ emulator).

### Vòng tiếp (user screenshot day 09:51: "header rất lộn xộn, sắp lại hợp lý")
Sau khi thêm mặt trời+mây, chúng chen vào dải header → vầng nắng nằm sau nút share, mây lởn vởn quanh chip/back = rối. **Header control vốn ổn** (`SubScreenHeader`: back · chip giữa · share) → KHÔNG đụng. Sửa thuần **bố cục cảnh trời** trong painter:
- **Dành ~28% đỉnh trời làm vùng SẠCH cho header** — dồn toàn bộ mặt trời + mây xuống vùng trời trống bên dưới header (mọi tâm thiên thể `y ≥ ~0.33` của dải trời; tính cả máy nhỏ header chiếm ~0.27 → vẫn thoát).
- **Cân đối mặt trời 1 bên / mây bên kia**, không bright-core nào nằm sau nút góc: dawn nắng thấp-trái + mây phải; day nắng phải `y0.37` (glow top ~0.23 > đáy header) + mây trái; dusk nắng to lặn phải `y0.49` + mây trái. Mây dồn xuống `y0.33–0.45` (trên thân cây ~0.67, dưới chip ~0.06). dawn giảm 4→3 mây.
- analyze 0. Client-only, không deploy. Verify runtime day (09:5x).
