# Home (Trang chủ) — Dev log

> File Dev sở hữu. Spec: [design.md](design.md) (Home v2). Overview PO chưa có (feature khởi nguồn từ lượt review Designer 2026-06-10).

## Changelog

- [2026-06-11] [Dev] **GỠ footer archive links khỏi TodayRitualCard** (Profile v2 — archive dời về "Tủ kỷ niệm" tab Hồ sơ, spec `../settings/design.md`): xoá `_buildArchiveLinks/_archiveLink/_push` + divider thứ 3 + import `journal_screen`/`love_note_history_screen`; key `journalLinkShort/loveNoteHistoryLinkShort` hết dùng (giữ ARB). Card giờ chỉ còn câu hỏi + lời nhắn. Chi tiết Profile v2: `../settings/dev.md`. analyze sạch.
- [2026-06-11] [Dev] **Cinema REVERT full-bleed → về khuôn card chuẩn (user kèm screenshot "set padding width consistent shape like card"):** `memory_cinema_card.dart` bo góc trở lại **r28** (decoration + ClipRRect — dùng radius card chuẩn design system thay r24 cũ); `home_screen.dart` bọc `_gutter` quanh `MemoryCinemaCard` trong `_buildRecentPhotosSection` + skeleton sync (240 r0 full-bleed → `_gutter` 240 r28). GIỮ cấu trúc per-block `_gutter` (không trả padding về ScrollView — đỡ động chạm, hành vi tương đương); comment "full-bleed" các nơi đã dọn. analyze sạch. CHƯA commit.
- [2026-06-10] [Dev] **Header take 4: "CALENDAR LEAF" tờ lịch** (user chê editorial vẫn chưa vừa ý, đòi "phá cách, nổi bật nhẹ"): helper mới `_buildCalendarLeaf` — số NGÀY hôm nay 32px **w300 thanh mảnh** + halo trắng .40 blur 16 ‖ vạch dọc 1×28 gradient fade 2 đầu (mép "tờ lịch xé") ‖ cột THỨ (10.5 w700 ls1.8 trắng .95) / THÁNG (10.5 w500 trắng .70) — `DateFormat.EEEE/MMMM(locale)` uppercase. Concept: lịch bloc VN + số-đối-số với day count hero bên dưới. BỎ sparkles + hairline ngang + mọi hộp. Bell chip giữ. analyze sạch.
- [2026-06-10] [Dev] **Redesign header eyebrow → "editorial" boxless (user kèm screenshot, chê chip nhạt):** bỏ hộp chip glass quanh ngày — giờ là `✨ sparkles 15 + ngày small-caps 12.5 w700 ls2.2 trắng .95 + HALO trắng .40 blur 14 ("lit from within" như số đếm) + hairline gradient trắng .50→0 chạy hết phần còn lại hàng` — cùng ngôn ngữ luxury-editorial với eyebrow "HAI BẠN ĐÃ BÊN NHAU" kẹp hairline trong CounterCard ngay dưới (trang mở đầu 1 mạch). Bell GIỮ chip trắng-glass (nút bấm duy nhất cần affordance). Date bọc Flexible + ellipsis chống overflow máy hẹp. Gallery/Profile vẫn dùng chip `pageEyebrowStyle` — user ưng thì lan hướng editorial sang 2 tab sau. analyze sạch (lượt đầu dính 2 error GIẢ ở `today_ritual_card.dart` do race với phiên song song đang ghi file — chạy lại sạch).
- [2026-06-10] [Dev] **Lời nhắn mới: BỎ phong bì "Tap to open" → hiện thẳng nội dung + badge MỚI** (user): xoá `_sealedEnvelope` + nhánh sealed; tin partner chưa-đọc giờ render `_noteBlock` ngay với 2 dấu hiệu — chip **"MỚI"** (rose đậm, trắng 9 w800, key l10n `loveNoteNewBadge` vi "MỚI"/en "NEW") cạnh title + **viền rose .45** quanh khối lavender; tự đánh dấu đã-đọc sau **6s** hiển thị (`_noteSeenTimer`, cancel dispose, re-check unread trước khi mark) → badge+viền fade qua AnimatedSwitcher (key kèm isNew); analytics `love_note_envelope_opened` giữ nguyên tên, giờ log lúc auto-seen. Key `loveNoteSealedTitle/Tap` hết dùng (giữ ARB). analyze sạch.
- [2026-06-10] [Dev] **XOÁ nút "Thêm kỷ niệm" khỏi Home (user):** gỡ pill CTA dưới cinema + xoá `_buildAddMemoryPill` + skeleton pill 52 — đăng ảnh giờ chỉ qua composer tab Gallery; Home thuần "xem lại". GIỮ empty-state polaroid trống (couple 0 ảnh vẫn cần lối đăng ảnh đầu tiên ngay tại Home — `_buildAddMemoryTile`/`_pickAndAddPhoto` còn dùng ở đó). ⚠️ PO note: trigger đăng ảnh rời khỏi Home — theo dõi metric đăng-ảnh-tuần sau thay đổi này. analyze sạch.
- [2026-06-10] [Dev] **Cinema bỏ bo góc (user, sau khi xem full-bleed):** `memory_cinema_card.dart` r24 → vuông cạnh (`BorderRadius.zero`, bỏ borderRadius khỏi decoration — full-bleed thuần edge-to-edge, shadow giữ); skeleton sync r24→0. analyze sạch.
- [2026-06-10] [Dev] **Memory cinema FULL-BLEED tràn mép màn hình (user "full width cho tôi thử"):** `home_screen.dart` — `SingleChildScrollView` tab Home bỏ padding ngang (LTRB 16→0), thêm helper `_gutter` (Padding ngang 16) bọc TỪNG block khác (header, counter, banner, 2 section title, ritual card); riêng `_buildRecentPhotosSection` cinema KHÔNG gutter (tràn mép, giữ r24 + shadow), pill CTA + empty-state tự `_gutter` lại bên trong. Chọn cách hạ gutter xuống từng block thay vì OverflowBox vì vùng tràn của OverflowBox chết hit-test (tap/vuốt sát mép không ăn). Skeleton sync cùng cấu trúc (padding 0 + `_gutter` từng block, cinema 240 full-bleed). analyze = No issues found. Đây là bản THỬ NGHIỆM — nếu user chê, revert = trả padding 16 cho ScrollView + gỡ `_gutter`. CHƯA commit.
- [2026-06-10] [Dev] **SYNC ảnh nền CounterCard giữa 2 user** (user) — lần đầu trong ngày đụng backend, ADDITIVE toàn bộ:
  - **Firestore:** subcollection MỚI `couples/{coupleId}/prefs/home` (1 doc duy nhất) field `counterBgPhotoId` string ≤200; rules: member read/write, `prefId == 'home'` + `hasOnly(['counterBgPhotoId'])` — app cũ không bao giờ đụng path này, zero rủi ro backward-compat. **+4 test** vào `firestore.couples-sub.test.js` (member write/partner read/outsider deny/extra-field+wrong-id+oversize deny) → **suite 144/144 PASS**. **Rules ĐÃ DEPLOY DEV** (tonyembeiu-dev, trace hook ghi vết); **PROD CHỜ LỆNH user**.
  - **Service MỚI** `lib/services/home_prefs_service.dart`: `watchCounterBg(coupleId)` stream + `setCounterBg` (fail-soft: không Firebase → stream rỗng + write nuốt lỗi; Hive cache ở home gánh local).
  - **home_screen:** `_bgSyncSub` subscribe trong `_ensureCounterBgLoaded` (cancel khi đổi couple/dispose); partner swipe → stream → setState + cache Hive (echo từ chính mình bị guard equality bỏ qua); `_swipeCounterBg` → `unawaited(setCounterBg)`. Animation trượt áp dụng luôn cho thay đổi từ partner (direction mặc định +1).
  - ⚠️ Smoke-test 2 thiết bị: A vuốt → card B đổi theo realtime; offline A vuốt → online lại có sync không (set sau cùng thắng).
- [2026-06-10] [Dev] **"Kỷ niệm gần đây" → MEMORY CINEMA (Hướng 1 user chốt — thay dải polaroid):** widget MỚI `lib/widgets/memory_cinema_card.dart` — 1 card lớn 240 r24 tự trình chiếu kiểu Apple Photos Memories: auto-advance 7s/slide (Timer, chỉ khi ≥2 ảnh), crossfade 700ms (`AnimatedSwitcher` + layoutBuilder Stack `StackFit.expand` chống letterbox — fix sẵn từ backdrop), **Ken Burns** zoom 1.0→1.10 linear 9s/slide (dài hơn slide để không thấy dừng; key theo photo.id → controller one-shot mới mỗi slide), scrim đáy black .55→0 (stop .55), caption (caption thật hoặc ngày `fullDateFormat`) + dots vị trí (active 16px animated), badge "on this day" pill `accentLoveDeep .85` (`onThisDayShort{n}`) + ReactionCountBadge top-right, vuốt ngang đổi slide thủ công (|v|≥150 + reset timer + haptic), tap → `onPhotoTap(index)` callback (widget KHÔNG import screen) — **pause auto-play khi preview mở** (await rồi restart, tránh hero bay về slide khác); `didUpdateWidget` clamp index khi feed co. `home_screen.dart`: `_buildRecentPhotosSection` → slides = [on-this-day dẫn đầu] + recent dedupe, `MemoryCinemaCard` + hero `cinema-photo-{id}` qua `GalleryScreen.openPreview` + **pill CTA "Thêm kỷ niệm" MỚI full-width 52 r16** (trắng + viền rose .18, camera tròn 28, chữ `accentLoveDeep` w700, ripple rose .08, uploading → disable + opacity .6) tách khỏi dải ảnh; XOÁ 5 helper polaroid (`_polaroidLean/_polaroidShell/_polaroidPhotoStack/_buildPhotoPolaroid/_buildOnThisDayPolaroid`) + import `reaction_bar`/`shared_photo_view` hết dùng; GIỮ empty-state polaroid trống (`_buildAddMemoryTile`); skeleton sync (240 r24 + 52 r16 thay strip 190). Hero tag cũ `recent-photo-*`/`recent-otd-*` hết dùng. Không key l10n mới. analyze = No issues found. CHƯA commit. ⚠️ Smoke-test: auto-play + vuốt + tap mở đúng ảnh + quay lại resume + 1 ảnh (không dots/không auto) + upload khi đang chiếu.
- [2026-06-10] [Dev] **Fix lóe hồng giữa transition** (user: "vẫn dính tông hồng khi chuyển ảnh"): root cause = cross-fade — giữa chừng cả 2 lớp ảnh đều bán-trong-suốt → lộ gradient hồng gốc của card phía sau. Đổi sang mô hình XẾP CHỒNG: ảnh CŨ đặc 100% đứng làm nền (chỉ parallax 24px theo hướng), ảnh MỚI opaque ngay trong 20% đầu (`opacity = v/0.2 clamp`) rồi trượt đè lên + settle-zoom — không còn thời điểm nào card hở nền. analyze sạch.
- [2026-06-10] [Dev] **Swipe ảnh nền: BỎ badge n/N + animation trượt theo hướng** (user): xoá overlay "🖼 n/N" khỏi home (coach-mark 1-lần GIỮ); crossfade AnimatedSwitcher → **directional swap tự viết** trong `_AdaptivePhotoBackdrop` (giờ là `SingleTickerProviderStateMixin`): `AnimationController` 480ms easeOutCubic — ảnh CŨ trôi 90px theo hướng vuốt + fade out, ảnh MỚI lướt vào 260px từ phía ngược + fade in + settle-zoom 1.06→1; hướng truyền từ home `_bgSwipeDirection` (+1/-1) qua param mới `CounterCard.photoSwipeDirection` → `swipeDirection`; `didUpdateWidget` capture ảnh cũ (`_outgoingLocal/Remote`) → forward(from 0) → status completed thì dọn outgoing; helper `_photoLayer(local,remote)` build từng lớp ảnh với frost-blur RIÊNG tra từ `_statsCache` (ảnh cũ giữ blur của nó suốt transition); `_photoKey` refactor thành `_keyFor` static dùng chung; dispose controller+curve. analyze sạch.
- [2026-06-10] [Dev] **Waiting-partner: câu hỏi thành PREVIEW, bỏ ô trả lời** (user bắt lỗi logic "chưa kết nối thì làm sao có câu hỏi hôm nay"): state isWaiting trong `_buildQuestionBody` giờ CHỈ hiện câu hỏi + dòng "Ghép đôi xong là hai đứa sẽ cùng trả lời một câu hỏi như vầy mỗi ngày 💞" (key MỚI `dailyQuestionPairedFirst` vi/en) — KHÔNG còn pill "Chạm để trả lời" (trả-lời-vào-khoảng-không là di sản thiết kế cũ "answer trước, unlock sau"); CTA mời partner đã có ở banner ngay trên, không lặp trong card. Branch hasAnswered-while-waiting GIỮ (legacy answer cũ vẫn hiển thị). Trade-off có chủ đích: bỏ "single-player answer trước khi ghép" — đổi lấy logic thật thà; PO note: nếu data sau này cho thấy cần investment-trước-ghép thì cân nhắc lại. gen-l10n + analyze sạch.
- [2026-06-10] [Dev] **CounterCard: chỉ đếm TỔNG SỐ NGÀY + đồng hồ giờ:phút:giây LIVE** (user yêu cầu kèm screenshot — bỏ breakdown năm/tháng/ngày): `counter_card.dart` thêm param `liveSince` (DateTime?) + widget `_LiveElapsedClock` stateful (Timer.periodic 1s, mỗi tick tính lại từ `DateTime.now()` nên không drift/lệch khi background; anchor về ĐẦU NGÀY của `liveSince` để khớp số totalDays date-only; chỉ subtree đồng hồ rebuild — aurora/backdrop params hằng không bị đụng); UI: pill glass (white .14, viền .22) chứa 3 segment HH:MM:SS width cố định 40 (số 20 w700 + halo, nhãn 9.5 uppercase) — số nhảy không xê dịch layout; hero mode thêm nhãn "NGÀY" (daysUnit uppercase) dưới số to; `years/months/days` thành optional default 0 (breakdown row GIỮ làm fallback/rollback). 2 call-site đổi sang `totalDays + liveSince`: `home_screen.dart` (couple.anniversaryDate) + `guest_counter_screen.dart` (date đã chọn — xoá `CounterData.calculateFromAnniversary` + import hết dùng). **l10n +3 key** `hoursShort/minutesShort/secondsShort` (vi giờ/phút/giây · en hr/min/sec) cả 2 ARB + `fvm flutter gen-l10n`. analyze = No issues found. CHƯA commit.
- [2026-06-10] [Dev] **Final polish — 7 điểm theo review tổng (user "làm hết"):**
  1. **On-this-day → polaroid viền rose TRONG strip** (xoá `_buildOnThisDayCard` — GlassCard chữ-trắng lạc hệ cuối cùng, đóng nợ S1): tile đặc biệt sau add-tile, viền `accentLove .50` 1.4px, caption "{n} năm trước 💞" `accentLoveDeep` (key MỚI `onThisDayShort{count}` plural), hero `recent-otd-{id}`, preview đơn ảnh; strip refactor thành tiles-list + 3 helper `_polaroidShell/_polaroidPhotoStack/_buildPhotoPolaroid` (dedupe ảnh otd khỏi recent).
  2. **"Xem tất cả" trắng → `accentLoveDeep` w700** (đóng nợ contrast sau revert).
  3. **Skeleton đồng bộ layout chốt** (lần 3): header chips row + counter 340 + section lines + ritual 230 + polaroid strip row 190 r6.
  4. **Swipe affordance:** badge "🖼 n/N" đen .28 top-right card (IgnorePointer) + coach-mark 1 LẦN "‹ Vuốt ngang để đổi ảnh nền ›" (đen .45, fadeIn, tự ẩn sau 4.5s qua Timer HOẶC ngay khi user vuốt lần đầu; cờ Hive `counter_bg_hint_done`; key MỚI `counterBgSwipeHint`; Timer cancel trong dispose).
  5. **Empty-state ảnh → polaroid TRỐNG** (dòng `addPhotosEmpty` + add-tile nghiêng -1.1° cao 200) — bỏ card trắng + FilledButton cũ.
  6. **Analytics 2 event MỚI:** `counter_bg_swiped` + `love_note_envelope_opened` (constants + wrapper trong `analytics_service.dart`; gọi ở `_swipeCounterBg` + tap phong bì trong `today_ritual_card.dart`; answer/note/reveal đã có sẵn ở provider).
  7. **Banner chờ-partner → card sáng chữ tối** (trắng .72 + shadow, icon link rose tint, chip mã `accentLove .10` + `accentLoveDeep`, `InviteActionButtons(onDark:false)`) — đóng nợ contrast còn lại.
  - Dọn import `glass_card` khỏi home_screen (hết GlassCard trên Home). `fvm flutter analyze` = No issues found. CHƯA commit.

- [2026-06-10] [Dev] **"Kỷ niệm gần đây" lột xác thành DẢI POLAROID scrapbook** (user "vẫn chưa hài lòng" — tile bo tròn caption-đè là photo-list vô hồn): mỗi ảnh = polaroid (khung trắng r6 padding 7/7/7/0, ảnh r3 bên trong, dải caption 32px dưới = caption thật hoặc ngày, shadow black .10), **nghiêng tay mỗi tấm một góc** qua `_polaroidLean` (6 góc ±1.7° deterministic theo vị trí — không nhảy khi rebuild, `Transform.rotate`); tile "Thêm kỷ niệm" cũng thành polaroid TRỐNG (vùng ảnh rose .07 + nút camera tròn, caption rose ở dải dưới); BỎ gradient scrim đè ảnh + caption trắng đè (caption xuống khung giấy); strip cao 176→204 + padding dọc 6 cho góc nghiêng thở. Giữ: Hero preview, reaction badge (6,6), InkWell, decode 140×DPR. Key `momentNumberFallback` hết dùng (giữ ARB). analyze sạch.
- [2026-06-10] [Dev] **Backdrop thông minh hơn: đo thêm ĐỘ RỐI + fix bug ảnh không phủ kín** (user gửi ảnh vườn hoa "nhìn rất kỳ"): (1) BUG — AnimatedSwitcher default layoutBuilder cấp constraint LỎNG → ảnh tall letterbox lộ viền hồng; fix `layoutBuilder` Stack `StackFit.expand` + `SizedBox.expand` quanh ảnh. (2) Máy đo nâng cấp: cache `(luminance, busyness)` — busyness = mean-abs-deviation của luma (vườn hoa = sáng trung bình nhưng MAD cao, metric cũ bó tay); map `busyT=(MAD-0.06)/0.14` clamp 0–1 → **frost blur sigma 0→3.6** (ảnh phẳng giữ NÉT 100%, ảnh rối tự mờ — `ImageFiltered` + `Transform.scale` 1+0.04busy giấu mép blur, TileMode.mirror) + scrim cộng thêm `0.12×busy` (clamp top .55/bottom .62). analyze sạch.
- [2026-06-10] [Dev] **Vuốt ngang CounterCard để ĐỔI ẢNH NỀN** (user): nguồn = [ảnh couple ('couple')] + 12 ảnh gallery mới nhất (key = photo.id, cần hasLocalPath/hasRemoteUrl); `home_screen` wrap CounterCard trong `GestureDetector.onHorizontalDragEnd` (|velocity| ≥ 150, vuốt trái = tiếp, phải = lùi, wrap-around modulo, haptic) — CHỈ ngang nên không đụng scroll dọc + tap chip; lựa chọn lưu Hive `app_settings` key `counter_bg_<coupleId>` (theo photo-id nên upload mới không làm lệch; key biến mất → fallback ảnh couple; box lỗi → session-only); crossfade 420ms trong `_AdaptivePhotoBackdrop` (AnimatedSwitcher keyed `counter-bg-<photoKey>`), adaptive scrim tự đo lại luminance từng ảnh (cache sẵn có). analyze sạch. ⚠️ Smoke-test: vuốt đổi ảnh + restart giữ ảnh đã chọn + vuốt khi chỉ có 0/1 ảnh (no-op).
- [2026-06-10] [Dev] **Milestone block trong card: viết lại copy tiếng người** (user chê "Tiếp:"/"13% rồi" khó hiểu + dòng cuối lặp): 3 tầng → 2 tầng — hàng 1 "🚩 Cột mốc {label} … còn {n} ngày" (icon award→**flag** 15, phải = days-left thay percent — ngày có nghĩa hơn % với user; đạt mốc → "hôm nay 🎉"), hàng 2 = progress bar; XOÁ dòng "Chỉ còn 26 ngày tới cột mốc" (lặp) + "13% rồi". +3 key `milestoneNextLabel{label}`/`milestoneDaysLeft{count}`/`milestoneTodayLabel` vi/en; key cũ `nextMilestonePrefix`/`percentThere`/`onlyDaysUntilMilestone`/`milestoneReached` còn dùng ở guest_counter (KHÔNG đụng guest). gen-l10n + analyze sạch.
- [2026-06-10] [Dev] **CounterCard tái cấu trúc theo user (Dev đồng thuận — "app đếm ngày yêu thì số phải là vua"):** (1) SỐ ĐẾM 44→**58px** ls -1.2, label đơn vị 11→12 (`_buildCounterItem`); (2) XOÁ pill đếm-ngược kỷ niệm (`footer` không truyền nữa — trùng vai milestone countdown ngay dưới; dọn `_getNextAnniversary`/`_daysUntil`/`daysUntilAnniversary` hết dùng ở home; key `daysUntilNextAnniversary`/`todayIsAnniversary` vẫn dùng ở guest_counter — GIỮ nguyên guest); (3) StreakChip chuyển XUỐNG ĐÁY card (gộp vào `progressFooter` = Column[milestone, 14, Center(chip)]; `footerExtra` không truyền). Bố cục chốt: tên → eyebrow → SỐ TO → "Bắt đầu từ…" → milestone → streak. analyze sạch.
- [2026-06-10] [Dev] **StreakChip chốt: XOÁ lửa, ✨ lấp lánh làm icon TRÁI duy nhất** (user): bỏ Text('🔥') + bỏ sao điều kiện bên phải; leading = `Text('✨')` 13px twinkle (fade .35→1 + scale .78→1.15, 900ms repeat-reverse) hiện ở MỌI state chip. Chip cuối cùng: [✨ lấp lánh] + text + viền trắng thuần, không glow/màu. analyze sạch.
- [2026-06-10] [Dev] **StreakChip: lửa Lucide → EMOJI 🔥 animate mạnh** (user: "ngọn lửa chưa cháy" — đúng, Lucide flame là outline nét mảnh 16px, animate mấy cũng không ra lửa): thay bằng `Text('🔥')` 14px (màu cam-đỏ rực sẵn của emoji) + 2 nhịp LỆCH nhau: scale .82→1.22/460ms + rotate ±0.022 turn/700ms repeat-reverse → bập bùng hữu cơ, không metronome. Dọn biến/import hết dùng (`flameColor`/`streak`/lucide trong chip — `StreakVisuals.flameColor` vẫn sống cho StreakSheet). Trade-off chấp nhận: 🔥 không đổi màu theo độ dài streak như Lucide+flameColor cũ (progression màu vẫn còn ở sheet). analyze sạch.
- [2026-06-10] [Dev] **StreakChip: bỏ nốt viền hồng → lửa CHÁY + sao LẤP LÁNH** (user): viền về trắng .30 thuần (hết mọi rim màu); ngọn lửa animate flicker (scale .90→1.15 + fade .78→1, 620ms repeat-reverse easeInOut — "đang cháy"); ✨ TÁCH KHỎI chuỗi l10n `streakChipActiveToday` (vi+en bỏ emoji tĩnh, gen-l10n) → widget Text('✨') riêng chỉ hiện state activeToday, twinkle thật (fade .30→1 + scale .75→1.15, 900ms lệch nhịp lửa). Tín hiệu ăn mừng giờ là CHUYỂN ĐỘNG của icon thay vì màu loang/viền. analyze sạch.
- [2026-06-10] [Dev] **StreakChip: XOÁ HẲN glow theo user** (sau lượt hạ nhẹ vẫn chưa ưng): bỏ toàn bộ `boxShadow` + xoá helper `_glow` — chip giờ phẳng hoàn toàn như pill kỷ niệm; tín hiệu "xong hôm nay" chỉ còn viền flameColor .60 + icon lửa màu. analyze sạch.
- [2026-06-10] [Dev] **StreakChip hết "dải hồng" trên nền ảnh** (user soi: chip "1 ngày kết nối · hôm nay xong rồi" có vệt hồng): thủ phạm = glow BoxShadow màu lửa alpha .35 blur 16 của state activeToday — tan đẹp trên gradient cũ nhưng LOANG như vết ố trên nền ảnh CounterCard mới. Fix `streak_chip.dart`: chuyển tín hiệu ăn mừng vào VIỀN (activeToday → viền flameColor .60, khác → trắng .30 như cũ); glow hạ còn thì thầm (.35→.15, .18→.10, .25→.12; blur 16/14→10, offset y 4→3). Nền chip vẫn kính trắng .18 đồng chất pill kỷ niệm. analyze sạch.
- [2026-06-10] [Dev] **Section "Kỷ niệm gần đây": BỎ banner CTA → tile "+" đầu dải ảnh** (user hỏi "có thật sự cần?" — quyết định PD: GIỮ section vì North Star + variable reward, nhưng banner rose full-width mắc banner-blindness): xoá `_buildAddMemoryCta`/`_buildAddMemoryCtaContent`; `_buildRecentPhotosSection` itemCount+1, index 0 = `_buildAddMemoryTile` MỚI (140×176 cùng cỡ ảnh, nền trắng .30 + viền trắng .55 r22, nút tròn camera rose 46 + shadow, label `addMemoryCta` tái dùng, InkWell, disable+opacity khi uploading — pattern Instagram "Your story +") → `_pickAndAddPhoto` giữ nguyên; `initialIndex: index-1` cho openPreview (lệch do tile đầu). Trang ngắn ~120px; empty-state "Đăng ảnh đầu tiên" + "Xem tất cả" + on-this-day GIỮ nguyên; HE1/HE3 (home-engagement) vẫn thoả — CTA luôn hiện diện, chỉ đổi hình hài. analyze sạch.
- [2026-06-10] [Dev] **Đổi icon 2 section header card "Hôm nay" (user):** "Câu hỏi hôm nay" `helpCircle` (tín hiệu FAQ/support — sai ngữ cảnh) → **`heartHandshake`** (hai bên kết nối — đúng bản chất cùng-trả-lời-để-gặp-nhau, đổi cả ở `_DailyAnswerSheet` cho đồng bộ); "Ghi chú tình yêu" `mailOpen` → **`mails`** (hai phong thư = thư qua thư lại, khớp motif phong bì 💌). Cùng rose 20 như cũ. analyze sạch.
- [2026-06-10] [Dev] **Polish bottom sheet "Soạn tin nhắn" (3 lỗi user duyệt):** (1) nút "Lưu" navy → **"Gửi" ROSE + icon send** (`ElevatedButton.icon`, đúng động từ + đúng tông lãng mạn — sai động từ "Lưu" là di sản từ thời edit-note); (2) **disable khi ô trống** (trước bấm trống = âm thầm đóng, khó hiểu; disabled = rose .35); (3) title "Lời nhắn của bạn" → **"Gửi lời nhắn cho {tên}"** (key mới `loveNoteSheetTitleTo{name}` vi/en — có người nhận cụ thể; sheet nhận thêm `partnerName`). Key `loveNoteSheetTitle` hết dùng (giữ ARB). gen-l10n + analyze sạch.
- [2026-06-10] [Dev] **Tin nhắn dùng CÙNG hệ khối 2 tông với answer block** (user hỏi "thuận mắt chưa" — chưa: 1 card 2 ngôn ngữ, answers có khối màu còn notes chữ trần, "chào em" xám như disabled): widget mới `_noteBlock(title, text, mine)` y hệ `_answerBlock` (mình = surfaceLight + label rose; partner = lavender .10 + label lavender; r16 padding 12, title 11 w700 chứa "{ai} · {time}", text 15 w500 textPrimary); xoá `_noteTitleLine` (hết dùng); spacing giữa 2 block 10. Toàn card giờ MỘT ngôn ngữ: hồng = mình, lavender = người ấy, pill = chỗ bấm. analyze sạch.
- [2026-06-10] [Dev] **Done-state row: check xanh → TIM ĐẬP** (user): `_inlineRow` đổi param `icon/iconColor` → `Widget leading`, truyền `AnimatedHeartIcon(18, accentRose)` (tái dùng heart pulse 820ms từ `animated_couple_name.dart`) — dòng "Hôm nay cả hai đã trả lời" giờ có tim đập thay icon hệ thống, hợp cảm xúc khoảnh khắc. analyze sạch.
- [2026-06-10] [Dev] **Answer block 2 tông phân biệt mình/partner** (user: "muốn có sự phân chia" — 2 quote bar rose giống hệt nhau): `_answerBlock` đổi sang Y NGUYÊN pattern Journal (`journal_screen._AnswerBlock`) — của mình = nền `surfaceLight` + label `accentLoveDeep`; của partner = nền `accentLavender .10` + label `accentLavenderDeep`; r16 padding 12, label 11 w700 ls .3, text 15 w500. Param mới `{required bool mine}` ở 3 call-site (reveal ×2 + answered-waiting). Nhất quán xuyên màn hình Journal↔Home. Quote-bar style chỉ sống 1 vòng (đã thay). analyze sạch.
- [2026-06-10] [Dev] **Card "Hôm nay" → nền TRẮNG ĐẶC kiểu gallery/settings** (user đề xuất, khớp luôn quy tắc design system "GlassCard không dùng cho card nội dung dày"): thay `GlassCard` (.16) bằng Container trắng r24 + shadow black .06 blur 16 offset(0,10) padding 20; pill "Soạn tin nhắn"/"Chạm để trả lời" fill trắng .55 → `surfaceLight` (trên nền trắng phải đặc); nền blur-teaser trắng .35 → `surfaceLight`; bỏ import glass_card. Hệ quả tốt kèm theo: bỏ 1 BackdropFilter blur (hiệu năng), thứ bậc trang rõ hơn — CounterCard ảnh = khối CẢM XÚC duy nhất, card trắng = bề mặt ĐỌC/TƯƠNG TÁC. analyze sạch.
- [2026-06-10] [Dev] **Polish 4 điểm theo design review card "Hôm nay" (user duyệt cả 4):** (1) đồng bộ 2 header trong card — "Câu hỏi hôm nay" lên cùng hệ với "Ghi chú tình yêu" (textPrimary 16 w800 ls -.2, trước là xám 15 w700); (2) chunking cặp title·tin-nhắn — trong cặp 4→3px, giữa 2 cặp 12→14px; (3) `_answerBlock` bọc câu trả lời user trong tint trắng .45 r12 → **sửa tiếp cùng ngày thành QUOTE-STYLE** (thanh dọc rose .55 × 2.5px trái + text, KHÔNG nền — user soi đúng: khối tint trắng bo tròn trông y hệt ô "Soạn tin nhắn" → xung đột affordance tĩnh-vs-input; dáng pill trắng giờ độc quyền cho input); (4) footer bỏ lặp "Xem lại": +2 key `journalLinkShort` "Nhật ký/Journal" + `loveNoteHistoryLinkShort` "Lời nhắn cũ/Past notes" (key dài `journalEntryCta`/`loveNoteHistoryCta` hết nơi dùng — GIỮ trong ARB theo tiền lệ). gen-l10n + analyze sạch.
- [2026-06-10] [Dev] **Title tin nhắn bỏ IN HOA** (user): `_noteTitleLine` bỏ `.toUpperCase()` → sentence case "anh tuan · 20 giờ trước"; size 11→12, ls .4→.1 (caps-tracking không còn lý do khi hết caps; +1px bù Quicksand airy ở cỡ nhỏ). analyze sạch.
- [2026-06-10] [Dev] **Đổi font TOÀN APP: Be Vietnam Pro → QUICKSAND** (user "muốn phông mịn màng nhẹ nhàng", chọn Quicksand trong 3 phương án Nunito/Quicksand/Baloo 2): tải 5 static TTF (Light→Bold, ~73KB/file) từ fonts.gstatic.com vào `assets/fonts/`, **verify glyph tiếng Việt bằng fontTools (ấềộữđơư hoa/thường — đủ)**; pubspec khai báo family `Quicksand` 300–700 + **w800 trỏ file Bold** (Quicksand max 700 — khai báo trùng có chủ đích, tránh Flutter tự pick); đổi 1 hằng `AppTheme.fontFamily` (mọi style ăn theo qua `_displayBase/_bodyBase` + `ThemeData.fontFamily`). **Be Vietnam Pro GIỮ bundle + giữ khai báo pubspec làm rollback rẻ** (đổi lại 1 hằng). pub get + analyze sạch. ⚠️ Smoke-test cần soi: caption cỡ 11–12px (Quicksand airy, có thể cần +1px), số đếm hero 76px w800 (render từ Bold 700), dấu TV trên thiết bị thật. Docs sync: CLAUDE.md mục 3+8, design-system.md.
- [2026-06-10] [Dev] **Cấu trúc lời nhắn chuẩn hoá "title → tin nhắn"** (user): header khu vực luôn là `loveNoteLabel` chung (16 w800); MỖI tin theo cùng khuôn — title line caps nhỏ `_noteTitleLine` "{AI} · {LÚC NÀO}" (11 w700 ls .4 textSecondary, uppercase) rồi nội dung bên dưới (partner: 15 w400 alpha .86; mình: 14 w400 textSecondary 1 dòng); bỏ dòng time rời dưới note partner + bỏ time gắn đuôi text của mình; dùng lại key `loveNoteYouLabel` ("Bạn"); `loveNoteFromPartner/YourNoteLabel` hết dùng (giữ ARB). analyze sạch.
- [2026-06-10] [Dev] **Typography hierarchy lời nhắn (user soi screenshot: title vs note "bằng cấp"):** title "Lời nhắn từ {tên}" 15 w700 → **16 w800 ls -0.2** (nổi bật); note partner 15 w500 textPrimary → **w400 alpha .86 height 1.6** (dịu, vẫn là nội dung chính); dòng "của bạn" 14 w500 textPrimary → **13 w400 textSecondary** (lùi hẳn về phụ). 3 nấc rõ: title > note partner > note mình. analyze sạch.
- [2026-06-10] [Dev] **Lời nhắn chat v3 — CHỐT: UI cổ điển + ngữ nghĩa chat** (user đổi ý lần 3: "UI như cũ, nút thành Soạn tin nhắn, sheet tự xoá nội dung cũ, gửi khác thì cập nhật — không thì giữ nguyên"): revert layout về bản gốc (note partner + dòng "Lời nhắn của bạn" + 1 pill + phong bì 💌 giữ); pill label = key MỚI `loveNoteComposeCta` "Soạn tin nhắn/Compose a message"; `_LoveNoteSheet` dựng lại với 2 thay đổi: field khởi tạo TRỐNG (compose tin mới, không prefill để "sửa") + **luật giữ-nguyên**: bấm Lưu khi text rỗng HOẶC trùng y nguyên note hiện tại → `pop(false)` không ghi gì (không snackbar); khác → `setMyNote` + snackbar như cũ. XOÁ `_noteBubble`/`_buildNoteComposer`/`_sendNote`/controller inline (bản bubble+inline chỉ sống 1 vòng review). Key `loveNoteReplyCta/SendFirstCta/StartChat/YouLabel` hết dùng (giữ ARB); `loveNoteEmptyFromPartner/YourNoteLabel` dùng lại. analyze sạch.
- [2026-06-10] [Dev] **Lời nhắn chat v2: hiện CẢ 2 bong bóng + composer INLINE (bỏ bottom sheet)** — user yêu cầu tiếp: thread mini hiện tin gần nhất của TỪNG người (xếp theo `updatedAt`, cũ trên mới dưới như messenger; partner trái/trắng .60, mình phải/rose .12, caption tên · time), phong bì 💌 chỉ bọc bubble partner khi chưa đọc (AnimatedSwitcher unseal giữ); **ô nhập inline trong card** (white .55 viền rose .25 r18, 1–3 dòng, max 140 ẩn counter) + nút gửi tròn 42 rose (disable khi trống, spinner khi gửi, Semantics "Gửi") → `LoveNoteProvider.setMyNote`, clear field + haptic khi xong, KHÔNG snackbar (bubble tự hiện là feedback); XOÁ class `_LoveNoteSheet` + `_openLoveNoteSheet`. Key `loveNoteReplyCta/SendFirstCta` vừa thêm đã hết dùng (giữ ARB). analyze sạch.
- [2026-06-10] [Dev] **Lời nhắn → CHAT 2 người chỉ hiện tin MỚI NHẤT** (user chốt concept: "đoạn chat giữa 2 user, chỉ hiện tin mới nhất, lịch sử cả 2 cùng coi lại"): KHÔNG đổi backend — model sẵn có đáp ứng (2 doc `notes/{uid}` overwrite + history append 2 chiều, `LoveNoteHistoryScreen` đã render isMine/author như chat log). Client (`today_ritual_card.dart`): `_buildLoveNoteSection` tính `latest` = note có `updatedAt` mới nhất giữa myNote/partnerNote (`latestIsMine`) → render BONG BÓNG CHAT (mình = phải, rose .12, đuôi góc phải; partner = trái, trắng .60, đuôi góc trái; caption "Bạn/{tên} · relative time"); phong bì 💌 chỉ khi latest là của partner & chưa đọc; bỏ block "Lời nhắn của bạn" riêng; pill đổi "Viết/Sửa" → **"Nhắn lại…"** (có hội thoại) / **"Gửi lời nhắn đầu tiên…"** (trống); sheet compose LUÔN TRỐNG (mỗi lần gửi = tin nhắn mới, không phải edit — tin cũ đã nằm history); empty-state mới "Chưa có lời nhắn nào — gửi cho nhau lời đầu tiên nhé 💌". +4 key l10n vi/en (`loveNoteReplyCta/SendFirstCta/StartChat/YouLabel`) + gen-l10n. Key `loveNoteWriteCta/EditCta/EmptyFromPartner/YourNoteLabel` hết dùng ở Home (giữ ARB). analyze sạch. ⚠️ Edge biết trước: gửi lại Y NGUYÊN text cũ → provider bỏ qua history-append (chống duplicate sẵn có) — chấp nhận.
- [2026-06-10] [Dev] **Nhóm "Hôm nay của hai đứa" v3 — implement TRỌN 5 suggest của lượt review PD (user: "làm hết tất cả"):** widget MỚI [`lib/widgets/today_ritual_card.dart`](../../../lib/widgets/today_ritual_card.dart) gộp 2 card cũ thành 1 card nghi thức:
  1. **Tap-to-compose:** BỎ TextField lộ thiên trên Home — pill "Chạm để trả lời…" (trắng .55 viền rose .25 r16) mở bottom-sheet `_DailyAnswerSheet` mới (hiện lại câu hỏi khi gõ, 280 ký tự, autofocus, submit qua `DailyQuestionProvider.submit`); love note giữ sheet cũ (`_LoveNoteSheet` CHUYỂN từ home_screen sang file mới).
  2. **Blur-teaser:** partner trả lời trước mà mình chưa → câu trả lời hiện MỜ (`ImageFiltered` blur 7 + `ExcludeSemantics` chống screen-reader leak) + overlay "🔒 {name} đã trả lời rồi đó 👀" + pill nhấn mạnh rose-tint "Trả lời để mở khoá…". Đã verify: rules cho cả 2 đọc responses (reveal là client-side UI) + `partnerAnswer` getter có sẵn trước khi mình trả lời.
  3. **Done-state thu gọn:** card sinh ra khi ĐÃ reveal → 1 dòng "✓ Hôm nay cả hai đã trả lời · Đọc lại" (toggle expand/Thu gọn); reveal TƯƠI trong phiên → expand + confetti + Lottie one-shot (giữ nguyên protocol guard cũ: `_confettiPlayed` init theo `hasRevealed`).
  4. **Phong bì 💌:** note partner CHƯA ĐỌC → render phong bì rose-tint "Lời nhắn mới từ {name} · Chạm để mở 💌", tap = unseal (AnimatedSwitcher scale .96→1 + fade 280ms, haptic medium) + lưu marker đã-đọc vào Hive `app_settings` key `love_note_seen_<coupleId>` (marker = updatedAt millis ?? text.hashCode → note SỬA lại = phong bì lại); chưa load Hive → hiện note thường (không flash phong bì với note đã đọc).
  5. **Gộp card + 1 hàng archive:** 1 GlassCard (AnimatedSize 240ms) = section câu hỏi → divider → section lời nhắn → divider → hàng 2 link "Nhật ký | Lịch sử lời nhắn" (ẩn khi waiting_partner). `home_screen.dart`: XOÁ `_partnerName/_relativeTime/_buildLoveNoteCard/_buildLoveNoteHistoryEntry/_buildWriteNoteButton/_openLoveNoteSheet/_buildDailyQuestionCard` + class `_LoveNoteSheet`/`_DailyQuestionCard` (~580 dòng), thay bằng `_buildTodayRitualCard` (key `today-{coupleId}-{question}` — reset state theo ngày/couple, deep-link `_dailyQuestionKey` giữ nguyên); dọn import confetti/love_lottie/journal/history; skeleton 2 block → 1 block 210.
  - **l10n:** +8 key vi/en (`dailyTapToAnswer`, `dailyPartnerAnsweredTeaser{name}`, `dailyAnswerToReveal`, `dailyBothAnsweredToday`, `dailyReadAgain`, `dailyCollapse`, `loveNoteSealedTitle{name}`, `loveNoteSealedTap`) + gen-l10n.
  - KHÔNG đổi backend/rules/provider; partner-name resolve theo uid (sửa luôn flaw cũ fallback person2). `fvm flutter analyze` = No issues found. CHƯA commit. **Cần smoke-test 2 thiết bị:** teaser/reveal 2 chiều, phong bì persist qua restart, sheet submit, deep-link notification daily_question, vi/en.
- [2026-06-10] [Dev] **Eyebrow 10→11px** (`AppTheme.pageEyebrowStyle` trong `app_theme.dart` — user duyệt sau khi eyebrow thành ngày-hôm-nay = nội dung thật, 11px khớp ngưỡng a11y của nav label): ảnh hưởng mọi eyebrow chip (Home/Gallery/Profile/auth) — chủ đích, đồng bộ. Giữ nguyên màu trắng .92 + khung chip (designer khuyến nghị KHÔNG phóng to chip/đổi màu — eyebrow là metadata thì thầm, không cạnh tranh CounterCard). analyze sạch.
- [2026-06-10] [Dev] **Eyebrow chip header = NGÀY HÔM NAY theo locale** (user chọn Hướng 1 sau design review eyebrow): `home_screen.dart` thay `l10n.loveHomeBadge` ("TRANG CHỦ" — chữ chết, trùng bottom nav) bằng `DateFormat.MMMMEEEEd(Localizations.localeOf(context).toString()).format(DateTime.now()).toUpperCase()` → vi "THỨ BA, 10 THÁNG 6" / en "TUESDAY, JUNE 10" (pattern uppercase giống journal_screen; `initializeDateFormatting` đã có sẵn ở main). Khung chip + sparkles + bell GIỮ nguyên. Key `loveHomeBadge` hết dùng — GIỮ trong ARB theo tiền lệ, không gen-l10n. `fvm flutter analyze` = No issues found. CHƯA commit.
- [2026-06-10] [Dev] **Implement Home v2 theo design.md — thuần UI, KHÔNG đổi backend/model/rules/CF, KHÔNG deploy.** Chi tiết:
  - **`lib/l10n/app_en.arb` + `app_vi.arb`:** +5 key `homeGreetingMorning/Afternoon/Evening`, `homeTodaySectionTitle`, `notificationBellLabel` (placeholder `{count}` int). Chạy `fvm flutter gen-l10n` OK. Key cũ `helloGreeting`/`homeSubtitle`/`quickMomentsTitle`/`latestMomentsSubtitle`/`viewAllPhotos` KHÔNG còn dùng ở Home nhưng GIỮ trong ARB (không xoá — tránh vỡ nơi khác nếu sau này tái dùng; đã verify hiện không nơi nào khác dùng).
  - **`lib/screens/home_screen.dart`** (file duy nhất sửa code):
    - Header hợp nhất: bỏ title "Trang chủ" + subtitle; thêm `_greeting(l10n)` theo buổi (5–11h59 sáng / 12–17h59 chiều / còn lại tối, `pageTitleStyle().copyWith(fontSize: 26)`) + `AnimatedCoupleName` (trắng w700 20, heartSize 18) chuyển từ hero glass cũ lên header. **XOÁ `_buildHeroSection`** toàn bộ.
    - Reorder `_buildHomeTab` theo habit loop: counter(1) → banner waiting(2) → section "Hôm nay của hai đứa"(3) + daily question(3, giữ `_dailyQuestionKey` KeyedSubtree) + love note(4) → section "Kỷ niệm"(5) + CTA Thêm kỷ niệm(5) + recent photos(6) + on-this-day(6) → section "Cột mốc"(7) + milestone(7). Spacing trong nhóm 12 / giữa nhóm 28. `_entrance` GIỮ nguyên quy tắc params hằng (chỉ đổi order).
    - `_buildSectionTitle`: `subtitle` thành optional (nhóm 1 & 3 không subtitle; nhóm 2 chỉ subtitle `addPhotosPrompt` khi rỗng ảnh, action `seeAll` → tab Gallery).
    - Bell: `GestureDetector` → `Semantics(label: notificationBellLabel(unread), button, excludeSemantics, onTap)` + `Material`/`InkWell` bo 18 (tách `_openNotificationCenter()`).
    - CTA Thêm kỷ niệm: `GestureDetector` → `Material`/`InkWell` bo 20, splash TRẮNG .15 (ripple rose vô hình trên nền rose — deviation có chủ đích so với design "rose .08", đã ghi chú); tách `_buildAddMemoryCtaContent`.
    - Recent photos: `_buildRecentPhotosSection` thêm param `couple`; mỗi card bọc `Hero(tag: 'recent-photo-{id}')` + `Positioned.fill` `InkWell` (splash trắng .12) → `GalleryScreen.openPreview(photos: 5 recent, heroTags, initialIndex, couple)` — tap mở ĐÚNG ảnh thay vì nhảy tab.
    - On-this-day: chữ trắng → chữ tối (icon `accentRose`, title `textPrimary`, body `textSecondary`, chevron `textTertiary`) — fix contrast S1; `GestureDetector` → `ClipRRect`+`Stack`+`Positioned.fill` `InkWell` (ripple nằm TRÊN lớp glass).
    - `RefreshIndicator` + `backgroundColor: white` (spinner rose tàng hình trên nền hồng); nav label 10→11px; skeleton khớp layout mới (counter → 2 card dọc 140/96 r24 → block 120).
  - **Docs:** `project/design-system.md` (layout home_screen v2 + 3 quy tắc mới: chữ-trên-glass, ripple, nút-trong-card r16 — bỏ mô tả "hero glass/quote card" stale).
  - **Verify:** `fvm flutter analyze` = **No issues found**. Không đụng rules/functions → không cần `test-firebase-rules.sh`. CHƯA commit (chờ user).
  - **Nợ để lại (P3, theo design 2.10):** rename `displaySerif`; thu gọn daily question card sau reveal; CounterCard vẫn GestureDetector (onTap hiện null — ngoài scope).
- [2026-06-10] [Dev] **Fix card kẹt "sương xám" (user báo kèm screenshot):** root cause — `SharedCouplePhotoView.placeholder` chỉ áp cho nhánh FALLBACK, nhánh đang-tải-mạng hard-code `ShimmerSkeleton.fill()` xám; ảnh couple tải không về (kẹt loading) → shimmer xám + scrim đè mất gradient. Fix: thêm param optional `loading` vào `shared_couple_photo_view.dart` (`_buildLoading` = `loading ?? shimmer`, mặc định giữ nguyên cho mọi call-site cũ); `_AdaptivePhotoBackdrop` truyền `loading: SizedBox.shrink()` — đang tải/kẹt/lỗi đều lộ gradient hồng, ảnh về thì fade in. analyze sạch. ⚠️ Lưu ý debug: ảnh không tải được có thể do debug build trỏ Firebase DEV trong khi `couplePhotoUrl` là URL bucket PROD (CLAUDE.md mục 5 Dev/Prod split) — cần user xác nhận khi smoke-test.
- [2026-06-10] [Dev] **Adaptive scrim theo độ sáng ảnh** (user chọn phương án #2 sau câu hỏi "ảnh sáng có ăn chữ không"): tách ảnh+scrim thành widget con stateful `_AdaptivePhotoBackdrop` trong `counter_card.dart` (CounterCard giữ stateless) — đo luminance trung bình Rec.601 (0.299R+0.587G+0.114B) từ decode `ResizeImage` 64px qua `CachedNetworkImageProvider`/`FileImage`, cache static `Map<key,double>` (1 lần/ảnh, dùng chung mọi call-site), `didUpdateWidget` re-đo khi đổi ảnh, fail-soft giữ scrim mặc định; map `t=(L-0.25)/0.45` clamp 0–1 → black top lerp .08–.40 / bottom lerp .22–.55, `AnimatedContainer` 600ms easeOutCubic chuyển mượt khi đo xong. KHÔNG thêm dependency. analyze sạch. Nợ nhỏ giữ lại: halo trắng của số trên ảnh rất sáng đã được scrim đậm bù, chưa cần đổi sang bóng tối.
- [2026-06-10] [Dev] **Tinh chỉnh scrim nền ảnh theo user (ảnh chìm quá + sáng quá):** scrim hồng hạ .88/.78/.88 → .55/.38/.55 → .34/.18/.34 → .20/.10/.20 → **lượt 4 CHỐT: XOÁ HẲN lớp hồng** (user quyết sau khi xem thử mức thoảng) — scrim còn đúng 1 lớp black .16→.30; ảnh hiển thị nguyên màu gốc, brand tone do aurora glow/viền kính/pill gánh; THÊM lớp tối `black .16→.30` (top→bottom, đáy đậm hơn vùng pill/milestone) — ảnh rõ hơn hẳn, bớt wash sáng, chữ trắng giữ contrast nhờ lớp tối thay vì lớp hồng. analyze sạch.
- [2026-06-10] [Dev] **Ảnh chung 2 người làm NỀN CounterCard** (user chọn "Nền ảnh + scrim" trong 3 phương án nền/medallion/polaroid): `counter_card.dart` +2 param optional `photoLocalPath/photoRemoteUrl` + getter `_hasPhoto`; layer mới ĐÁY Stack = `SharedCouplePhotoView` full-card (decodeWidth = màn hình × DPR, placeholder rỗng — gradient base tự lộ khi ảnh chưa tải) + scrim `LinearGradient` sunset1/.88 → sunset2/.78 → sunset3/.88 (giữa trong hơn cho ảnh ló); aurora + nội dung đè trên. Home truyền `couple.couplePhotoPath/Url`. Không ảnh → card như cũ 100% (guest screen không đổi). analyze sạch.
- [2026-06-10] [Dev] **Redesign CounterCard "aurora glass" (user: bỏ tim, bắt mắt/xịn/tương lai):** viết lại `counter_card.dart` — BỎ `_buildHeartBadge`; bố cục: tên couple (headerExtra) → eyebrow "HAI BẠN ĐÃ BÊN NHAU" kẹp 2 hairline gradient trắng → số 44 w600 + halo (Shadow trắng .45 blur 22), divider vạch dọc → chấm sáng 5px; viền kính trắng .30 1.2px, thêm shadow lavender bloom; 3 lớp glow aurora (trắng/lavender/coral) "thở" chậm lệch nhịp 3.2–4s (flutter_animate repeat-reverse, params hằng — không re-init khi rebuild). API giữ nguyên + dùng chung guest_counter_screen an toàn (không headerExtra → mở bằng eyebrow). Chỉ dùng token sẵn có, không đổi màu chữ. analyze sạch.
- [2026-06-10] [Dev] **Refactor bố cục header (user): BỎ greeting theo buổi, tên couple GỘP vào CounterCard.** `counter_card.dart` thêm slot optional `headerExtra` (render giữa heart badge và title, Center + cách dưới 10); `home_screen.dart` header còn 1 hàng slim (badge chip + bell), truyền `AnimatedCoupleName` (trắng w700 20, WrapAlignment.center) vào `CounterCard.headerExtra`, XOÁ helper `_greeting`. 3 key `homeGreeting*` hết dùng (GIỮ trong ARB). Bố cục card: tim → tên couple → "HAI BẠN ĐÃ BÊN NHAU" → số → pill → streak → milestone. analyze sạch.
- [2026-06-10] [Dev] **REVERT TOÀN BỘ thay đổi MÀU trong ngày theo user ("để lại màu như cũ, refactor bố cục trước"):** (1) token mực về navy/gray gốc (#1A1A2E/#6B6B7B/#A0A0B0); (2) greeting về trắng; (3) BỎ ShaderMask gradient tên couple → trắng w700; (4) "Xem tất cả" về trắng; (5) banner chờ partner về glass trắng (white .18/viền .32, chữ trắng, chip mã trắng .22, `onDark: true`); (6) on-this-day về chữ trắng. **GIỮ nguyên phần BỐ CỤC/interaction Home v2:** header gộp + greeting theo buổi, reorder 2 nhóm, milestone trong CounterCard, recent tap→openPreview+Hero, InkWell/Semantics/ripple, bell Lucide outline (trắng), RefreshIndicator nền trắng, nav label 11px, skeleton mới, 5 key l10n. Yêu cầu "phông chữ thanh mảnh" đang xem dở → DỪNG chưa làm theo lệnh revert/bố-cục-trước. analyze sạch. Docs revert theo (design-system.md, CLAUDE.md). Nợ design giữ lại trong design.md (S1 contrast on-this-day) chờ đợt màu sau.
- [2026-06-10] [Dev] **Revert 2 chip header về trắng-glass theo user:** badge eyebrow (white .12/viền .18, sparkles + chữ trắng `pageEyebrowStyle()` mặc định) + bell (chip white .16/viền .18, `LucideIcons.bell` trắng 22, splash trắng .15) — user thấy bản rose-tint đậm quá. GIỮ: Lucide bell thay Lottie, Semantics, InkWell, greeting plum, tên couple gradient, banner sáng, seeAll `accentLoveDeep`. Ghi vào design-system là ngoại lệ duy nhất của luật mực tối. analyze sạch.
- [2026-06-10] [Dev] **Đổi họ mực TOÀN APP: navy lạnh → "Deep Plum" ấm** (user chê chữ đen, chọn Plum trong 3 phương án Plum/Burgundy/Cocoa): `lib/theme/app_colors.dart` — textPrimary #1A1A2E→**#43203A**, textSecondary #6B6B7B→**#7E5A70**, textTertiary #A0A0B0→**#A98EA0**. 1 token = 116 chỗ dùng đổi theo, gồm nút chính pill + snackbar (giờ nền mận đậm — chủ đích, hết "nút đen lạc tông"). Không còn hex cũ hardcode ngoài token. analyze sạch. Docs sync: design-system.md + CLAUDE.md mục 8.
- [2026-06-10] [Dev] **Header contrast pass + chữ gradient** (user duyệt sau review "2 màu chữ"): chốt quy tắc *chữ trực tiếp trên nền dawnBlush = mực tối; trắng chỉ trong bề mặt đậm*. `home_screen.dart`: (1) greeting → `textPrimary`; (2) badge eyebrow → rose chip (`accentLove .10` + viền .18, icon/chữ `accentLoveDeep`); (3) "Xem tất cả" TextButton → `accentLoveDeep` w700; (4) banner chờ partner → card trắng .72 + shadow nhẹ, icon link `accentLove` trên tint .10, title `textPrimary`/sub `textSecondary`, chip mã `accentLove .10`+chữ `accentLoveDeep`, `InviteActionButtons(onDark: false)`; (5) bell: BỎ hẳn Lottie bell + `ColorFiltered` (flood-tint thành silhouette đen đặc — user chê xấu, đúng) → chip rose-tint (`accentLove .10` + viền .18, r18) + `LucideIcons.bell` outline 22 `accentLoveDeep`, xoá `_buildBellGlyph` + import `lottie` khỏi home_screen (LoveLottie daily-reveal không ảnh hưởng); (6) **tên couple = chữ GRADIENT** `accentLoveDeep→accentLavenderDeep` (ShaderMask srcIn, child trắng opaque, w700→w800, heart ăn tint giữa gradient — chủ đích) — brand moment gradient duy nhất của màn. Milestone progress trong CounterCard giữ trắng (bề mặt đậm). analyze sạch. Quy tắc đã ghi vào `design-system.md`.
- [2026-06-10] [Dev] **Gộp milestone progress VÀO CounterCard** (yêu cầu user kèm screenshot): `lib/widgets/counter_card.dart` thêm slot optional `progressFooter` (full-width, render dưới `footerExtra`, cách 18 — không Center để progress bar tràn ngang); `home_screen.dart` thay `_buildMilestoneSection` (card trắng riêng) bằng `_buildInlineMilestoneProgress` white-on-gradient (award icon 16 + "Tiếp theo: {mốc}" 13 w700 + "{n}% there" 12 / bar trắng minHeight 8 trên track trắng .25 / dòng "Còn {n} ngày…" 12 trắng .85), truyền qua `progressFooter`; XOÁ nhóm 3 "Cột mốc" (section title + card) khỏi tab Home — trang còn 2 nhóm (Hôm nay / Kỷ niệm). Key `milestoneProgressTitle/Subtitle`, `daysCountLabel` không còn dùng ở Home (giữ ARB). Call-site khác của CounterCard (`guest_counter_screen`) không ảnh hưởng (param optional). `fvm flutter analyze` = No issues found. Đã sync `design-system.md`. CHƯA commit.
- [2026-06-11] [Dev] **Header: calendar leaf → GREETING cá nhân hoá + redesign bell** (yêu cầu user kèm screenshot): (1) `_buildCalendarLeaf` (số ngày + thứ/tháng) XOÁ, thay `_buildGreetingHeader` — lời chào theo buổi (tái dùng 3 key `homeGreeting*` 2026-06-10 + key MỚI `homeGreetingNight` "Khuya rồi đó,") + tên user (`AuthProvider.currentUser.displayName`, rỗng → bỏ dấu phẩy cuối) 21 w800 trắng, dưới là 1 dòng nudge xoay vòng từ pool **12 key `homeGreetingTeaser1..12`** ("Bạn đã chúc người ấy chưa? 💌"…) — chọn deterministic `(dayOfYear*7 + bucket*5) % 12` (7,5 nguyên tố cùng nhau với 12 → 4 buổi/ngày 4 câu khác nhau, 12 ngày đi hết pool, không nháy khi rebuild); (2) bell: disc tròn trắng → **squircle 48 r17 frosted** (gradient trắng .98→.86, viền trắng .65, shadow accentLove .30) + icon **gradient rose→lavender** qua ShaderMask, unread>0 đổi `bell`→`bellRing`, badge gradient love→loveDeep viền trắng + shadow, đặt -4/-4; (3) skeleton header khớp layout mới (2 dòng text + squircle 48); (4) bỏ import `intl` (hết DateFormat). ARB cả en+vi, gen-l10n, analyze sạch. CHƯA commit.
