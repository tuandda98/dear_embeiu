# Chat — Dev log

> Dev sở hữu file này. Ghi lại file/hàm đã đụng, thay đổi backend, trạng thái deploy.

## [2026-06-19] [dev] Deploy `firestore:rules` → DEV (chat read-receipt `receipts`)
- Theo lệnh user "deploy rules receipts lên cả dev". rules-test **178 passing**. Deploy output: *"already up to date, skipping upload"* ⇒ rules `receipts` (`firestore.rules:483-498`, chat read/delivery) ĐÃ có sẵn trên DEV từ lúc dựng feature chat-status 06-18 → "đã đọc/đã nhận" chạy đúng trên dev. Lệnh này xác nhận DEV = file rules hiện tại (gồm `receipts` + fix collectionGroup `reactions`).
- ⚠️ **PROD chưa có `receipts`** (feature 06-18 thêm SAU full-sync prod 06-15) → trên production ghi `readAt` sẽ permission-denied (fail-soft) → status kẹt ở "đã gửi". **Cần deploy rules PROD trước release — chờ lệnh user.**

## [2026-06-11] [dev] Implement trọn gói D1–D9 theo design.md — DEV deployed, chờ Tester

### Trạng thái: 💻 Dev xong full-stack · analyze 0 issue · 18 test pass · rules test 154 passing (10 case messages mới) · **ĐÃ DEPLOY DEV (rules + CF `notifyChatMessage`); PROD CHƯA deploy (chờ lệnh user)** · CHƯA commit

### File MỚI

| File | Nội dung |
|---|---|
| `lib/models/chat_message.dart` | `ChatMessage` (id, authorUserId, text, createdAt nullable khi pending, `isPending` từ `hasPendingWrites`, fromDoc/fromJson, parse Timestamp microsecond cho cursor) |
| `lib/services/chat_service.dart` | `watchMessages(coupleId, {limit:50})` orderBy createdAt desc + `snapshots(includeMetadataChanges:true)` (pending state); `fetchOlder` (cursor `Timestamp.fromDate`); `send` (trim ≤1000, `FieldValue.serverTimestamp()`, **fire-and-forget có chủ đích** — await sẽ treo composer khi offline, latency-compensation tự echo bubble pending). **Local fallback (quyết định Dev): Hive box `chat_messages_local` append-only, gửi/đọc local-only** — copy pattern `_appendLocalHistory` của LoveNoteService, rẻ nhất mà composer vẫn sống; KHÔNG sync partner (ghi rõ ở doc-comment) |
| `lib/providers/chat_provider.dart` | Pattern pagination SAU fix (map `_byId` tích lũy theo id + dedup, window 50, `hasMore`/`loadingMore`/`loadMore` + **guard `coupleId == _coupleId` sau MỌI await** — loadMore, markSeen, _loadSeenMarker, local re-load), `watchForCouple(coupleId, myUid)`/`clear()`. Sort desc với tiebreaker `_arrival` seq (pending createdAt null → đầu list; List.sort không stable). **Unread D7:** Hive `app_settings` key `chat_seen_<coupleId>` = epoch-millis tin partner mới nhất đã thấy; `hasUnread` (false khi marker chưa load — không nháy dot), `markSeen()` idempotent |
| `lib/screens/chat_screen.dart` | `ChatScreen(keyboardVisible, onRequestTab)` — header 2 tầng (hàng GHIM EyebrowChip `chatBadge` + HeaderIconButton history → push `LoveNoteHistoryScreen`; title block = item đầu list reverse, cuộn khuất); `_MessageList` divider ngày-mới-HOẶC-gap≥60ph, burst 3px góc dẹt 10, maxWidth 72%; `_ChatBubble` mine navy/partner trắng + avatar 24 cuối cụm + **pending = AnimatedOpacity .65 + clock3 11 ngoài bubble + Semantics `chatSending`**; `_MessageEntrance` (AnimationController thuần, KHÔNG flutter_animate — tránh crash element-tree như comment `_entrance` Home; chỉ animate tin đến SAU lần load đầu, set `_revealedIds` chống replay khi scroll-recycle; Reduce Motion → tĩnh); `_LoadMoreButton` (`journalLoadMore` — copy history); `_ChatSkeleton`; `_EmptyState`; `_WaitingPartnerState` (KHÔNG composer, CTA pill navy h52 → `onRequestTab(3)` tab Hồ sơ); composer card trắng r24 margin 16 + input pill surfaceLight r22 borderless maxLength 1000 counter ẩn + đĩa gửi 44 ghost↔NAVY (spec Designer, PO duyệt) + AnimatedPadding 260ms (đóng: `safeBottom+16+84+10`; mở: 10); haptic selectionClick/mediumImpact; giữ bàn phím sau gửi |

### File SỬA

| File | Thay đổi |
|---|---|
| `lib/screens/home_screen.dart` | Nav **4 tab** `[Home ♥ · Chat 💬(messageCircle) · Gallery 🖼 · Profile 👤]` (`_chatTabIndex=1`), IndexedStack chèn `TickerMode(ChatScreen)` index 1 (truyền `keyboardVisible` — **Scaffold strip viewInsets khỏi MediaQuery của body, đã verify source SDK** — và `onRequestTab`); TickerMode Gallery/Profile → 2/3; `_navLabel` + `_logTabScreenView` ['Home','Chat','Gallery','Profile']; "Xem tất cả" recent → index **2**; auto-`markSeen()` postFrame khi `_selectedIndex==1 && hasUnread` (cover cả tin đến lúc đang mở tab); **unread dot** Stack quanh icon (10px = lõi 7 accentLoveDeep + viền trắng 1.5, Positioned top-3/right-4, AnimatedScale 200ms, Reduce Motion = Duration.zero, chỉ render khi tab≠active) + Tooltip/Semantics kèm `chatUnreadDotSemantics`; re-arm `ChatProvider.watchForCouple` trong postFrame `_buildHomeTab`. **KHÔNG đụng `_entrance`** |
| `lib/main.dart` | Đăng ký `ChangeNotifierProvider(create: (_) => ChatProvider())` |
| `lib/app/session_resolver.dart` | Wire `chatProvider.watchForCouple` khi couple active; `chatProvider.clear()` ở cả 3 nhánh (guest / verify-email / no-couple) |
| `lib/services/push_notification_service.dart` | **Deep-link index ĐỔI:** `_homeTabIndex=0, _chatTabIndex=1, _galleryTabIndex=2`; case `chat_message` MỚI → 1; `photo_posted`/`photo_reaction` → 2 (qua hằng); 4 type cũ → 0 giữ. Comment cảnh báo sync 3 nơi |
| `lib/models/app_notification.dart` | Enum + parse `chatMessage`; `targetHomeTab`: photo → **2**, chatMessage → **1**, còn lại 0 (notif-center + auto-read theo tab dùng chung getter này → tự đúng) |
| `lib/screens/notification_center_screen.dart` | `_titleFor` case `chatMessage` → `notifChatMessage(name)`; icon hàng messageCircle/accentLove |
| `lib/screens/profile_screen.dart` | **Gỡ tile "Nhật ký lời nhắn"** khỏi "Tủ kỷ niệm" (D6) + divider + import; còn 2 hàng Journal/Streak, layout nguyên; screen+route GIỮ (lối vào mới = header chat) |
| `lib/l10n/app_en.arb` + `app_vi.arb` | +13 key: `navChat`, `chatBadge`, `chatHeaderSubtitle`, `chatEmptyHint`, `chatComposerHint`, `chatSendFailed`, `chatSendSemantics`, `chatSending`, `chatUnreadDotSemantics`, `chatWaitingPartnerTitle/Body/Cta`, `notifChatMessage(name)` — đúng copy design.md §C, no-ICU; `fvm flutter gen-l10n` sạch |
| `firestore.rules` | Block ADDITIVE `couples/{coupleId}/messages/{messageId}`: member read; create = author==auth.uid + `hasOnly([authorUserId,text,createdAt])` + text string 1..1000 + `createdAt == request.time` (chỉ serverTimestamp pass); **update/delete = false** (D3) |
| `firebase_rules_test/test/firestore.couples-sub.test.js` | +10 case messages: member create OK / 1000 OK / 1001 DENY / rỗng DENY / sai author DENY / thừa field DENY / createdAt cố định DENY / non-member read+create DENY / partner read OK / update+delete DENY (cả author). Suite **154 passing** |
| `functions/index.js` | CF v2 `notifyChatMessage` onDocumentCreated messages — pattern y `notifyLoveNote`: skip-self qua memberIds filter, authorName từ users (fallback "Người ấy"), `writeInboxNotifications` type `chat_message` **KHÔNG kèm nội dung tin** (privacy lock-screen, design §C), `sendToRecipientDevices` + `buildChatMessageText` vi/en ("Tin nhắn mới 💬 / <tên> vừa gửi cho bạn một tin nhắn 💌"). Không thêm mute-pref field (chat = always-on v1, như love_note) |

### Backend / Deploy

- ✅ `scripts/test-firebase-rules.sh` PASS (154 test, emulator).
- ✅ `node -c functions/index.js` (= `npm run lint`) OK.
- ✅ **DEV deployed (tonyembeiu-dev, 2026-06-11):** `npx firebase-tools deploy --only firestore:rules` + `--only functions:notifyChatMessage` (create thành công, Node 20 2nd-gen, us-central1).
- ⛔ **PROD: CHƯA — chờ lệnh user** (`--project prod`).
- Hive: KHÔNG adapter mới (box dynamic/String thuần) → không cần build_runner.

### Quyết định Dev (trade-off đã chốt)

1. **Local fallback = Hive local-only** (không disable composer): rẻ, đồng nhất pattern LoveNoteService; giới hạn = không sync partner, không pagination, stream one-shot (re-load sau send).
2. **KHÔNG extract bubble/divider chung với history** (design.md khuyên NÊN): copy-có-kỷ-luật vào `chat_screen.dart` để không đụng `love_note_history_screen.dart` vừa fix race. **NỢ:** hợp nhất `_ChatBubble`/`_TimeDivider`/`_LoadMoreButton`/`_ChatSkeleton` ra `lib/widgets/` khi cả 2 màn ổn định (param khác: maxLength 140/1000, divider per-day vs +gap60', composer strip vs card, pending chỉ chat có).
3. **`send()` fire-and-forget ở service:** await `.add()` khi offline sẽ treo spinner vô hạn; rules-DENY bất ngờ → tin biến mất im lặng (chấp nhận v1 — member hợp lệ không thể vi phạm rule vì client đã trim/clamp).
4. **`keyboardVisible` truyền từ HomeScreen:** đã verify Flutter SDK — Scaffold `resizeToAvoidBottomInset` xoá `viewInsets.bottom` khỏi MediaQuery của body → tab không tự đọc được; HomeScreen (context trên Scaffold) thấy đủ.
5. **Đĩa gửi NAVY** (không rose như chữ D8) — theo design.md đã được PO duyệt, đồng bộ composer history.
6. **markSeen đặt ở HomeScreen build (watch + postFrame)** thay vì onTap tab: cover đủ 3 đường vào (tap, deep-link cold/warm, tin mới đến khi đang mở tab), idempotent.

### Verify đã chạy

- `fvm flutter analyze` → **No issues found**.
- `fvm flutter test` → **18/18 pass**.
- `fvm flutter gen-l10n` → sạch.
- `./scripts/test-firebase-rules.sh` → **154 passing**.

### Điểm cần Tester soi (chưa verify runtime)

1. **Regression deep-link QUAN TRỌNG NHẤT:** tap push `photo_posted`/`photo_reaction` cold + warm phải mở **tab 2** (Gallery) + đúng ảnh; `chat_message` → tab 1; 4 type cũ → tab 0; tap từ notification-center cũng vậy (dùng `targetHomeTab`).
2. 2 máy realtime ≤1s; offline gửi → bubble pending mờ + clock → online tự rõ, KHÔNG duplicate (AC2).
3. Unread dot: bật khi tin partner đến lúc ở tab khác; vào tab → tắt; kill app mở lại vẫn tắt (Hive); tin của MÌNH không bật; Reduce Motion dot không scale.
4. Composer vs bàn phím trên máy thật (iOS home-indicator + Android): đóng = nổi trên nav hở 10, mở = sát bàn phím, chuyển 260ms; nav tự ẩn.
5. Phân trang: couple >50 tin → "Xem thêm" load 50 cũ; cuộn không giật (reverse list).
6. Waiting partner: không composer, CTA nhảy tab Hồ sơ (3); partner join realtime → tab chat sống lại không cần restart.
7. Auto-read inbox theo tab (`markReadForTab`) với index mới — vào Gallery (2) phải mark read notif photo, vào Chat (1) mark read notif chat.
8. Push nội dung: lock screen KHÔNG hiện text tin nhắn; vi/en theo `languageCode` device.
9. Local fallback (máy không Firebase/guest-coupled local): composer gửi được, tin chỉ ở máy đó, không crash.
10. `_logTabScreenView`/analytics screen_view tên tab mới ('Chat') — confirm GA4 DebugView khi smoke.
- [2026-06-12] [dev/lead] Vòng fix sau Tester (4 bug): #1 send-reject muộn nổi SnackBar (`onServerReject` → `sendRejections` counter → listener ChatScreen, offline thuần không báo lỗi); #2 `ChatWindow.isFromCache` — cursor/hasMore chỉ chốt từ server snapshot; #3 bỏ clamp substring UTF-16 (UI maxLength + rules là chốt); #6 Semantics `enabled: canSend`. Analyze 0, test 18/18. Nợ: #4 entrance seed sớm, #5 rules-test unauthenticated, #7 local Hive cap, perf-note watch ở HomeScreen.
- [2026-06-12] [dev/lead] Gỡ `_ChatTitleBlock` (title 32 "Trò chuyện" + subtitle) khỏi list + empty + skeleton theo user ("nhắn nhiều nó trôi lên trên"): trong reverse-list title nằm ở ĐẦU lịch sử (phía tin cũ nhất) — chìm sau vài tin, đứng cạnh "Xem thêm" lạc lõng; chip ghim + icon lịch sử gánh định danh màn. Key `chatHeaderSubtitle` hết dùng, GIỮ ARB. Chat = ngoại lệ chính thức của 🔒 RULE header lớn. Analyze 0 issue.
- [2026-06-12] [dev/lead] Chip ghim tab Chat thành DYNAMIC (user yêu cầu): `EyebrowChip` thêm slot `child` (giữ vỏ pill, label/icon thành optional + assert); chat render `AnimatedCoupleName` (TÊN1 ♥ TÊN2 uppercase, tim đập 12px `accentLoveDeep`, style `pageEyebrowStyle(.70)`) trong chip, Flexible chống tràn khi tên dài; fallback chip tĩnh `chatBadge` khi chưa có couple/tên. Analyze 0 issue.
- [2026-06-14] [dev/lead] Xoá icon đồng hồ `history` ở header tab Chat (lối vào DUY NHẤT tới `LoveNoteHistoryScreen`/Nhật ký yêu thương) theo user: tab Chat (tin nhắn realtime) giờ CHÍNH LÀ lịch sử trò chuyện của couple → lối vào "lời nhắn cũ" riêng thành thừa. Bỏ `HeaderIconButton` + `Spacer`/`SizedBox`, method `_openHistory`, 2 import (`header_icon_button.dart`, `love_note_history_screen.dart`); padding header phải 12→20 cho cân. `LoveNoteHistoryScreen` thành orphan (KHÔNG xoá file, feature Love Note ở Home/`today_ritual_card` vẫn nguyên). Analyze 0 issue.
- [2026-06-14] [dev/lead] **Pagination D5b — auto-load khi cuộn (infinite scroll)** (user "phân trang Chat": phân trang đã có từ D5 = window 50 + nút "Tải thêm" thủ công; user chốt nâng cấp tự nạp khi cuộn). `_MessageList` Stateless→**Stateful**: thêm `ScrollController` + listener `_onScroll` — `reverse:true` nên tin cũ ở `maxScrollExtent`, khi `pixels >= maxScrollExtent - 400` **và** `hasMore && !isLoadingMore` → gọi `widget.onLoadMore()` (= `provider.loadMore`, vốn đã re-entrancy-guarded → gọi thừa vô hại). Nút `_LoadMoreButton` GIỮ làm fallback (list quá ngắn không cuộn được). Prepend tin cũ ở đỉnh reverse list KHÔNG nhảy vị trí đọc. Chỉ đụng `chat_screen.dart` (provider/service pagination y nguyên). `flutter analyze chat_screen.dart` 0 issue. Client-only, không deploy.
- [2026-06-14] [dev/lead] AUTO-MIGRATE Love Note → Chat + huỷ feature Love Note (user: "gộp lịch sử love note cũ vào Chat rồi huỷ luôn, giờ chỉ xài Chat"). **Backend** (`functions/index.js`): helper `mirrorNoteToChat` tạo `messages/lovenote_<entryId>` (deterministic id → idempotent) field `migratedFromNote:true`, giữ đúng `authorUserId`+`createdAt` gốc; trigger `mirrorNoteHistoryToChat` (onCreate `noteHistory/{entryId}`) mirror realtime — couple LỆCH PHIÊN BẢN (B bản cũ gửi love note) thì A bản mới thấy ngay trong Chat; callable `migrateLoveNotesToChat({coupleId})` backfill toàn bộ noteHistory cũ (member-auth, idempotent, set flag `couples.loveNotesMigratedToChat`); `notifyChatMessage` SKIP khi `migratedFromNote` (tránh double-push với `notifyLoveNote` + tránh spam backfill). **Client**: `ChatProvider._kickLoveNoteMigration` gọi callable lazy 1 lần/session/couple khi mở Chat (fire-and-forget, nuốt lỗi). Rules KHÔNG đổi (admin SDK bypass — message migrated có 4 field + createdAt quá khứ, client create vẫn bị hasOnly-3-field chặn → an toàn). Deploy DEV OK (mirror+migrate created, notifyChatMessage updated). analyze 0, rules-test 154 pass. **Prod chờ lệnh user.** Nợ: note ở `notes/{uid}` mà appendHistory từng fail (offline) sẽ thiếu trong noteHistory → không migrate (edge hiếm).
- [2026-06-17] [dev/lead] **Chat = full-screen DRILL-IN (đổi hành vi tab, user yêu cầu "vào Chat ẩn luôn bottom nav + header có icon back").** Trước: Chat là tab peer trong IndexedStack, nav 4 tab luôn hiện (chỉ trượt ẩn khi bàn phím mở). Giờ: vào tab Chat → **ẩn hẳn floating nav** + **icon back ở header** (back về tab vừa rời). KHÔNG đổi sang pushed route (giữ IndexedStack child → watcher chat/provider nguyên, không vướng nav-gotcha SessionResolver). Cụ thể: (a) `home_screen.dart`: thêm state `_previousIndex` + helper `_selectTab(index)` (nhớ tab đang rời KHI vào chat); mọi điểm đổi tab (nav-tap, deep-link warm `_onNotificationTapRequest`, `onRequestTab` của Chat/Profile) đi qua `_selectTab`; `_buildFloatingNavigationBar` thêm `hideNav = isKeyboardVisible || _selectedIndex==_chatTabIndex` (nav slide+fade away khi ở Chat); `PopScope(canPop: _selectedIndex!=_chatTabIndex, ...)` để **back hệ thống Android** lúc ở Chat cũng `_selectTab(previous)` thay vì pop Home route/thoát app; ChatScreen nhận `onBack: () => _selectTab(prev==chat?0:prev)`. (b) `chat_screen.dart`: thêm param `onBack`, render `HeaderIconButton(arrow_left, semanticsLabel: l10n.back)` bên trái chip khi `onBack!=null`; composer `bottomClearance` bỏ chừa chỗ nav (`safeBottom + _navMargin + _navHeight + 10` → `safeBottom + 16`), gỡ 2 const `_navHeight`/`_navMargin` thừa. `dart format` + `flutter analyze` 2 file = 0 issue. **Verify thật trên emulator**: Home(nav hiện) → tap Chat(nav ẩn + ← back) → tap ←(về Home, nav hiện) ✓; UI dump xác nhận chat-state có "Quay lại", KHÔNG có nhãn nav. Client-only, không deploy. ⚠️ Lệch §8 CLAUDE.md "4 tab top-level CHIP-ONLY, header pinned, nav luôn hiện" — Chat nay là ngoại lệ (drill-in: chip + back, nav ẩn).
- [2026-06-19] [dev/lead] **Presence suppression — đang trong cuộc trò chuyện thì KHÔNG push cho nhau** (user: "đang nhắn tin qua lại thì không cần thông báo"). Tái dùng hệ **receipts** sẵn có (`couples/{id}/receipts/{uid}.readAt`) làm tín hiệu "đang xem" → **KHÔNG tạo collection/rules mới** (rule receipts có `hasOnly(['deliveredAt','readAt'])` nên không nhét field mới được — dùng luôn `readAt`). **CF `notifyChatMessage`**: trước khi gửi, đọc `receipts/{recipientId}.readAt` của từng người nhận; ai có `readAt` trong **ACTIVE_WINDOW_MS = 100s** → coi là đang trong chat → loại khỏi `idleRecipientIds` (bỏ CẢ inbox + push); hết người idle → return. **Fail-open** (lỗi đọc receipt → vẫn gửi, thà dư còn hơn mất). **Client**: `ChatProvider.pingPresence()` = bump `readAt` (markReceipt read:true); heartbeat đặt ở `home_screen` (KHÔNG ở chat_screen — chat sống trong IndexedStack, không dispose khi đổi tab) trong `_selectTab`: vào tab Chat → ping ngay + `Timer.periodic(40s)`; rời tab → cancel; OS suspend app nền → timer tự dừng → presence stale → lại nhận push (đúng ý). Đã có sẵn markSeen stamp readAt khi vào tab + mỗi tin partner đến lúc đang mở → CF-only cũng đã chặn đúng cho "nhắn qua lại" kể cả app cũ chưa có heartbeat. node -c OK, analyze 0, rules-test 178 pass. **DEV deployed `functions:notifyChatMessage`; PROD chờ lệnh.** Nợ/edge: ngồi đọc IM LẶNG >100s không có heartbeat (app cũ) thì 1 tin tới vẫn push 1 lần (app mới có heartbeat che ca này). Verify cross-device (2 máy) do user (single emulator không test được suppression).
- [2026-06-19] [dev/lead] 🐛 **FIX over-suppress: user "sau khi sửa xong nhắn ko thấy thông báo nữa".** Bản trên dùng `readAt` làm presence — SAI: `readAt` = thời điểm đọc cuối, **còn "tươi" tới cả window SAU KHI rời chat** (heartbeat bump readAt rồi rời → readAt mới <window) → CF chặn nhầm → rời chat rồi vẫn không nhận thông báo. **Sửa: tách field presence riêng `chatActiveAt`** (KHÔNG đụng readAt — readAt giữ đúng cho đã-đọc). (1) **rules**: receipts `hasOnly` thêm `chatActiveAt` (timestamp, optional) — rules-test 178 pass. (2) **ChatService.setChatActive(active)**: active→`chatActiveAt`=serverTimestamp (merge); !active→`FieldValue.delete()` (XOÁ ngay → CF hết thấy active → báo lại liền). (3) **ChatProvider**: `pingPresence`→setChatActive(true); `leavePresence`→setChatActive(false). (4) **home_screen**: `with WidgetsBindingObserver`; `_startChatPresence` ping + Timer 20s; `_stopChatPresence` **clear presence** + cancel; gọi trong `_selectTab` (vào/rời tab Chat) + `didChangeAppLifecycleState` (background→clear, resume→re-arm khi đang ở tab Chat); `_chatProvider` capture ở initState để clear an toàn trong dispose (không dùng context). (5) **CF**: đọc `chatActiveAt` thay `readAt`, **ACTIVE_WINDOW_MS=45s** (chỉ là backstop khi app bị kill mid-chat; rời sạch thì đã clear). node -c OK, analyze 0, rules-test 178. **DEV deployed rules+functions; PROD chờ lệnh.** Giờ: đang mở chat→không báo; rời chat/đóng app→báo lại NGAY.
- [2026-06-19] [dev/lead] **Header chat nhẹ nhàng + nhất quán** (user: "design lại header sao cho consistent với app và nhẹ nhàng thanh lịch"). Chip tên cặp đôi đang bị ép `minHeight: 44` (khối to) + chữ `15/w800/ls1.0` (chunky) — đây cũng là chip DUY NHẤT trong app bị ép cao 44 (Home/Gallery/Profile đều dùng EyebrowChip mảnh ~27 cạnh control 44 → chat đang LỆCH chuẩn). Sửa `chat_screen.dart`: bỏ `minHeight:44` ở cả chip tên + chip fallback (`chatBadge`) → chip về chiều cao tự nhiên như mọi tab khác; làm mềm typography `AnimatedCoupleName` `15/w800`→`13/w700`, alpha `1.0`→`0.85`, `heartSize 13→11`, `spacing 7→6`. Vẫn caps+tracked nên phân biệt rõ với tin nhắn `15/w400`. Nút back (đĩa frosted `backed` khi có ảnh nền) GIỮ 44 — chip mảnh center cạnh nó đúng pattern header chuẩn (chip + action 44). analyze 0. Client-only, không deploy.
- [2026-06-20] [dev/lead] **Chạm/cuộn vùng chat → bàn phím tự thu xuống** (user: "khi mở bàn phím lên, tab vào màn hình thì bàn phím tự lùi xuống"). `chat_screen.dart`: (1) bọc vùng `Expanded` (list/skeleton/empty/waiting) bằng `GestureDetector(behavior: opaque, onTap: FocusScope.of(context).unfocus())` → chạm chỗ trống quanh bubble = thu bàn phím; (2) ListView thêm `keyboardDismissBehavior: ScrollViewKeyboardDismissBehavior.onDrag` → kéo/cuộn list cũng thu (idiom Messenger/iMessage). KHÔNG phá: drag recogniser của ListView vẫn thắng scroll, InkWell con ("Xem thêm") vẫn thắng tap riêng → chỉ tap chỗ TRỐNG mới unfocus; composer (tap vào ô input) không bị ảnh hưởng. analyze 0, format OK. Client-only, không deploy.
- [2026-06-21] [dev/lead] **Tin nhắn kéo full-top (cuộn sau header)** (user: "cho kéo tin nhắn full top ở trên, hiện tại chỉ kéo được tới header"). Trước: `Column[header(48), Expanded(list), composer]` → trần list = đáy header, tin cũ nhất dừng dưới header. Sửa `chat_screen.dart` build: gộp list + header vào `Expanded(Stack[ Positioned.fill(GestureDetector>Builder>_MessageList) , Positioned(top:0,left:0,right:0, header) ])` → list tràn full chiều cao, cuộn SAU header (chip frosted nổi trên). Thêm param `_MessageList.topInset` (=48 = 44 chip + 4 gap) áp vào `ListView.padding.top` (reverse:true) để tin cũ nhất nghỉ ngay dưới chip khi cuộn đỉnh (giữ đúng vị trí at-rest cũ). Header overlay vẽ sau = trên cùng → back button vẫn nhận tap; tap vùng trống vẫn rơi xuống unfocus. analyze 0. Client-only, không deploy.
- [2026-06-21] [dev/lead] **Header chat kiểu iMessage + fix mũi tên back bị che** (user: "làm chat giống iphone/imess, mũi tên back bị che bởi text color"). Sau khi cho list cuộn sau header (full-top), mũi tên back (navy) đè lên bubble navy phía sau → mất nét. Sửa `chat_screen.dart`: (1) bọc header trong **thanh frosted nav bar** `ClipRect>BackdropFilter(blur 18)>Container(AppColors.white .28)` → tin nhắn cuộn dưới header bị làm mờ như nav bar iOS, chip + back đọc rõ; (2) back button `backed: widget.hasBackground`→**`backed: true`** (luôn có đĩa frosted) → mũi tên navy luôn trên nền trắng, không bị bubble/màu chữ che, cả khi không có ảnh nền. Thêm import `dart:ui show ImageFilter`. dart format + analyze 0. Client-only, không deploy. (Bubble navy/trắng bo tròn vốn đã giống iMessage; nếu muốn thêm: đuôi bubble, đổi màu — chờ user.)
- [2026-06-21] [dev/lead] **Chat giống iMessage: bỏ nền header + bubble hồng-đỏ + tail** (user nhiều vòng: "làm giống imessage" → "ko set bg cho header quá xấu" → "đổi sang hồng đỏ" → "thêm tail"). (1) **Gỡ thanh frosted nav bar** (user thấy xấu) → header trong suốt; mũi tên back `backed: true` (đĩa frosted) vẫn đọc rõ trên tin/ảnh; gỡ import `dart:ui`. (2) **Bubble**: mình = **navy `AppColors.textPrimary` (màu cũ)** — *từng thử gradient hồng→đỏ rồi user đổi ý "để màu đen, màu cũ" → revert về navy solid* (chữ trắng); người ấy = **xám** `_kChatPartnerGrey #E9E9EB` phẳng; chữ 16. Nút gửi cũng navy. (3) **TAIL**: `_BubbleBorder extends ShapeBorder` + `ShapeDecoration` → bo góc (full 20, góc nhóm/góc-dưới-người-gửi 6) **+ đuôi tail nhỏ hook góc dưới-ngoài bubble CUỐI nhóm** (`Path.combine union`, 7px; mine phải/partner trái), gradient fill liền cả tail. (4) Nút gửi → `_kChatMine`. ⚠️ **GOTCHA: `package:intl` export `TextDirection`** → override ShapeBorder "invalid override" (bind nhầm enum intl) → `import 'package:intl/intl.dart' hide TextDirection;`. analyze 0. Client-only. **Cần verify máy: hình tail (geometry áng chừng, chỉnh `_full/_flat/_tail`) + tương phản chữ trắng trên gradient hồng.**
- [2026-06-21] [dev/lead] **Typing indicator "đang soạn…"** (user: "thêm state đang typing"). Realtime qua Firestore, mô phỏng pattern presence `chatActiveAt`. **Backend (additive):** `firestore.rules` receipts `hasOnly` thêm `typingAt` (timestamp, optional) — rules-test PASS, **deploy DEV xong; PROD chờ lệnh**. **Service** (`chat_service.dart`): `ChatReceipt` typedef +`typingAt`; `watchPartnerReceipt` parse thêm; method `setTyping(coupleId, uid, {typing})` = serverTimestamp khi gõ / `FieldValue.delete()` khi dừng (merge). **Provider** (`chat_provider.dart`): GỬI — `setTyping(bool)` throttle ghi ≤1 lần/3s + `_typingStopTimer` 4s tự clear khi ngừng gõ; tự `_stopTyping()` khi send/leavePresence/clear. NHẬN — `_updatePartnerTyping(typingAt)` từ receipt stream: hiện khi value MỚI + tươi (<10s, lọc leftover cũ lúc mở), `_partnerTypingHideTimer` 6s tự ẩn (phòng app-kill ko clear), ẩn ngay khi partner clear (null). Getter `partnerTyping`. **UI** (`chat_screen.dart`): ô nhập `onChanged → _chat.setTyping(text.isNotEmpty)`; widget `_TypingIndicator` (3 chấm nhún `AnimationController` repeat, bubble xám partner `_BubbleBorder` có tail) đặt giữa list & composer khi `provider.partnerTyping`. Chỉ chạy khi `isUsingFirebase` (local fallback no-op). Best-effort try/catch toàn bộ. analyze 0, dart format. **⚠️ Cần deploy PROD rules trước khi release để typing chạy ở prod (fail-soft: thiếu rule → write bị từ chối, app ko crash, chỉ ko có typing).**
- [2026-06-22] [dev/lead] **FIX typing "ko hoạt động"** (anh tuan gõ Android → em iOS ko hiện). Read-receipt (đã đọc) vẫn chạy ⇒ receipt stream OK ⇒ nghi gate freshness `DateTime.now().difference(typingAt) < 10s` lệch trên **simulator iOS clock drift** → tin typing bị coi "cũ" → bỏ qua. **Sửa clock-independent:** bỏ hẳn so-với-now; thêm `_partnerReceiptPrimed` (bỏ qua snapshot ĐẦU sau (re)subscribe = trạng thái hiện tại/leftover, không show) → chỉ phản ứng với typingAt **thay đổi** sau đó = đang gõ thật; vẫn auto-hide 6s + ẩn ngay khi null. Reset primed ở `watchForCouple`+`clear`. analyze 0. **⚠️ Feature thêm field/state mới vào provider → HOT RELOAD không áp; phải HOT RESTART (Shift+R) / re-run CẢ 2 máy.**
- [2026-06-22] [dev/lead] **BỎ tail** (user: "cái tail nhìn xấu quá để như cũ"). `_ChatBubble` về `BoxDecoration`+`BorderRadius` bo tròn (full 20, góc nhóm flat 6, KHÔNG tail) — giữ navy(mình)/xám(người ấy). `_TypingIndicator` về `BoxDecoration` bo tròn 20. **Xoá hẳn class `_BubbleBorder`** + revert import `intl` (bỏ `hide TextDirection` — chỉ cần khi có ShapeBorder override). analyze 0.
- [2026-06-21] [dev/lead] **Deploy PROD rules `typingAt`** (user: "deploy prod rules"). `npx firebase-tools deploy --only firestore:rules --project prod`. Pre-check: diff vs snapshot prod gần nhất (`20260620T072735Z-PROD`) = CHỈ thêm `typingAt` (additive); rules-test PASS. ✅ released, trace `20260621T044706Z`. ⟹ typing-indicator chạy được ở PROD khi bản 1.3.3 lên store. (DEV đã deploy 06-21 từ trước.)
- [2026-06-21] [dev/lead] **Typing hiện LIÊN TỤC khi đang soạn** (user: "typing hiện mấy s rồi mất, muốn hiện hoài tới khi gửi"). Trước: gửi typingAt throttle 3s + `_typingStopTimer` 4s tự clear khi ngừng gõ → ngừng gõ vài giây (suy nghĩ) là tắt. **Đổi sang HEARTBEAT:** `setTyping(hasDraft)` — còn chữ → bật `_amTyping` + `Timer.periodic(3s)` refresh `typingAt` đều đặn (KHÔNG phụ thuộc keystroke, qua được pause); `_stopTyping` huỷ heartbeat + ghi null. Clear chỉ khi: xoá hết chữ (onChanged false), gửi (`send`), rời chat (`leavePresence`). Viewer hide timer 6s→**8s** (>heartbeat 3s, dung sai 2 nhịp; chỉ ẩn khi app-kill, còn gửi/xoá thì clear→ẩn ngay). Bỏ `_lastTypingWriteMs`/`_typingStopTimer`. analyze 0. Client-only (rule typingAt đã có). **⚠️ Sau build 1.3.3 → chưa vào artifact.**
