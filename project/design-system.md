# Design system — Dear Embeiu

> Trích nguyên văn từ CLAUDE.md (Mục 8) trong đợt tái cấu trúc 2026-06-03 để giữ CLAUDE.md gọn. Nội dung KHÔNG đổi. Designer/Dev đọc file này khi cần chi tiết; CLAUDE.md chỉ giữ tóm tắt + link về đây.

## ⭐ CẬP NHẬT 2026-06-11 — DESIGN UNIFY (đọc mục này TRƯỚC; chi tiết đầy đủ: [`features/design-unify/design.md`](features/design-unify/design.md))

> Toàn app đã được đồng bộ theo ngôn ngữ Home (yêu cầu user 2026-06-11: "tất cả phải đồng nhất, bắt buộc dựa trên màn hình Home"). Mục 8 bên dưới GIỮ làm nền + lịch sử, nhưng vài đoạn mô tả Home đã **STALE** — đoạn nào mâu thuẫn với mục này thì mục này thắng.

**Hiện trạng Home v3 (code 2026-06-11 — khác mô tả cũ ở mục 8):**
- Header Home = **greeting theo buổi + tên user** (trắng 21 w800 + bóng tối 0x52 blur 10) + dòng teaser xoay vòng 13.5 w500 (12 key `homeGreetingTeaser*`) — calendar-leaf ĐÃ BỎ (key `loveHomeBadge` hết dùng nhưng giữ ARB).
- Bell = **ICON TRẦN navy `textPrimary` 26** trong vùng chạm 48 (user 2026-06-11 vòng 2: "bỏ cái badge màu trắng đi, làm hết toàn app" — squircle trắng + ShaderMask gradient ĐÃ BỎ); badge đỏ số chưa đọc giữ, neo right/top 2.
- Đồng hồ CounterCard = **pill kính 3 cột GIỜ/PHÚT/GIÂY đếm TỔNG thời gian bên nhau** (user 2026-06-11: "đếm số giờ bên nhau chứ không phải thời gian thực") — cột GIỜ = tổng giờ lũy kế từ ngày kỷ niệm, format nghìn theo locale ("17.640"), bề rộng tự co; PHÚT/GIÂY positional 2 chữ số. KHÔNG dùng `% 24` cho cột giờ (sẽ thành đồng hồ thật). Tick 1s check `TickerMode.valuesOf` khi tab ẩn.
- MemoryCinema = card **trong gutter 16, r28, CÓ shadow black .12** — không còn full-bleed.

**Token sheet chuẩn toàn app (derive từ Home — bản đầy đủ PHẦN A của design.md):**
- **Nền mọi màn:** `dawnBlush` (Journal đã đổi từ mint về blush; `dreamyMint` chỉ còn accent trang trí). Hero duy nhất = `sunsetRomance` (CounterCard).
- **Radius scale:** 28 hero/nav/glass-form · **24 card nội dung trắng đặc** · 22 tile white .72 · 20 input · **16 nút-trong-card / icon squircle 44 / compose pill** · 17 bell 48 · 999 pill. Hết r30.
- **Shadow:** card trắng = `black .06 blur 16 offset(0,10)` (chuẩn duy nhất — `ContentCard`); media = black .12; **chữ trắng trên blush = bóng TỐI black .28–.32 blur 8–12 — KHÔNG halo trắng** (halo chỉ cho số hero trên scrim tối). `AppTheme.pageTitle/pageEyebrow/pageSubtitleStyle` đã có bóng tối MẶC ĐỊNH (smart: chỉ khi màu trắng; tắt bằng `shadowed: false`).
- **Ripple:** mọi phần tử bấm = InkWell (hết GestureDetector trần) — **rose .08 nền sáng · trắng .12–.15 nền đậm/ảnh/chip gradient**.
- **GlassCard:** CHỈ form auth/setup/guest + bottom nav + overlay trên ảnh. CẤM list dài + card nội dung (NotifCenter đã bỏ glass).
- **Nút:** primary = pill r999 **height 52** (navy theme; rose cho hành động tình cảm) · nút trong card = r16 · destructive = error r16 · secondary outlined = r999.
- **Reduce Motion (WCAG 2.3.3) đã vá đủ:** aurora counter, cinema auto-advance + Ken Burns (Timer còn dừng theo TickerMode khi tab ẩn, chỉ restart khi gate đổi), marquee gallery (tĩnh + tự chạy lại), `EntranceReveal`.
- **D3:** mọi `DateFormat` hiển thị truyền locale.

**HEADER — 1 hệ duy nhất toàn app (header-sync 2026-06-11; vòng 4 CÙNG NGÀY đảo mực trắng→NAVY sau khi screenshot thật chứng minh trắng-trên-blush chỉ ~1.7:1):**
- **MỰC HEADER = NAVY `textPrimary`, KHÔNG bóng** trên nền dawnBlush — eyebrow alpha .55, title 1.0, subtitle .62 (default mới của `pageEyebrow/pageTitle/pageSubtitleStyle`). **Chữ TRẮNG + bóng tối CHỈ còn hợp lệ trên scrim tối/ảnh/pill gradient đậm** (CounterCard, hero Profile, overlay ảnh, bottom nav, pill chọn) — helper vẫn hỗ trợ `color: white` cho các chỗ đó (shadow tự bật).
- **Eyebrow = `EyebrowChip`** (`lib/widgets/eyebrow_chip.dart`, vòng 5 — user yêu cầu chip trở lại, phối màu hệ mới): pill fill white .72 + viền trắng .65 + shadow rose .14 blur 10 + icon 13 `accentLoveDeep` (tuỳ màn: lock/userPlus/keyRound/mailCheck/sparkles/pencil/heart) + label `pageEyebrowStyle(alpha: .70)` 11 w700 ls1.4 navy. Chip → title cách 14. KHÔNG dùng lại bản chip kính trắng cũ (white .12 + chữ trắng — fail contrast). Eyebrow KHÔNG lặp chữ của title (vd `editCoupleBadge` = "HỒ SƠ CẶP ĐÔI", không phải "CHỈNH SỬA"). Dùng ở mọi header.
- **🔒 4 TAB = CHIP-ONLY (header-unify 2026-06-14, user "xoá title, chỉ để chip, đồng bộ cả 4 màn"):** Home/Chat/Gallery/Profile mỗi tab chỉ còn **1 hàng header = chip trái + (tuỳ chọn) 1 action icon phải**, KHÔNG còn `pageTitle`/`pageSubtitle` to. Geometry chung: gutter ngang **16**, top **16**, `EyebrowChip` trái + `Spacer/Align` đẩy icon sang phải (`HeaderIconButton` 44 / bell 48, mép phải = 16). **🔒 RULE căn dọc (2026-06-14):** header Row PHẢI dùng `crossAxisAlignment: CrossAxisAlignment.start` (Home dùng `Align.topLeft` cho chip) — action icon (44/48) CAO hơn chip (~27); nếu để `center` mặc định thì chip bị căn-giữa-dọc đẩy xuống (iconH−chipH)/2 ⇒ tab có icon (Home/Profile) lệch thấp hơn tab không icon (Chat/Gallery) ~22–28px. Top-align → chip ở đúng safeArea+16 trên cả 4 tab (verify đo pixel: spread 0). Chip từng tab: **Profile** = `loveProfileBadge`+sparkles & settings icon · **Gallery** = `privateGalleryBadge`+sparkles, KHÔNG icon · **Chat** = `AnimatedCoupleName` dynamic (fallback `chatBadge`), KHÔNG icon (đã gỡ history 2026-06-14) · **Home** = **lời chào theo giờ TRONG chip** (icon `sunrise/sun/sunset/moon` + greeting+tên, `.toUpperCase()`, ellipsis) & bell. Dòng nudge/teaser cũ ở Home đã bỏ. Gallery dùng `SliverPersistentHeader` collapse → `_floatingTopShowcaseMaxHeight` chỉnh **340→296** sau khi bỏ title+subtitle. **NỀN đồng bộ:** dawnBlush vẽ ĐÚNG 1 LẦN bởi Home shell (Container ngoài, từ y=0 sau status bar) — cả 4 tab nằm trong IndexedStack đều TRONG SUỐT (Profile/Gallery đã gỡ Container gradient riêng vốn làm gradient chéo lệch gốc ~60px). Gallery scrim header chỉ fade-in theo độ cuộn (`scrim = easeOut(progress)`, rest=0 trong suốt) để không hồng hơn tab khác. **Màn tab mới: KHÔNG tự vẽ nền — để trong suốt, nền chung lo.**
- **Màn CON (sub-screen) vẫn header lớn** (auth ×4, guest, setup + mọi màn con): eyebrow trần → `pageTitleStyle` 32 → `pageSubtitleStyle` — tất cả navy. (Khác 4 tab: tab tối giản chip-only, màn con giữ title mô tả.)
- **🔒 RULE MỌI SCREEN (kể cả screen MỚI — chốt vòng 5c, 2026-06-11, user yêu cầu):** **(REDESIGN v2 2026-06-14, user "chip xích trái, cùng hướng dọc với title"):** Màn con dùng widget chung **`SubScreenHeader`** (`lib/widgets/sub_screen_header.dart`) = 1 CỘT canh-trái mép gutter: **`←` back (glyph nudge -10 cho thẳng cột) → `EyebrowChip(badge, icon)` → `pageTitleStyle(28)` → `pageSubtitleStyle`**, param `trailing` (action phải hàng back: Save/counter/overflow). KHÔNG dùng `appBar`/`subScreenAppBar` nữa; SafeArea bỏ `top:false`. Header = item đầu body scroll (settings/journal/milestone/form) hoặc fixed trên list (notif-center/custom-reminders 4-state). **Title màn con = 28** (landing/auth hero 32). ⚠️ **Bản v1 "chip cạnh back trên app bar" ĐÃ BỎ** (chip bị thụt phải, lệch cột title). `subScreenAppBar` còn tồn nhưng 0 consumer màn con — chỉ dùng nếu có lý do đặc biệt. Badge/icon từng màn: settings=settings · notif=bell `notifCenterBadge` · journal=bookOpen `journalBadge` · history=mail `loveNoteHistoryBadge` · milestone=flag `milestoneBadge` · custom-list=bellRing `customRemindersBadge` · custom-form=bellPlus `customReminderFormBadge` (subtitle keys `*HeaderSubtitle` tương ứng). **Screen mới = bắt buộc theo rule này** (badge KHÔNG lặp chữ title). ⚠️ **Rule header lớn này CHỈ áp cho màn CON** — **4 tab top-level (Home/Chat/Gallery/Profile) đã chuyển sang CHIP-ONLY** từ 2026-06-14 (xem mục riêng ở trên). Tab Chat: chip DYNAMIC `AnimatedCoupleName` "TÊN1 ♥ TÊN2" uppercase + tim đập 12 loveDeep (fallback `chatBadge`), **icon history đã GỠ 2026-06-14** (tab Chat tự đóng vai "lịch sử trò chuyện", `love_note_history` thành orphan — giữ file, Love Note ở Home vẫn xài). Keys hết dùng nhưng giữ ARB: `chatHeaderSubtitle`, `galleryTitle`/`gallerySubtitle`, `profileTitle`, `homeGreetingTeaser1..12`.
- **Phụ kiện header trên gradient cùng họ "bề mặt sáng + mực navy":** LanguageToggle pill fill white .72 viền .65 chữ navy · sign-out pill setup white .72 navy .85 · month chip gallery white .72 navy .75 · invite card setup = ContentCard trắng đặc (code `accentLoveDeep` 30 w900, GlassCard đã bỏ ở card này).
- Badge/pill trong NỘI DUNG card (today-badge gallery, streak chip, on-this-day…) KHÔNG phải header — giữ.
- **Copy:** title sentence-case tiếng Việt ("Thư viện ảnh", không "Thư Viện Ảnh"; ngoại lệ duy nhất: brand `appTitle` "Kỷ Niệm Của Chúng Mình"); caption không lặp nguyên văn label (invite card couple-active bỏ description).

**TYPE SCALE — chỉ được dùng các cỡ này (vòng 3, 2026-06-11 — đã quét sạch 49 chỗ cỡ lẻ 9.5/11.5/12.5/13.5/14.5/15.6/16.5/17/20.5…):**
`10` micro-caps (label đồng hồ, badge nhỏ) · `11` eyebrow/nav · `12` caption/meta · `13` body-s · `14` label/body · `15` tile title/body-m · `16` card header/input/Save · `18` app bar · `20` title-m · `21` prominent content (câu hỏi ritual, greeting Home) · `22` section · `26` headline · `30/32` page/hero name · display 40/56/76 (số đếm/code). Cỡ chữ MỚI phải lấy từ scale này — không chế cỡ lẻ.
**MÀU CHỮ:** chỉ token `AppColors` (textPrimary/Secondary/Tertiary, white + alpha, accent theo vai trò) — không `Colors.*` trần. **BUTTON:** ăn theme (label 15 w600, pill h52); ngoại lệ hợp lệ duy nhất: action section 14 w700 textPrimary, Save app-bar 16 w800 trắng; màu = rose (primary tình cảm) / navy theme / error (destructive) / white .72 (sign-out).

**Primitives dùng chung (`lib/widgets/` — DÙNG thay vì tự chế):** `ContentCard` (card trắng đặc r24 black .06) · `subScreenAppBar()` + `HeaderIconButton` (**ICON TRẦN navy 24 trong vùng chạm 44, ripple r16 — KHÔNG còn squircle trắng**, user 2026-06-11 "bỏ badge trắng, làm hết toàn app"; back + mọi action header dùng chung; bell Home 48 cùng ngôn ngữ trần; **ngoại lệ: icon đè trên ẢNH (vd pencil hero Profile) GIỮ đĩa nền** — mực trần không đảm bảo contrast trên ảnh user) · `SectionHeader` · `IconBadge` (squircle tint .12, 44/r16) · `InkTile` (tile white .72 r22 splash rose .08) · `ComposePill` · `EntranceReveal` (entrance 1-lần + Reduce Motion) · `ScreenBackground` (B1 — chưa có consumer, wire dần). Màn con MỚI bắt buộc: nền dawnBlush + `subScreenAppBar` + `ContentCard`/`InkTile` + token trên.

**Per-screen sau unify:** auth ×4 + guest (banner mực-tối, submit pill h52, hint textSecondary) · setup (error banner sáng, mode tab InkWell, nút pill) · gallery (surface trắng đặc, feed 28/ảnh 24, month chip bóng tối) · journal (nền blush, ContentCard) · **history = CHAT MESSENGER-STYLE (2026-06-11, polish vòng 2 cùng ngày):** bubble mình phải **NAVY `textPrimary` đặc** chữ trắng (bản gradient đỏ bị loại — đỏ-trên-hồng vibrate + trùng palette destructive; navy = primary-action) / partner trái trắng navy + **avatar chữ cái đầu 24 `primaryGradient` cạnh bubble cuối cụm**, KHÔNG tên per-message, timestamp = **divider 1/NGÀY micro-caps UPPERCASE `textSecondary` w700**, cụm cùng người cách 3px góc dẹt 10, `reverse:true` mở ở tin mới nhất, load-more pill trên cùng (pagination), **+COMPOSER BAR (chat thật, 2026-06-11):** strip trắng shadow hắt lên, nền KÉO QUA home indicator (SafeArea bottom off, content tự pad — không hở gradient dưới) + input pill surfaceLight r22 **KHÔNG viền (override cả enabled/focusedBorder — viền rose theme trong chat đọc như lỗi validation)** maxLength 140 counter ẩn + đĩa gửi 44 ghost-state (rỗng = surfaceLight/icon tertiary → có draft = navy/trắng, AnimatedContainer 180ms; spinner khi gửi) — gửi qua `setMyNote` (cùng flow ritual Home, ĐÃ cho phép gửi trùng), lỗi giữ draft + snackbar `loveNoteSendFailed` · notif-center (ContentCard + shimmer thay spinner; notif unread = viền accentLove .45 + dot) · profile/settings (ContentCard, header card 16 w800 + icon rose trần, IconBadge, sign-out pill) · reminders ×3 (subScreenAppBar, InkWell chips, gold→lavender) · language pill (InkWell + bóng tối khi onDark). Chi tiết từng màn: design.md PHẦN C.

## 8. Design system

Brand "Sunset Romance" — romantic minimalism, soft gradient hồng, glassmorphism, serif cho số hero, trái tim là motif chính. Style ở `lib/theme/app_colors.dart` + `app_theme.dart`; chỉ light mode, Material 3.

Màu (AppColors) — 3 gradient chủ đạo (topLeft→bottomRight):
- `sunsetRomance` = [#FF6B9D, #FF8FA3, #FFB6C1] — hero (counter card). Alias `primaryGradient`/`counterGradient`.
- `dawnBlush` = [#FFC1CC, #E8B4D8, #C8A8E9] — nền app & auth/main screen. Alias `secondaryGradient`.
- `dreamyMint` = [#FFD6E0, #E0D4F7, #C6E5D9] — gallery/milestone. Alias `galleryGradient`.

Accent: accentLove #FF4D6D (=accentRose), accentLoveDeep #E63956, accentLavender #A78BFA, accentLavenderDeep #7C5CD6 (label câu trả lời partner trên nền lavender tint .10 — feature couple-journal), accentCoral #FF8FA3, accentGold #E8B4D8.
Surface: bgLight #FAFAFC, surfaceLight #F5F0F5, cardSurface #FFFFFF.
Text: textPrimary #1A1A2E (cũng là nút chính), textSecondary #6B6B7B, textTertiary #A0A0B0, textOnGradient #FFFFFF. *(Thử nghiệm "Deep Plum" 2026-06-10 đã REVERT theo user — ưu tiên refactor bố cục trước, màu bàn sau.)*
Status: success #66BB6A, error #EF5350, warning #FFA726, info #A78BFA.

Typography (cập nhật 2026-06-10): **1 phông QUICKSAND cho TOÀN APP** — bundled `assets/fonts/` (OFL, static 300–700, **w800 map vào file Bold** — khai báo 2 lần trong pubspec; đủ glyph tiếng Việt dấu chồng, đã verify fontTools), rounded terminals = cảm giác "mịn màng nhẹ nhàng" user chốt sau 3 phương án Nunito/Quicksand/Baloo 2. Be Vietnam Pro GIỮ bundle làm rollback rẻ (đổi 1 hằng `AppTheme.fontFamily`). *(Lịch sử 2026-06-06: Be Vietnam Pro thay Fraunces+Plus Jakarta+google_fonts vì vỡ dấu/nháy font — lý do bundle-offline vẫn giữ nguyên.)* Chi tiết cũ: **1 phông Be Vietnam Pro cho TOÀN APP** — bundled `assets/fonts/` (weight 300–800, OFL, Việt-first, dấu chồng ấ/ề/ộ/ữ chuẩn, offline). **ĐÃ BỎ Fraunces + Plus Jakarta Sans + `google_fonts`** (lý do: google_fonts fetch runtime gây vỡ dấu tiếng Việt + nháy font trên iOS). Wired tập trung ở `app_theme.dart`: `static const fontFamily = 'Be Vietnam Pro'` → `ThemeData.fontFamily` + `_displayBase/_bodyBase` + helper no-context (`pageEyebrowStyle/pageTitleStyle/pageSubtitleStyle/sectionTitleStyle/sectionSubtitleStyle/dayCountStyle`). Weight: hero/số đếm w700/800 (`dayCountStyle()` ≈ 76px + glow trắng), body w400/500. Size giữ: title 20/16/14, body 16/15/13, label 14/12/11. Quy ước letter-spacing: số lớn âm, label/caps dương. ⚠️ Không còn serif — "hero" giờ là Be Vietnam Pro ở weight nặng.

Revamp Đợt 1 — hạ tầng mới (2026-06-02, → [`project/features/ui-revamp/`](project/features/ui-revamp/overview.md)):
- Iconography: Material `Icons.*` → Lucide (`lucide_icons`) toàn app; giữ Material `favorite` cho heart (Lucide không có filled heart).
- GlassCard (`lib/widgets/glass_card.dart`): glass THẬT (ClipRRect+BackdropFilter blur 18+fill .16+viền highlight) — thay "glass giả" ở auth/setup/guest/home-hero. KHÔNG dùng cho list cuộn dài (hiệu năng) hay card trắng đặc (settings/profile/gallery feed).
- ShimmerSkeleton (`lib/widgets/shimmer_skeleton.dart`, package `shimmer`): thay `CircularProgressIndicator` ở loader ảnh/nội dung; giữ spinner inline trong nút submit + splash.
- Motion: `AppMotion` (`lib/theme/app_motion.dart`) token fast 200/base 280/slow 320/entrance 360/stagger 50ms, curve easeOutCubic. `flutter_animate` staggered entrance (fadeIn+slideY 8px) cho home/settings/gallery-feed(6 đầu)/reminders — chạy 1 lần (`_OnceEntrance`). `HapticFeedback` ở đổi tab/đăng ảnh/submit/đổi ngày/toggle. Tile bấm → `InkWell` ripple thay `GestureDetector`. **LUẬT Reduce Motion (2026-06-10, WCAG 2.3.3):** mọi animation TRANG TRÍ/VĨNH VIỄN (aurora breathe, Ken Burns, twinkle, pulse, auto-advance carousel, entrance) PHẢI check `AppMotion.reduceMotion(context)` → render frame tĩnh / tắt auto; transition theo state (switcher, expand/collapse) được giữ. **LUẬT TickerMode:** tab trong IndexedStack bọc `TickerMode(enabled: selected)`; Timer tự quản (đồng hồ, slideshow) check `TickerMode.valuesOf(context).enabled` trước khi tick. **LUẬT chữ-trắng-trên-nền-sáng:** nếu giữ chữ trắng (lý do brand) thì shadow phải là BÓNG TỐI mềm (black .28–.32 blur 8–12 offset y1) — KHÔNG halo trắng (làm chìm thêm); link/chữ nhỏ <16px trên dawnBlush dùng `textPrimary` (accentLoveDeep chỉ đạt ~2.7:1).
- Trạng thái: dev xong + analyze sạch + smoke-test simulator (guest landing) OK. CHƯA submit/commit (build 1.0(3) đang Apple review — phát hành revamp = 1.1 sau khi 1.0 duyệt). Đợt 2 (splash/counter count-up/invite reveal/gallery shimmer-stagger) chưa làm.

Tokens: radius card lớn = 28 (`cardRadius`); pill = 999; input/nút phụ = 20; tile = 22-24; profile hero = 32. Spacing: 4 · 6-8 · 12-16 · 18-24 · 20. Nút: height 52, ElevatedButton nền navy bo pill. Input: filled surfaceLight bo 20, focus viền accentLove 1.4. AppBar phẳng. FAB tròn accentLove. SnackBar floating navy bo 20.

Components tái dùng (lib/widgets): counter_card (HERO gradient sunsetRomance bo 28 — **redesign "aurora glass" 2026-06-10:** BỎ huy hiệu tim; slot `headerExtra` (tên couple) → eyebrow kẹp 2 hairline gradient → số w600 + halo trắng blur 22, divider = chấm phát sáng 5px thay vạch dọc; viền kính trắng .30 1.2px + shadow lavender bloom; 3 lớp glow "thở" chậm lệch nhịp (trắng topLeft 3.2s · lavender bottomRight 4s · coral topRight 3.6s, scale 1→1.12 + fade .85→1, flutter_animate repeat-reverse); slots `footerExtra` streak + `progressFooter` milestone; **counter = TỔNG SỐ NGÀY duy nhất + đồng hồ live (2026-06-10):** bỏ breakdown năm/tháng/ngày — hero number (dayCountStyle + halo) + nhãn "NGÀY" + `_LiveElapsedClock` **1 dòng TỔNG GIỜ "8.990 giờ 23 phút 45 giây"** (đổi 2026-06-10 từ pill HH:MM:SS — pill cũ trùng đồng hồ status bar + tốn 60px; 13.5 w600 trắng .90, NumberFormat theo locale, tick 1s, skip khi TickerMode off; anchor đầu-ngày `liveSince` khớp totalDays date-only; chỉ subtree đồng hồ rebuild) — áp cả Home lẫn guest_counter; **Home KHÔNG truyền subtitle "Bắt đầu từ"** (tĩnh, đã có ở Profile — tiết kiệm fold; guest vẫn truyền); l10n `hoursShort/minutesShort/secondsShort`; **không ảnh → scrim đen .10→.28** (chữ trắng vùng đáy #FFB6C1 hết fail); **nền ảnh couple (2026-06-10):** `photoLocalPath/photoRemoteUrl` → ảnh chung full-card qua `SharedCouplePhotoView` dưới **scrim tối ADAPTIVE theo độ sáng ảnh** (`_AdaptivePhotoBackdrop`, 2026-06-10): đo luminance Rec.601 từ decode 64px 1 lần/ảnh (cache static theo key) → map L 0.25–0.70 thành black top .08–.40 / bottom .22–.55 (đáy đậm hơn cho pill/progress), AnimatedContainer 600ms chuyển mượt khi đo xong, fail-soft về mức giữa; KHÔNG còn lớp hồng (user chốt sau 3 lượt hạ .88→.55→.34→.20→bỏ) — tông hồng do aurora/viền/pill gánh — không ảnh → card gradient thuần như cũ), animated_couple_name ("P1 ♥ P2" heart pulse 820ms), shared_couple_photo_view/shared_photo_view (loader local→network→fallback), blocking_loading_overlay, language_toggle_button, invite_action_buttons (cụm "Copy | Share" mã mời — feature invite-sharing P1; 2 biến thể onDark/sáng-rose + iconOnly; share_plus), photo_item + masonry_gallery (⚠️ MasonryGallery KHÔNG dùng — gallery là feed dọc), auth_background (không dùng).

Animation durations chuẩn: heart pulse 820ms · nav pill 320ms easeOutCubic · mode selector 260ms · header snap 250ms · dismiss 220ms · switcher 200ms. → animation mới theo 200-320ms easeOutCubic.

Layout từng screen (luồng: splash `/` → auth_gate → login/register → setup → home):
- splash / auth_gate — nền secondaryGradient, tim trắng 80px, spinner trắng; không animation, chỉ resolve route.
- login / register — nền gradient, header badge eyebrow + pageTitle, form card glass (white alpha .22 bo 28). Field label rose w700, input bo 20 prefix icon rose, nút submit rose. Register thêm: policy disclosure clickable, checkbox điều khoản (bắt buộc tick), link privacy. LanguageToggle góc trên phải.
- setup_screen — tạo/join couple. Mode selector pill trượt (AnimatedPositioned 260ms easeInOutCubic). Invite-code card (code 30px w900 ls4 + nút copy). Form card glass, date picker + photo picker (ImagePicker quality 92, preview tròn 118px). FilledButton.icon, BlockingLoadingOverlay bọc.
- home_screen — IndexedStack 3 tab + custom floating bottom nav (không dùng BottomNavigationBar): cao 84, margin 16, bo 28, BackdropFilter blur 24, gradient trắng .28→.14, viền trắng .35, 2 shadow. Pill chọn gradient sunset→accentLoveDeep trượt 320ms, icon scale 1.12 (unselected trắng .75 — nâng từ .55, nợ H6 đã đóng 2026-06-10), label 11px. 3 tab bọc `TickerMode(enabled: selected)` — tab ẩn không animate/tick. 3 tab: favorite/photo_library/person. **Tab Home (Home v2, 2026-06-10 — spec `project/features/home/design.md`):** header SLIM chỉ còn 1 hàng (**"CALENDAR LEAF" tờ lịch hôm nay** (chốt 2026-06-10 sau 3 take chip→editorial→leaf): số NGÀY 40px w600 trắng + **bóng TỐI mềm black .32 blur 10** (đổi từ halo trắng 2026-06-10 — contrast pass C1) ‖ vạch dọc 1.2×36 fade 2 đầu ‖ cột THỨ 13 w800 ls2.0 / THÁNG 13 w600 trắng .92 — cả 2 bóng tối black .28 (`DateFormat.EEEE/MMMM(locale)` uppercase) — concept lịch bloc VN, số-đối-số với day count hero; KHÔNG hộp/sparkles (key `loveHomeBadge` hết dùng nhưng giữ ARB; Gallery/Profile vẫn chip) + **bell ĐĨA TRÒN trắng đặc .92, chuông `accentLoveDeep` 21, shadow rose .25** (bề-mặt-sáng→mực-rose, 2026-06-10) `LucideIcons.bell` outline trắng 22 Semantics/InkWell — Lottie bell ĐÃ BỎ; greeting theo buổi ĐÃ BỎ 2026-06-10, AnimatedCoupleName chuyển VÀO CounterCard qua slot `headerExtra` — trắng w700 20, trên dòng "HAI BẠN ĐÃ BÊN NHAU"; KHÔNG còn title "Trang chủ" + hero glass riêng) → CounterCard (**chứa luôn milestone progress** white-on-gradient ở đáy card qua `CounterCard.progressFooter` — bỏ section "Cột mốc" riêng, 2026-06-10) → banner chờ partner (nếu waiting) → nhóm **"Hôm nay của hai đứa"** = **1 card nghi thức hợp nhất `TodayRitualCard`** (`lib/widgets/today_ritual_card.dart`, 2026-06-10 — gộp 2 card cũ; **nền TRẮNG ĐẶC r24 + shadow black .06** kiểu gallery/settings, KHÔNG glass — card nội dung dày theo đúng quy tắc; pill input fill `surfaceLight`): câu hỏi (tap-to-compose pill → bottom sheet; blur-teaser `ImageFiltered` blur 7 + 🔒 khi partner trả lời trước; done-state thu gọn 1 dòng "✓ Đọc lại", reveal tươi mới expand + confetti/Lottie) → divider → lời nhắn = **UI cổ điển + ngữ nghĩa chat** (chốt 2026-06-10 sau 3 vòng: note partner + time + dòng "Lời nhắn của bạn" 1 dòng; phong bì 💌 rose-tint khi tin partner CHƯA ĐỌC — marker Hive `love_note_seen_<coupleId>`, unseal AnimatedSwitcher 280ms; pill **"Soạn tin nhắn"** `loveNoteComposeCta` mở sheet field TRỐNG — gửi nội dung KHÁC thì cập nhật, trùng/rỗng thì pop(false) giữ nguyên; lịch sử 2 chiều ở LoveNoteHistoryScreen; hàng 2 link archive "Nhật ký | Lịch sử" ĐÃ DỜI sang "Tủ kỷ niệm" tab Hồ sơ — Profile v2 2026-06-11) → nhóm **"Kỷ niệm"** = **MEMORY CINEMA** (`lib/widgets/memory_cinema_card.dart`, chốt 2026-06-10 — thay dải polaroid): 1 card lớn 240 **full-bleed vuông cạnh KHÔNG shadow** (shadow trên full-bleed thành 2 vệt ngang — bỏ 2026-06-10) tự trình chiếu kiểu Apple Photos Memories — auto 7s/slide + crossfade 700ms + Ken Burns zoom 1.0→1.10/9s (cả 2 TẮT khi Reduce Motion; auto cũng dừng khi tab ẩn qua TickerMode), scrim đáy black .55, caption/ngày trắng + dots (active 16px), slide on-this-day DẪN ĐẦU với badge pill `accentLoveDeep .85` "{n} năm trước 💞" + ReactionCountBadge; slides LOẠI ảnh đang làm nền CounterCard (không trùng 1 ảnh 2 nơi); vuốt ngang đổi slide, **GIỮ ngón = pause** (gesture Stories, WCAG 2.2.2), tap mở đúng ảnh (hero `cinema-photo-{id}`, auto-play PAUSE khi preview mở, log `memory_cinema_opened`); cả card bọc Semantics `cinemaCardSemantics` (caption/ngày + vị trí i/n); placeholder offline = surfaceLight + icon image 34; header section chỉ còn "Xem tất cả" (`textPrimary` w700 — loveDeep chỉ 2.7:1 trên blush; camera "thì thầm" từng thêm rồi GỠ cùng ngày theo user — Home thuần xem lại, đăng ảnh = composer Gallery + empty-state); empty-state = polaroid trống + 1 dòng mời (giữ). Spacing: trong nhóm 12 / giữa nhóm 28. extendBody, nav ẩn khi bàn phím hiện. **Quy tắc mới (Home v2):** tile bấm = InkWell ripple (accentRose .08 trên nền sáng, trắng .12–.15 trên nền đậm) · nút full-width trong card = radius 16. *(Lịch sử: "contrast pass" màu từng bị revert giữa ngày 2026-06-10, nhưng đến final polish CÙNG NGÀY user duyệt đóng toàn bộ nợ contrast: "Xem tất cả" `accentLoveDeep`, banner chờ-partner card sáng chữ tối, on-this-day vào strip — S1 ĐÃ ĐÓNG. CounterCard có badge n/N + coach-mark vuốt-đổi-ảnh 1 lần `counter_bg_hint_done`.)*
- gallery_screen — feed dọc, KHÔNG grid. CustomScrollView: SliverPersistentHeader co giãn (expanded 340/compact 122, snap 250ms) chứa composer card (avatar gradient + nút thêm 1/nhiều ảnh + marquee chip 45px/s) → CTA "hôm nay" → feed card (avatar+tên+time+menu, ảnh Hero 4:5 bo 26, caption) theo tháng. Fullscreen preview: PageView swipe, InteractiveViewer pinch zoom (max 4×), drag-to-dismiss dọc (nền fade .94→.2, threshold 140px), panel info + nút edit/close.
- profile_screen (feature settings — **Profile v2, redesign 2026-06-11**, spec `features/settings/design.md` §Redesign Profile v2) — job: *danh tính + tủ kỷ vật + cổng quản trị*; mỗi con số xuất hiện đúng 1 lần. Bố cục: header Row [eyebrow chip + **icon ⚙️ `HeaderIconButton` 44/r16 GÓC PHẢI** → push Settings (thay tile full-width cuối trang, user 2026-06-11)] + title (KHÔNG subtitle) → **hero tap-to-edit** (bo 32 h220, ảnh nền blur/gradient + initials 56, tên 30 w800 + "Bắt đầu từ" dồn đáy, đĩa ✎ 36 white .92/pencil loveDeep top-right, cả card → SetupScreen + reload couple; BỎ pill đếm-ngược + avatar 72 lặp ảnh nền) → **Stats "Bức tranh hành trình" 2×2 GỐC giữ nguyên** (section card sparkles + 4 stat card tint .10 + icon squircle trắng: rose năm/coral tháng/info tổng-ngày/lavender ảnh, value 26 w800 — 2 bản thay thế "strip 3 chip" trắng & tint đều đã thử rồi REVERT cùng ngày 2026-06-11, user chốt bản gốc; không tappable, không `onOpenGallery`) → **"Tủ kỷ niệm"** (ẩn khi waiting): **1 ContentCard grouped** 3 hàng hairline Journal/LoveNoteHistory/StreakSheet (dời từ footer TodayRitualCard Home; bản 3 tile viền riêng đã gom 2026-06-11) → tile mã mời (chỉ waiting). Tile "⚙️ Cài đặt" cuối trang ĐÃ XOÁ — thay bằng icon góc phải header. Stats 2×2 + Info tiles + subtitle ĐÃ BỎ (key cũ giữ ARB). Reminders/ngôn ngữ/danger/edit-story/đăng xuất/privacy vẫn ở Settings.
- settings_screen (feature settings — **Settings v2, redesign 2026-06-11**, spec `features/settings/design.md` §Redesign Settings v2) — job: *phòng điều khiển, chỉ control*. **Grouped list phẳng** (hết card-lồng-card): `SectionHeader` ngoài + 1 ContentCard/nhóm + hàng ngăn hairline indent 58. Nhóm: **Tài khoản** (card info thuần tên/email) → **Thông báo** (gộp local+push: master toggle → nhắc câu hỏi → mốc dim/badge → custom force-open Dv6 → sub-label micro-caps "ĐẨY TỪ NGƯỜI ẤY" → 3 toggle push) → **Chung** (Ngôn ngữ · analytics D1c · privacy-policy hàng thật externalLink) → **danger zone giữ nguyên** (3 mức severity, compliance) → **Đăng xuất = button pill h52 CUỐI TRANG** (2 vòng feedback user 2026-06-11: button phải trông như button + exit action nằm cuối trang) → footer "Dear Embeiu · v{x} ({build})" (`package_info_plus`). Header BỎ subtitle. XOÁ sau Profile v2: section Kỷ niệm/Journal + tile Chỉnh sửa câu chuyện (một-lối-vào — đã có Tủ kỷ niệm + hero tap-to-edit bên Profile).
- milestone_reminders_screen — Cột mốc & kỷ niệm: tile "Giờ mặc định" (accentGold, tap → time picker → `setTime`) + 7 mốc (toggle + next-fire) + chip-giờ mỗi mốc (giờ-theo-mốc Dv8): mờ "Theo mặc định · {giờ}" khi chưa đặt riêng → tap đặt riêng; đậm rose "{giờ} ✕" khi đã đặt → ✕ về mặc định; ẩn khi mốc tắt.
